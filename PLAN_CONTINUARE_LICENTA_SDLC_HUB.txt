PLAN CONTINUARE LICENTA - SDLC HUB
Document unificat din PLAN_CONTINUARE_LICENTA_SDLC_HUB.txt si PLAN_CONTINUARE_LICENTA_SDLC_HUB_1.txt.
Generat pentru stadiul curent al aplicatiei: 18 mai 2026
Actualizat/contopit: 29 mai 2026
Update 18 mai 2026:
- Prioritatea 1 a fost inceputa si implementata functional:
  - tabela project_audit_logs;
  - endpoint preview: GET /projects/{project_id}/methodology-transition-preview?target=KANBAN;
  - endpoint apply: POST /projects/{project_id}/methodology-transition;
  - modal in Settings pentru impact preview, blockers, warnings si apply controlat;
  - migrare Alembic: 3c4d5e6f7a8b_add_project_audit_logs.py.
- Ramane de facut pentru Prioritatea 1:
  - testare manuala completa pe proiecte reale SCRUM/KANBAN/SCRUMBAN;
  - eventual afisarea Project Audit Log in UI;
  - eventual reguli mai fine pentru strategii multiple, nu doar recommended.
- Cerinte adaugate:
  - trebuie implementata pagina Account Settings pentru fiecare utilizator;
  - sidebar-ul trebuie sa ofere acces jos la setarile contului;
  - aplicatia trebuie sa primeasca iconita/logo de produs si favicon actualizat.
- Update curent dupa ultimul batch:
  - Account Settings exista la /dashboard/account;
  - avatarul se incarca din fisier local prin POST /auth/me/avatar, nu prin URL manual;
  - avatar_url este servit static din /uploads si apare in sidebar, account, team, create issue, board, backlog, task detail, comments, audit log, calendar si activity;
  - raspunsurile de task/comment/audit includ assignee_avatar_url, author_avatar_url si actor_avatar_url;
  - Activity Center foloseste actor_avatar_url;
  - Prioritatea 2 a fost inceputa: backend-ul verifica acum permission keys reale pentru task-uri, sprinturi, membri, roluri si setari.

Update 27 mai 2026:
- Am recitit planul si am verificat codul actual. Documentul istoric de mai jos era partial ramas in urma fata de implementari.
- Prioritatile implementate/partial implementate in cod acum:
  - Prioritatea 1 Methodology Transition Wizard: implementat functional cu preview/apply, blockers, warnings si audit log pe proiect.
  - Prioritatea 2 Permissions: implementat pe task-uri, sprinturi, team, settings si calendar; exista si GET /projects/{project_id}/my-permissions.
  - Prioritatea 3 Workload Balancer: implementat backend + pagina /dashboard/workload + sugestii de redistribuire bazate pe reguli/risk score.
  - Prioritatea 4 Reporting: implementat /dashboard/reports, charts cu Recharts, overview metrics si export PDF prin /projects/{id}/reports/status.pdf.
  - Prioritatea 5 Notifications: implementat model/tabela notifications, centru /dashboard/notifications, unread count in sidebar, notificari pentru assignari, mentiuni, calendar, reminders si AI risk.
  - Prioritatea 6 Documentation/Wiki: implementat /dashboard/documentation, CRUD pagini si generare documentatie din task Done.
  - Prioritatea 7 GitHub/DevOps: implementat /github/webhook cu verificare semnatura, auto-link commit -> task comment, PR open -> REVIEW, PR merged -> DONE si pagina /dashboard/devops.
  - Prioritatea 9 Account Settings: implementat cu upload local avatar, editare nume/email/parola si lista de proiecte/roluri.
  - Prioritatea 10 Iconita aplicatie: implementata in batch-ul curent.
- Batch curent branding/iconita:
  - frontend/components/brand-mark.tsx
  - frontend/app/icon.svg
  - frontend/app/apple-icon.svg
  - frontend/app/manifest.webmanifest
  - frontend/app/layout.tsx
  - frontend/app/page.tsx
  - frontend/components/dashboard/app-sidebar.tsx
- Verificari rulate dupa batch-ul curent:
  - .venv/bin/python -m compileall backend: OK
  - npm run lint in frontend: OK
  - npm run build in frontend: OK
- Batch Calendar V2 inceput dupa branding:
  - frontend/services/calendar.ts: adaugat updateProjectCalendarEvent.
  - frontend/app/dashboard/calendar/page.tsx:
    - edit event UI folosind acelasi modal ca la create;
    - butoane edit/delete pe event cards in functie de permisiuni sau autor;
    - Day view dedicat;
    - sprint start/end apar ca milestone-uri virtuale in calendar pentru SCRUM/SCRUMBAN;
    - filtru nou pentru Sprint milestones;
    - KPI nou pentru Sprint milestones.
- Verificari rulate dupa Calendar V2:
  - npm run lint in frontend: OK
  - npm run build in frontend: OK
  - ./start.sh: backend/frontend pornite
  - curl http://127.0.0.1:8000/health: OK
  - curl -I http://127.0.0.1:3000/dashboard/calendar: 200 OK
  - curl -I http://127.0.0.1:3000/icon.svg: 200 OK
- Batch WebSocket / realtime implementat:
  - backend/realtime.py: connection manager pe user si proiect, broadcast user/project events.
  - backend/main.py: endpoint WebSocket /ws?token=JWT, autentificat cu token JWT, abonare la proiectele userului.
  - backend/utils/notifications.py si backend/routers/notifications.py: broadcast notification.created/read/read_all.
  - backend/routers/tasks.py: broadcast task.changed pentru create/update/subtask/comment.
  - backend/routers/calendar.py: broadcast calendar.changed pentru create/update/delete events.
  - backend/routers/sprints.py: broadcast sprint.changed pentru create/start/complete sprint.
  - backend/routers/auth.py: broadcast user.updated la schimbare profil/avatar.
  - frontend/components/realtime/realtime-bridge.tsx: conexiune WebSocket, reconnect automat, dispatch event global si toast pentru notificari noi.
  - frontend/hooks/use-realtime-event.ts: hook comun pentru pagini.
  - frontend/app/dashboard/layout.tsx: RealtimeBridge montat global in dashboard.
  - frontend/components/dashboard/app-sidebar.tsx: unread notifications se actualizeaza live.
  - frontend/app/dashboard/notifications/page.tsx: lista de notificari se reîncarca live.
  - frontend/app/dashboard/calendar/page.tsx: calendarul se reîncarca live la task/calendar/sprint changes.
  - frontend/app/dashboard/page.tsx: dashboard metrics se reîncarca live.
  - frontend/app/dashboard/board/page.tsx: board-ul se reîncarca live la task/sprint changes.
  - frontend/app/dashboard/backlog/page.tsx: backlog-ul se reîncarca live la task/sprint changes.
  - frontend/app/dashboard/tasks/[taskId]/page.tsx: task detail se reîncarca live pentru task-ul curent.
  - frontend/app/dashboard/team/page.tsx: team se reîncarca live la user.updated.
  - verificare suplimentara in log: conexiunile WebSocket /ws?token=JWT sunt acceptate de backend.
- Verificari rulate dupa WebSocket batch:
  - .venv/bin/python -m compileall backend: OK
  - npm run lint in frontend: OK
  - npm run build in frontend: OK
  - ./start.sh: backend/frontend pornite
  - curl http://127.0.0.1:8000/health: OK
  - curl -I http://127.0.0.1:3000/dashboard: 200 OK
  - curl -I http://127.0.0.1:3000/dashboard/calendar: 200 OK
  - curl -I http://127.0.0.1:3000/manifest.webmanifest: 200 OK
  - dupa optimizarea RealtimeBridge ca sa nu reconecteze cand se actualizeaza userul, npm run lint si npm run build au ramas OK.
- Fix UX realtime dupa feedback:
  - Problema observata: nu era refresh de browser, dar unele pagini intrau in loading state complet la evenimente WebSocket si se simtea ca reload.
  - Calendar, Notifications, Task Detail si Team folosesc acum refresh silentios la evenimente realtime.
  - Calendar nu mai declanseaza reload suplimentar cand seteaza automat attendee-ul curent.
  - Team nu mai declanseaza reload suplimentar cand seteaza rolul default pentru invitatii.
  - Verificari dupa fix: npm run lint OK, npm run build OK.
- Calendar reminders:
  - frontend/services/notification.ts: generateDueTaskReminders(projectId).
  - frontend/app/dashboard/calendar/page.tsx: buton Reminders care genereaza notificari pentru task-urile due soon/overdue ale utilizatorului curent din proiect.
  - Verificari dupa reminders: .venv/bin/python -m compileall backend OK, npm run lint OK, npm run build OK, ./start.sh OK, health OK, /dashboard/calendar 200 OK.
- Calendar recurring events:
  - frontend/app/dashboard/calendar/page.tsx permite la creare: Does not repeat, Daily, Weekdays, Weekly.
  - Recurenta este expandata frontend in evenimente calendaristice normale, fara migrare DB.
  - Limitare: maximum 60 intrari generate la o creare.
  - Daily Standup preset seteaza automat titlu, tip DAILY, ora 09:15-09:30, Weekdays pe 2 saptamani si selecteaza toti membrii.
  - Verificari dupa recurring/preset: npm run lint OK, npm run build OK.
- AI V2 task intelligence:
  - backend/services/ai_service.py suporta acum GEMINI_API_KEY sau GOOGLE_API_KEY si fallback local daca nu exista cheie.
  - backend/routers/tasks.py are endpointuri:
    - POST /tasks/ai-refine pentru Spec Refiner structurat;
    - POST /tasks/ai-estimate pentru Poker Estimator/story points;
    - /tasks/ai-generate verifica AI_USE daca primeste project_id.
  - frontend/services/task.ts expune refineTaskSpec si estimateTaskStoryPoints.
  - frontend/components/dashboard/create-task-dialog.tsx are:
    - Generate with AI;
    - Refine spec;
    - Estimate story points;
    - card cu confidence/reasoning pentru estimare.
- AI V2 release notes:
  - backend/services/ai_service.py are generate_release_notes cu fallback local.
  - backend/routers/sprints.py are POST /sprints/{sprint_id}/release-notes cu permisiune AI_USE.
  - frontend/services/sprint.ts expune generateSprintReleaseNotes.
  - frontend/app/dashboard/reports/page.tsx are buton Release Notes pentru ultimul sprint inchis din velocity si afiseaza markdown-ul generat.
- AI V2 subtasks + Task Detail:
  - Create Issue poate trimite catre backend subtasks generate de Spec Refiner, nu doar text in descriere.
  - backend/schemas/task.py accepta TaskCreate.subtasks.
  - backend/routers/tasks.py creeaza Subtask + audit log pentru subtasks generate la creare.
  - frontend/app/dashboard/tasks/[taskId]/page.tsx are AI refine pentru descriere, AI estimate pentru story points si Apply checklist pentru subtasks sugerate.
- Documentation automation:
  - backend/services/documentation_service.py centralizeaza generarea/upsert-ul paginii de documentatie din task.
  - cand un task trece in Done prin update normal, backend-ul genereaza/actualizeaza automat pagina de documentatie si adauga audit log DOCUMENTATION_GENERATED.
  - cand un PR GitHub este merged si muta task-ul in Done, se genereaza/actualizeaza automat documentatia.
  - backend/routers/documentation.py trimite realtime documentation.changed pentru create/update/delete/generate.
  - frontend/app/dashboard/documentation/page.tsx asculta documentation.changed/task.changed si se actualizeaza silentios.
- Calendar V2 automatic reminders:
  - backend/config.py are setari pentru scheduler: enable_notification_scheduler, notification_scheduler_interval_seconds, notification_scheduler_initial_delay_seconds.
  - backend/main.py porneste un background loop la startup si il opreste la shutdown.
  - backend/utils/notifications.py genereaza automat notificari pentru task-uri overdue/due soon si evenimente calendaristice care incep in urmatoarele 30 minute.
  - deduplicarea se face pe notificari recente, ca sa nu se creeze spam daca userul marcheaza notificarea ca read.
  - backend/routers/notifications.py foloseste acelasi generator pentru butonul manual Reminders.
  - frontend/app/dashboard/notifications/page.tsx afiseaza corect tipurile TASK_OVERDUE, TASK_DUE_SOON, CALENDAR_REMINDER si AI_RISK.
- Realtime V2 presence:
  - backend/realtime.py transmite presence.changed cu online_user_ids pe proiect la connect/disconnect WebSocket.
  - frontend/store/use-presence-store.ts tine starea globala de prezenta.
  - frontend/components/realtime/realtime-bridge.tsx actualizeaza store-ul de presence.
  - frontend/app/dashboard/team/page.tsx afiseaza Online now si indicator/badge Online pe membri.
- Reporting V2:
  - backend/routers/projects.py calculeaza sprint_burndown pentru sprintul activ, cumulative_flow pe baza audit log/status istoric si lead_time_distribution pe prioritati.
  - frontend/services/project.ts contine tipurile ReportLeadTimePoint, ReportCumulativeFlowPoint si ReportSprintBurndownPoint.
  - frontend/app/dashboard/reports/page.tsx afiseaza grafice noi: Sprint burndown, Cumulative flow si Lead time by priority.
- Team Hierarchy V1:
  - backend/models/project.py are ProjectTeam, ProjectMember.team_id si Task.team_id.
  - migrare Alembic: backend/alembic/versions/9c0d1e2f3a4b_add_project_teams.py.
  - backend/routers/teams.py expune list/create/update/delete teams si update member team.
  - backend/routers/tasks.py accepta team_id la create/update task si face audit log cand se schimba echipa task-ului.
  - backend/schemas/task.py si backend/schemas/project.py expun team_id/team_name/team pe raspunsuri.
  - frontend/services/project.ts expune ProjectTeam si functii CRUD pentru teams.
  - frontend/app/dashboard/team/page.tsx are Team hierarchy, creare echipa/subechipa, stergere echipa si asignare membru la echipa.
  - frontend/components/dashboard/create-task-dialog.tsx poate asigna Delivery Team la creare task.
  - frontend/app/dashboard/tasks/[taskId]/page.tsx poate schimba Delivery Team din Properties.
  - frontend/app/dashboard/settings/page.tsx include permisiunea TEAM_MANAGE.
- Team Hierarchy V2:
  - /dashboard/team afiseaza arbore ierarhic vizual recursiv pentru echipe/subechipe.
  - fiecare nod de echipa afiseaza membri, task-uri asignate si avataruri pentru membrii directi.
  - rolurile de coordonare vad card "My team view" cu echipa proprie, membri directi, subechipe si task-uri.
- Calendar Recurrence V2:
  - backend/models/project.py are recurrence_series_id, recurrence_mode si recurrence_until pe CalendarEvent.
  - migrare Alembic: backend/alembic/versions/ad1e2f3a4b5c_add_calendar_recurrence.py.
  - backend/routers/calendar.py creeaza serii recurente persistente pe backend, maximum 60 aparitii, folosind daily/weekdays/weekly.
  - backend/routers/calendar.py are DELETE /calendar/{event_id}/series pentru stergerea intregii serii.
  - frontend/services/calendar.ts include recurrence metadata si deleteCalendarEventSeries.
  - frontend/app/dashboard/calendar/page.tsx trimite recurrence_mode/recurrence_until la backend si afiseaza badge SERIES; exista actiune separata pentru delete entire series.
- Verificari dupa AI V2:
  - .venv/bin/python -m compileall backend: OK
  - npm run lint in frontend: OK
  - npm run build in frontend: OK
- Verificari dupa AI subtasks + auto documentation:
  - .venv/bin/python -m compileall backend: OK
  - npm run lint in frontend: OK
  - npm run build in frontend: OK
- Verificari dupa Calendar automatic reminders + Realtime presence:
  - .venv/bin/python -m compileall backend: OK
  - npm run lint in frontend: OK
  - npm run build in frontend: OK
- Verificari dupa Reporting V2:
  - .venv/bin/python -m compileall backend: OK
  - npm run lint in frontend: OK
  - npm run build in frontend: OK
- Verificari dupa Team Hierarchy V1:
  - .venv/bin/python -m compileall backend: OK
  - npm run lint in frontend: OK
  - npm run build in frontend: OK
  - .venv/bin/alembic -c backend/alembic.ini upgrade head: OK
  - ./start.sh: OK
  - curl http://127.0.0.1:8000/health: OK
  - curl -I http://127.0.0.1:3000/dashboard/team: 200 OK
  - curl -I http://127.0.0.1:3000/dashboard/tasks/1: 200 OK
- Verificari dupa Calendar Recurrence V2:
  - .venv/bin/python -m compileall backend: OK
  - npm run lint in frontend: OK
  - npm run build in frontend: OK
  - .venv/bin/alembic -c backend/alembic.ini upgrade head: OK
  - ./start.sh: OK
  - curl http://127.0.0.1:8000/health: OK
  - curl -I http://127.0.0.1:3000/dashboard/calendar: 200 OK
- Avertisment ramas la build:
  - Next afiseaza warning Node: `--localstorage-file` was provided without a valid path. Build-ul trece; pare warning de mediu/runtime, nu eroare de cod.
- Urmatoarea prioritate recomandata dupa acest update:
  1. Admin Global: dashboard global, lista proiecte, statistici AI/API/errors si support tickets.
  2. Stabilizare UI/UX pe fluxurile principale dupa toate feature-urile mari.
  3. Verificare functionala manuala la finalul functionalitatilor principale.

Update UI/UX polish si bug pass - 3 iunie 2026:
- Directia curenta este stabilizare vizuala inainte de bug-urile de logica.
- Task 1 - Audit Log UI:
  - toate audit log-urile trebuie afisate intr-un format uman, fara `field: old -> new` brut;
  - valorile JSON, in special riscurile AI din comentarii, trebuie parsate si prezentate ca badge-uri, sumar si recomandare;
  - locuri de verificat/editat: frontend/app/dashboard/tasks/[taskId]/page.tsx, frontend/app/dashboard/activity/page.tsx, frontend/app/dashboard/page.tsx, frontend/app/dashboard/settings/page.tsx;
  - recomandare tehnica: componenta reutilizabila in frontend/components/dashboard/audit-log-event.tsx.
- Task 2 - Liste laterale si panouri:
  - AI Risk Radar din dashboard trebuie integrat mai bine cu pagina, cu densitate si spacing mai profesioniste;
  - Activity/side lists trebuie sa arate ca parti naturale din layout, nu carduri lipite separat;
  - de verificat responsive pe desktop lat, laptop si mobile.
  - Implementat in batch-ul curent:
    - frontend/app/dashboard/page.tsx: AI Risk Radar nu mai sta ca lista lunga in coloana din dreapta; dashboard-ul foloseste acum un flux mai unitar, iar radarul este afisat ca grid de carduri in pagina;
    - frontend/app/dashboard/activity/page.tsx: Activity Center a fost readus la structura anterioara cu filtre in stanga si timeline in dreapta, pastrand doar sumarul audit curat introdus la Task 1;
    - rândurile de activity din dashboard au fost facute mai elastice pentru mobile.
  - Verificari rulate: npm run lint OK, npm run build OK.
- Task 3 - Responsive pass general:
  - dashboard, tasks, task detail, activity, settings, team, calendar, reports si admin trebuie verificate pe mobile;
  - zonele cu grid/aside sticky trebuie sa cada corect pe o coloana;
  - textul lung si JSON/audit content trebuie sa faca wrap, nu overflow.
  - Batch curent Task 3:
    - frontend/components/dashboard/audit-log-event.tsx: sumarul de audit/risk nu mai forteaza o singura linie; textele lungi fac wrap controlat;
    - frontend/app/dashboard/activity/page.tsx: timeline-ul foloseste `minmax(0, 1fr)`, cardurile au `min-w-0/overflow-hidden`, avatarul si headerul se adapteaza pe mobile, iar textele lungi nu mai imping pagina in dreapta;
    - frontend/app/dashboard/activity/page.tsx: butonul Reset este acum si in headerul cardului de filtre, pentru acces rapid fara scroll pana la finalul filtrarii;
    - frontend/app/dashboard/page.tsx: dashboard-ul foloseste padding responsive, grile cu `minmax(0, 1fr)`, butoane full-width pe mobile si risk/activity cards mai rezistente la overflow;
    - frontend/app/dashboard/tasks/page.tsx: lista de task-uri intra in layout tabelar doar la `xl`, nu la `lg`, ca sa nu inghesuie coloanele cand sidebar-ul consuma spatiu; filtrele si actiunile sunt full-width pe mobile;
    - frontend/app/dashboard/tasks/[taskId]/page.tsx: header, KPI-uri, subtasks, comments si audit log au padding/grile/butoane mai sigure pe mobile, cu wrap pentru texte lungi;
    - frontend/app/dashboard/activity/page.tsx: filtrul sticky nu se mai lipeste de marginea de jos; are `max-height`, scroll intern si spatiu de jos, iar reset-ul duplicat de la final a fost eliminat;
    - frontend/app/dashboard/settings/page.tsx: header, metric cards si grilele interne folosesc padding responsive si `minmax(0, 1fr)` unde era risc de overflow;
    - frontend/app/dashboard/settings/page.tsx: randurile din WIP/Board workflow nu mai suprapun controalele peste input; actiunile Up/Down/Shown se muta pe rand separat pana exista suficient spatiu;
    - frontend/components/dashboard/audit-log-event.tsx: auditul pentru workflow_config nu mai afiseaza `[object Object]`, ci formateaza obiectele/array-urile in text citibil;
    - frontend/app/dashboard/team/page.tsx: header, metric cards, formularul de creare echipa, invitatiile si lista de membri au controale full-width pe mobile si grile mai sigure pe desktop;
    - frontend/app/dashboard/team/page.tsx: butonul Send invitation a fost mutat in cardul Invite a teammate, langa email/rol;
    - frontend/app/dashboard/calendar/page.tsx: header, actiuni, metric cards si calendar month au layout responsive; calendarul lunar foloseste scroll intern pe ecrane mici, nu overflow de pagina;
    - frontend/app/dashboard/reports/page.tsx: headerul, butoanele, metric cards si chart cards au `min-w-0`, padding responsive si butoane full-width pe mobile;
    - verificari rulate: npm run lint OK, npm run build OK.
  - Task 3 este complet pentru paginile principale non-admin. Ramane Global Admin Console ca pas separat/design major.
- Task 4 - Global Admin Console redesign:
  - refacere layout principal si sidebar admin;
  - navigatie activa clara, fara dublari;
  - support tickets cu comentarii complete, actiuni clare si accesibilitate buna.
- Task 5 - Bug-uri de logica dupa UI pass:
  - board si tranzitii intre metodologii;
  - arhivare proiect si acces readonly pe pagini;
  - flows support tickets, comentarii si stergeri;
  - realtime unde inca se simte ca reload.

Scopul fisierului:
- Sa poata fi folosit ca handoff rapid daca se termina tokenii in Codex.
- Sa explice ce este deja implementat, ce lipseste fata de master plan si in ce ordine trebuie continuat.
- Sa puna accent pe trecerile intre metodologii: SCRUM, KANBAN, SCRUMBAN.


1. STADIUL ACTUAL AL APLICATIEI

Stack actual:
- Backend: FastAPI, SQLAlchemy, Alembic, PostgreSQL, JWT, email, Gemini prin google-genai.
- Frontend: Next.js App Router, TypeScript, Tailwind, componente shadcn/Radix locale, Zustand, Axios, dnd-kit, Lucide.
- Aplicatia ruleaza local prin ./start.sh:
  - backend: http://127.0.0.1:8000
  - frontend: http://127.0.0.1:3000

Module implementate partial sau complet:
- Landing page: exista pagina publica /.
- Auth:
  - register/login/verify email/forgot password/reset password exista.
  - exista trimitere email si flux de verificare.
  - exista redirect spre verificare email cand userul nu este verificat.
- Onboarding:
  - pagina /onboarding exista.
  - utilizatorul poate intra in proiect pe baza de cod.
  - utilizatorul poate crea proiect nou prin /project-wizard.
- Project Wizard:
  - identitate proiect: nume, key, descriere.
  - AI methodology advisor: intrebari + recomandare SCRUM/KANBAN/SCRUMBAN.
  - roluri default in functie de metodologie.
  - AI role suggestions.
  - invitatii pe email pe roluri.
- Workspace / sidebar:
  - selector de proiecte/workspaces.
  - link-uri dinamice: Backlog apare doar pentru SCRUM/SCRUMBAN.
  - sign out trimite in /.
- Dashboard:
  - metrice principale: velocity/throughput, completion, my active work, team health.
  - Work Distribution.
  - Active Sprint / Flow Status in functie de metodologie.
  - Delivery Signals: overdue, due soon, critical open, review queue, unassigned, backlog.
  - AI Risk Radar bazat momentan pe reguli simple din datele task-urilor.
  - Project Activity jos, cu audit events.
- Activity Center:
  - pagina /dashboard/activity exista.
  - are filtre mai avansate si afisare moderna a auditului.
- Board:
  - pagina /dashboard/board exista.
  - drag and drop intre coloane.
  - pentru SCRUM/SCRUMBAN foloseste sprint activ.
  - pentru KANBAN functioneaza ca board continuu.
  - Complete Sprint foloseste modal modern.
- Backlog:
  - pagina /dashboard/backlog exista.
  - doar pentru SCRUM/SCRUMBAN in sidebar.
  - creare sprint cu nume, goal, start date, end date.
  - start sprint / complete sprint.
  - task-uri cu afisare mai moderna.
- Task Detail / TaskID:
  - pagina /dashboard/tasks/[taskId] exista.
  - editare titlu, descriere, status, prioritate, story points, due date, assignee.
  - properties sticky.
  - subtasks/checklist.
  - comentarii.
  - userul care a postat comentariul il poate edita/sterge.
  - delete comment foloseste modal modern.
  - audit log per task.
- Create Issue:
  - title, description, priority, assignee.
  - story points doar cand metodologia permite.
  - target date / due date.
  - AI generate description.
- Team:
  - pagina /dashboard/team exista.
  - membri, invitatii, roluri custom.
  - schimbare rol membru.
  - resend/cancel invitations.
  - delete role cu modal modern.
- Settings:
  - pagina /dashboard/settings exista.
  - modificare nume, descriere, metodologie.
  - role permissions.
  - delete project securizat prin cheia proiectului + modal.
- Calendar:
  - pagina /dashboard/calendar exista.
  - afiseaza task-uri dupa due date.
  - afiseaza evenimente/meeting-uri.
  - se pot crea evenimente cu titlu, tip, data, ora start/end, link meeting, locatie, descriere, participanti.
  - filtre pe membru, tip si "my schedule".
  - month/week/agenda view.
- Backend calendar:
  - tabela calendar_events.
  - list/create/update/delete events.
  - participantii sunt validati ca membri ai proiectului.
- Migrations importante:
  - 8f4b0e2b7d91_task_detail_tables.py
  - 1a2b3c4d5e6f_add_task_due_date.py
  - 2b7c8d9e0f1a_add_calendar_events.py


2. CE NU ESTE IMPLEMENTAT SAU ESTE DOAR PARTIAL

2.1. Metodologii si tranzitii SCRUM/KANBAN/SCRUMBAN
Stadiu actual:
- Metodologia exista pe Project.
- Sidebar-ul ascunde Backlog pentru KANBAN.
- Backend-ul blocheaza trecerea la KANBAN daca exista sprint activ.
- Create Issue ascunde story points pentru KANBAN.
- Sprint endpoints blocheaza sprinturile pentru KANBAN.

Ce lipseste:
- Nu exista inca un "Methodology Transition Wizard" real.
- Nu exista preview al impactului cand schimbi metodologia.
- Nu exista reguli complete pentru migrarea datelor intre moduri:
  - SCRUM -> KANBAN: ce se intampla cu backlog-ul, sprinturile viitoare si task-urile din sprinturi inactive.
  - KANBAN -> SCRUM: cum se creeaza primul backlog/sprint si ce task-uri intra unde.
  - SCRUM -> SCRUMBAN: se pastreaza backlog-ul si sprinturile, dar executia trebuie sa fie mai flexibila.
  - SCRUMBAN -> SCRUM: trebuie verificat daca exista task-uri neplanificate care ar trebui mutate in backlog.
- Nu exista adaptare automata a rolurilor la schimbarea metodologiei:
  - Scrum Master pentru SCRUM.
  - Flow Manager pentru KANBAN.
  - ambele sau un hibrid pentru SCRUMBAN.
- Nu exista audit log pentru schimbarea metodologiei la nivel de proiect.
- Nu exista confirm modal specific pentru schimbarea metodologiei, cu explicatii si pasi.
- Nu exista inca un document complet de verificare manuala pentru aceste tranzitii.

2.2. Task Management
Implementat:
- task title, description, priority, story points, due date, assignee, status.
- subtasks.
- comments.
- audit log.
- board drag and drop.
- subtask edit/delete din TaskId, cu audit SUBTASK_UPDATED/SUBTASK_DELETED si realtime task.changed.
- delete task cu governance: permisiune TASK_DELETE, roluri de guvernanta, confirmare prin cheia task-ului, motiv obligatoriu si Project Audit Log.
- invalidare estimare/story points cu motiv obligatoriu si audit special ESTIMATE_INVALIDATED.
- coloane Board configurabile per proiect in workflow_config: label, ordine, vizibilitate si culoare pe statusurile existente.
- descriere Rich Text reala cu Tiptap pentru creare issue si pagina TaskId.

Lipseste / partial:
- subtask-uri SDLC predefinite: Planificare, Analiza, Design, Implementare, Integrare, Testare.
- restrictii granulare bazate pe permisiuni pentru fiecare actiune UI/API.
- WIP limits pentru Kanban.
- List View tabelar.
- task teams / echipa asignata pe task, nu doar assignee individual.

2.3. AI
Implementat:
- AI methodology advisor.
- AI role suggestions.
- AI task description/spec generation in Create Issue.
- AI Risk Radar combina semnale din task-uri cu analiza automata a comentariilor pentru bottleneck/risk.
- Workload suggestions exista pe reguli/risk score, iar explicatiile sunt rafinate prin prompt AI real cand exista provider configurat.
- Workload page permite drag/drop direct intre membri pentru redistribuirea task-urilor.

Lipseste:
- Spec Refiner complet: user story + acceptance criteria + subtasks generate structurat.
- Poker Estimator: sugestie AI pentru story points.
- Release Notes: rezumat sprint generat automat.
- contorizare API calls AI pentru Global Admin.
- fallback UX mai clar cand GEMINI_API_KEY lipseste.

2.4. DevOps Integration
Implementat:
- GitHub Webhooks.
- validare semnatura webhook cu GITHUB_WEBHOOK_SECRET.
- Auto-link commit message -> task comment.
- Smart transition: PR open -> task mutat in Review.
- PR merged -> task mutat in Done.
- pagina /dashboard/devops listeaza evenimentele GitHub legate de proiect.
- pagina dedicata /dashboard/devops/pull-requests pentru Pull Requests.
- actiuni manuale de confirmare PR -> Review / Done.
- mapping automat GitHub sender_login -> user SDLC Hub pe baza de github_username, email local-part sau nume.
- configurare GitHub repository per proiect.
- flow ngrok din UI pentru webhook local.
- test/configurare webhook cu status CONNECTED/WAITING_FOR_PING/ERROR.

Lipseste / partial:
- polish/test manual pentru GitHub webhook end-to-end cu repository real.

2.5. Reporting
Implementat:
- dashboard are metrice operationale.
- activity center exista.
- pagina /dashboard/reports foloseste Recharts.
- Velocity Chart pe sprinturi inchise.
- Sprint Burndown chart.
- Sprint Burnup chart.
- lead time separat pentru Kanban / flow work.
- CFD / Cumulative Flow Diagram istoric.
- WIP history si WIP limit status.
- status/priority distribution.
- bottleneck/status age pe baza audit log.
- average cycle time.
- Export PDF raport stare proiect prin /projects/{id}/reports/status.pdf.

Lipseste / partial:
- polish UI si validare manuala pe proiecte cu date reale.

2.6. Notifications
Implementat:
- notificari in-app cu clopotel/unread count in sidebar.
- pagina /dashboard/notifications.
- @mentions in comentarii prin parser simplu.
- notificari la task assigned, mention, calendar attendee, due soon/overdue si AI risk.
- email notifications pentru task assigned, mention, calendar invite/reminder, due soon/overdue si AI risk.
- preferinte per utilizator pentru canal in-app, canal email si tipuri: task assignments, mentions, calendar, due dates, AI risk.
- model/tabela Notification.
- stare read/unread.
- WebSocket pentru update live: notification.created/read/read_all.
- realtime comments:
  - comment.created/comment.updated/comment.deleted updateaza pagina TaskId fara refetch complet.
  - typing indicator prin WebSocket cu throttle/debounce pe backend.

Lipseste / partial:
- presence online globala, daca vrem polish suplimentar.

2.7. Calendar
Implementat recent:
- evenimente/meeting-uri cu link, locatie, participanti.
- task-uri vizibile dupa due date.
- month/week/agenda.
- disponibilitate/concedii/time off pentru membri.
- warning cand un meeting se suprapune peste disponibilitatea participantilor.
- filtre pentru availability/time off.
- day view dedicat.
- edit event UI.
- sprint start/end apar ca evenimente virtuale pentru proiectele SCRUM/SCRUMBAN.

Lipseste:
- reminder notifications.
- disponibilitate/concedii.
- recurent events: Daily Standup repetitiv.
- sincronizare cu Google Calendar/Outlook este in afara scope-ului actual.

2.8. Documentatie/Wiki
Implementat:
- pagina /dashboard/documentation.
- tabela documentation_pages.
- CRUD pentru pagini.
- generare documentatie din task Done.
- legare task -> documentatie.
- istoric modificari / revision history pe documentatie.
- generare automata/upsert cand task-ul ajunge Done.

Lipseste / partial:
- polish UI si validare manuala pe flow-uri cu task-uri reale.

2.9. Team Hierarchy
Implementat:
- Team page are membri, roluri, echipe si subechipe.
- arbore ierarhic vizual recursiv.
- asignare task la echipa.
- vizualizare "My team" pentru roluri de coordonare.

Lipseste:
- polish UI si validare manuala pe proiecte cu multe echipe.

2.10. Admin Global
Implementat:
- rol global admin separat.
- interfata /admin separata de interfata principala de proiect.
- dashboard global cu proiecte/utilizatori/tickets/errors/AI usage.
- support tickets catre admin global cu comentarii si stergere.
- arhivare proiecte si read-only pe proiectele arhivate, cu Support functional.
- export CSV/PDF si panou operational.

Lipseste / partial:
- polish UI si validare manuala pe date reale.

2.11. Profil Utilizator
Implementat:
- pagina profil/account settings.
- schimbare nume, email, avatar prin upload local.
- schimbare parola din profil.
- afisare proiecte in care se afla utilizatorul si rolul pe fiecare proiect.
- preferinte notificari in Account Settings.
- sesiuni active si revoke session.
- security log in Account Settings.

Ramane de facut ulterior:
- polish UI si validare manuala pe scenarii login/logout/revoke.

Ramane de facut ulterior:
- sesiuni active / security log.

2.12. Testare si calitate
Lipseste:
- teste automate backend.
- teste frontend.
- checklist manual complet pentru fluxurile principale.
- seed data/dev fixtures.
- document de testare pentru licenta.


3. PLANIFICARE RECOMANDATA DE CONTINUARE

Prioritatea 0 - Stabilizare inainte de features noi
1. Ruleaza mereu:
   - npm run lint in frontend
   - npm run build in frontend
   - .venv/bin/python -m compileall backend
   - .venv/bin/alembic -c backend/alembic.ini upgrade head
2. Curata warning-uri/riscuri evidente:
   - frontend/app/project-wizard/page.tsx foloseste <img>; optional trecere la next/image.
   - verifica in .env diferenta intre GOOGLE_API_KEY si GEMINI_API_KEY. Codul nou foloseste settings.gemini_api_key, dar ai_service.py cauta GOOGLE_API_KEY. Recomand standardizare pe GEMINI_API_KEY sau suport pentru ambele.
3. Verifica manual principalele rute:
   - /
   - /login
   - /register
   - /onboarding
   - /project-wizard
   - /dashboard
   - /dashboard/board
   - /dashboard/backlog
   - /dashboard/tasks/[id]
   - /dashboard/activity
   - /dashboard/calendar
   - /dashboard/team
   - /dashboard/settings

Prioritatea 1 - Methodology Transition Wizard
Scop: sa ne asiguram ca trecerile intre tipuri de proiect functioneaza elegant si sigur.

Backend de facut:
1. Endpoint nou:
   - GET /projects/{project_id}/methodology-transition-preview?target=KANBAN
   - returneaza:
     - current_methodology
     - target_methodology
     - blockers
     - warnings
     - actions
     - affected_counts: active_sprints, future_sprints, backlog_tasks, sprint_tasks, story_point_tasks
2. Endpoint nou:
   - POST /projects/{project_id}/methodology-transition
   - body:
     - target_methodology
     - strategy
     - confirm_key sau confirm_text
   - executa tranzitia in tranzactie DB.
3. Reguli recomandate:
   - SCRUM -> KANBAN:
     - blocat daca exista sprint activ, sau cere complete sprint inainte.
     - pastreaza task-urile, seteaza sprint_id = null pentru task-uri neterminate daca strategia este "flatten_to_flow".
     - ascunde/dezactiveaza sprints/backlog in UI.
     - story_points raman in DB ca istoric, dar nu se mai editeaza in UI.
   - KANBAN -> SCRUM:
     - task-urile existente devin backlog.
     - userul este invitat sa creeze primul sprint.
     - story points se reactiveaza.
   - SCRUM -> SCRUMBAN:
     - pastreaza backlog si sprinturi.
     - board poate arata sprint activ, dar permite flux mai flexibil.
   - SCRUMBAN -> SCRUM:
     - verifica daca exista task-uri fara sprint.
     - pastreaza backlog, dar cere planificare sprint pentru executie.
   - SCRUMBAN -> KANBAN:
     - similar SCRUM -> KANBAN, dar backlog-ul devine intake/To Do.
4. Audit:
   - adauga tabela project_audit_logs sau foloseste un audit generic:
     - PROJECT_METHODOLOGY_CHANGED
     - old_value
     - new_value
     - actor_id
     - created_at

Frontend de facut:
1. In Settings, cand userul schimba metodologia, nu se salveaza direct.
2. Se deschide un modal "Change Methodology".
3. Modalul afiseaza:
   - impact preview.
   - blockers.
   - ce pagini/functionalitati se activeaza/dezactiveaza.
   - actiuni automate propuse.
4. Buton final:
   - "Apply Methodology Change".
5. Dupa aplicare:
   - refresh currentProject in store.
   - redirect:
     - target KANBAN -> /dashboard/board
     - target SCRUM/SCRUMBAN -> /dashboard/backlog sau /dashboard
   - sidebar se actualizeaza.

Criterii de acceptare:
- Nu pot trece la KANBAN cu sprint activ fara sa il finalizez.
- Dupa trecerea la KANBAN, Backlog dispare din sidebar.
- Dupa trecerea la SCRUM/SCRUMBAN, Backlog reapare.
- Create Issue afiseaza story points pentru SCRUM/SCRUMBAN si le ascunde pentru KANBAN.
- Sprint endpoints refuza KANBAN.
- Board nu ramane gol din greseala dupa tranzitii.

Prioritatea 2 - Permissions reale end-to-end
Scop: rolurile si permisiunile sa nu fie doar afisate, ci aplicate real peste tot.

Implementat:
1. utils/permissions.py verifica permission key din role.permissions, cu bypass pentru project owner si Project Admin.
2. Backend-ul aplica deja chei reale pentru:
   - task create/update/assign/move/comment;
   - subtask create/update prin TASK_UPDATE;
   - sprint create/start/close;
   - project update/delete/settings manage;
   - invite/resend/cancel invitation;
   - member role change/remove;
   - role create/update/delete/permissions.
3. Activity Center primeste actor_avatar_url si afiseaza poza reala.
4. UI permission-aware implementat pe:
   - Board: create task, drag/drop status si complete sprint respecta permisiunile;
   - Backlog: create task, create sprint, start sprint si mutare task in sprint respecta permisiunile;
   - Task Detail: save, status, assignee, priority/story points/due date, subtasks si comments respecta permisiunile;
   - Team: invite, change role, remove member si role management respecta permission keys;
   - Settings: accesul la settings si editarea role permissions folosesc permission keys.
5. ProjectMember.role trimite acum permissions catre frontend.

Ramane de facut:
1. Extinde verificarea si pe Calendar cu chei dedicate daca decidem ca meeting-urile nu trebuie create de oricine.
2. Adauga mesaje UX mai clare cand backend returneaza 403.
3. Verifica/completeaza rolurile existente din DB care nu au cheile noi:
   - TASK_CREATE
   - TASK_UPDATE
   - TASK_ASSIGN
   - TASK_MOVE
   - TASK_COMMENT
   - SPRINT_CREATE
   - SPRINT_START
   - SPRINT_CLOSE
   - MEMBER_INVITE
   - MEMBER_REMOVE
   - ROLE_MANAGE
   - SETTINGS_MANAGE
4. Optional: adauga un endpoint /projects/{id}/my-permissions ca UI-ul sa nu mai incarce membrii proiectului doar ca sa calculeze permisiunile curente.

Prioritatea 3 - Workload Balancer
Scop: functionalitate puternica pentru licenta si diferentiere AI.

Stadiu 27 mai 2026: implementat functional.

Backend:
- GET /projects/{project_id}/workload
- returneaza per membru:
  - active_tasks
  - story_points
  - overdue_tasks
  - review_tasks
  - risk_score
- POST /projects/{project_id}/workload/ai-suggestions
  - Gemini recomanda redistribuire.

Frontend:
- pagina /dashboard/workload
- carduri per membru.
- drag and drop task intre membri.
- filtre: sprint/current board/overdue/critical.

Criterii:
- Se vede cine este supraincarcat.
- Se poate schimba assignee prin drag/drop.
- AI explica redistribuirea.

Ramane:
- DnD direct intre membri pe workload page, daca se doreste UX mai spectaculos.
- Gemini prompt real pentru explicatii mai contextuale; acum logica este determinista/risk-score.

Prioritatea 4 - Reporting
Stadiu 27 mai 2026: implementat functional pentru overview, charts si PDF.

Implementat:
1. Velocity Chart:
   - foloseste sprinturi inchise.
   - story_points_done per sprint.
2. Bottleneck Analysis:
   - necesita timp petrecut in status.
   - extinde audit log ca sa calculezi duration per status.
3. Export PDF:
   - backend endpoint /projects/{id}/reports/status.pdf
   - fpdf2.
   - include dashboard metrics, active sprint, risks, overdue, velocity.

Ramane:
- Burndown/Burnup.
- lead time si CFD pentru Kanban.
- WIP historical warnings.

Prioritatea 5 - Notifications si @mentions
Stadiu 30 mai 2026: implementat functional cu WebSocket, email si preferinte per utilizator.

Backend:
- tabela notifications:
  - id, user_id, project_id, type, title, body, href, read_at, created_at.
- creare notificari la:
  - task assigned.
  - mention in comment.
  - event attendee added.
  - due soon/overdue.
  - AI risk.
- canal live WebSocket:
  - refresh avatar/nume in team, comments, assignee chips;
  - push notificari in-app;
  - invalidare cache task/board/calendar cand alt utilizator modifica datele.
- email notifications:
  - task assignments.
  - mentions.
  - calendar invites/reminders.
  - due soon/overdue.
  - AI risk.
- user preferences:
  - canal in-app.
  - canal email.
  - categorii task/mentions/calendar/due dates/AI risk.

Frontend:
- clopotel in topbar/sidebar.
- dropdown notifications.
- pagina /dashboard/notifications.
- parser @Name in comments.

Ramane:
- typing/comments live fara refetch complet si debounce suplimentar pentru evenimente foarte dese, optional pentru polish.

Prioritatea 6 - Documentation/Wiki
Stadiu 27 mai 2026: implementat functional.

Backend:
- tabela documentation_pages:
  - project_id, task_id optional, title, content, created_by_id, updated_at.
- endpoint generate from task.

Frontend:
- pagina /dashboard/documentation.
- la task Done se poate genera pagina.
- AI poate crea sumar tehnic.

Ramane:
- istoric editari documentatie.
- auto-trigger la trecere in Done.

Prioritatea 7 - GitHub/DevOps
Stadiu 27 mai 2026: implementat functional.

Backend:
- endpoint /github/webhook.
- verifica semnatura cu github_webhook_secret.
- parse commit messages: KEY-123.
- parse PR opened/closed.

Comportament:
- commit cu TASKKEY adauga comment automat.
- PR open muta task in REVIEW.
- PR merged poate muta task in DONE sau cere confirmare.

Frontend:
- pagina /dashboard/devops sau /dashboard/pr.
- listare PR events/task links.

Ramane:
- documentare ngrok/GitHub setup.
- mapping GitHub user -> user intern.
- pagina PR dedicata daca mai este timp.

Prioritatea 8 - Admin Global
De facut doar daca mai ramane timp:
- global role pe User.
- /admin dashboard.
- proiecte, useri, AI usage, erori, support tickets.

Prioritatea 9 - Account Settings si identitate utilizator
Scop: fiecare utilizator sa poata gestiona datele contului si sa vada contextul sau in platforma.

Backend:
- extinde User cu avatar_url.
- endpoint GET /auth/me/account-summary:
  - user;
  - projects_count;
  - projects: id, name, key, methodology, role_name, owner flag.
- endpoint PUT /auth/me:
  - full_name;
  - email.
- endpoint POST /auth/me/avatar:
  - upload fisier imagine;
  - salveaza avatarul local in /uploads/avatars;
  - actualizeaza avatar_url cu path-ul servit static.
- endpoint PUT /auth/me/password:
  - current_password;
  - new_password.

Frontend:
- pagina /dashboard/account.
- cardul utilizatorului din sidebar este link direct catre Account Settings.
- formular profil:
  - nume;
  - email;
  - upload avatar din calculator;
  - preview avatar.
- formular securitate:
  - parola curenta;
  - parola noua;
  - confirmare parola.
- sectiune "My projects":
  - lista proiecte;
  - metodologia fiecaruia;
  - rolul userului;
  - owner/project admin indicator.

Prioritatea 10 - Iconita aplicatie
Scop: branding coerent in sidebar, favicon si metadata.

Stadiu 27 mai 2026: implementat.

Implementat:
- brand mark geometric de tip hub/lifecycle loop.
- logo in sidebar.
- logo in landing page header, mockup si footer.
- metadata app icons.
- app/icon.svg.
- app/apple-icon.svg.
- app/manifest.webmanifest.

Fisiere:
- frontend/components/brand-mark.tsx
- frontend/app/layout.tsx
- frontend/app/page.tsx
- frontend/components/dashboard/app-sidebar.tsx
- frontend/app/icon.svg
- frontend/app/apple-icon.svg
- frontend/app/manifest.webmanifest

Prioritatea finala - verificare functionala manuala
Scop: dupa ce functionalitatile principale sunt gata, verificam aplicatia ca produs, direct in browser.

Pregatire:
- porneste aplicatia cu ./start.sh.
- foloseste useri reali/demo si proiecte SCRUM, KANBAN si SCRUMBAN.
- verifica si roluri diferite: Global Admin, Project Admin, Tech Lead/Flow Manager si Developer/QA.

Fluxuri minime:
- register -> verify -> login.
- onboarding -> create project cu wizard.
- schimbare metodologie SCRUM -> KANBAN -> SCRUM/SCRUMBAN.
- creare sprint, start sprint, complete sprint.
- create issue, assign, due date, story points.
- board drag/drop.
- task detail: subtask, comment edit/delete.
- calendar: create event cu link si participanti.
- team: invite/resend/cancel, role change.
- settings: update project si transition modal.
- account settings: update profile, avatar si schimbare parola.
- support tickets user/admin, comentarii si stergere.


4. PLAN DE TESTARE MANUALA PENTRU TRECERI METODOLOGIE

Pregatire:
1. Creeaza proiect SCRUM.
2. Creeaza 5 task-uri:
   - 2 in backlog, 3 in sprint.
   - story points setate.
   - due dates setate.
3. Creeaza sprint activ.

Teste:
1. SCRUM -> KANBAN cu sprint activ:
   - trebuie blocat sau sa ceara complete sprint.
   - nu trebuie sa corupa task-urile.
2. Complete sprint:
   - DONE ramane istoric.
   - unfinished se muta in backlog.
3. SCRUM -> KANBAN dupa complete:
   - Backlog dispare.
   - Board arata task-urile.
   - Create Issue nu mai arata Story Points.
   - Sprint create/start este refuzat.
4. KANBAN -> SCRUM:
   - Backlog reapare.
   - Task-urile existente sunt disponibile pentru planificare.
   - Create Sprint merge.
   - Create Issue arata Story Points.
5. SCRUM -> SCRUMBAN:
   - Backlog ramane.
   - Sprinturi raman.
   - Board functioneaza.
6. SCRUMBAN -> KANBAN:
   - aceeasi protectie ca SCRUM -> KANBAN.
7. Calendar:
   - task-urile cu due date apar.
   - meeting-urile create raman dupa refresh.
8. Dashboard:
   - Delivery Signals se actualizeaza dupa due date/status/priority.


5. PROMPT DE CONTINUARE PENTRU CHATGPT WEB

Copiaza textul de mai jos cand vrei sa continui in interfata web:

---
Lucrez la proiectul de licenta "SDLC AI Hub", in repo-ul /home/slavoliu/SDLChub.
Stack: FastAPI + SQLAlchemy + Alembic + PostgreSQL pe backend, Next.js App Router + TypeScript + Tailwind + shadcn/Radix + Zustand pe frontend.

Stadiu actual:
- Auth + email verification exista.
- Onboarding + Project Wizard cu AI methodology advisor si AI role suggestions exista.
- Dashboard, Activity, Board, Backlog, Task Detail, Team, Settings si Calendar exista.
- Task-uri au due_date, story_points, assignee, subtasks, comments, audit log.
- Calendarul are events/meetings cu link, locatie, participanti si afiseaza task due dates.
- Sidebar-ul ascunde Backlog pentru KANBAN si il arata pentru SCRUM/SCRUMBAN.
- Methodology Transition Wizard este implementat functional in Settings cu preview impact, blockers, warnings si apply controlat.
- Backend blocheaza sprinturile pentru KANBAN si blocheaza switch la KANBAN daca exista sprint activ.
- Account Settings exista, avatarul se incarca din fisier local si poza este propagata in sidebar/activity/board/backlog/team/calendar/task detail/comments.
- Permissions reale sunt aplicate pe task-uri, sprinturi, membri, roluri, setari si calendar.
- Workload Balancer exista la /dashboard/workload cu sugestii de redistribuire.
- Reports exista la /dashboard/reports cu Recharts si export PDF.
- Notifications exista la /dashboard/notifications cu unread count in sidebar.
- Documentation/Wiki exista la /dashboard/documentation.
- GitHub/DevOps exista la /dashboard/devops si /github/webhook.
- Iconita/branding aplicatie au fost adaugate in frontend/app/icon.svg, apple-icon.svg, manifest si BrandMark.

Vreau sa continuam cu:
1. Calendar V2 continuare: reminder flow mai clar si recurring events pentru Daily Standup.
2. Realtime V2 optional: presence online, debounce/refetch controlat si events mai fine pe comentarii.
3. AI V2: Poker Estimator, Spec Refiner structurat si Release Notes generate din sprint.
4. Dupa functionalitati: verificare manuala pe fluxurile principale.
5. Sa se respecte batch-urile de fisiere din sectiunea 6.1.

Te rog sa citesti PLAN_CONTINUARE_LICENTA_SDLC_HUB.txt si sa implementezi urmatorul pas.
---


6. COMENZI UTILE

Pornire:
./start.sh

Migrare DB:
.venv/bin/alembic -c backend/alembic.ini upgrade head

Backend compile:
.venv/bin/python -m compileall backend

Frontend lint:
cd frontend
npm run lint

Frontend build:
cd frontend
npm run build

Verificare health:
curl http://127.0.0.1:8000/health


6.1. BATCH-URI DE FISIERE DE VERIFICAT LA CONTINUARE

Batch A - Avatar, Account Settings si identitate
- backend/main.py
- backend/models/user.py
- backend/routers/auth.py
- backend/schemas/auth.py
- backend/schemas/user.py
- backend/alembic/versions/4d5e6f7a8b9c_add_user_avatar_url.py
- frontend/app/dashboard/account/page.tsx
- frontend/components/dashboard/app-sidebar.tsx
- frontend/components/user-avatar.tsx
- frontend/services/auth.ts
- frontend/store/use-auth-store.ts

Batch B - Avataruri propagate in proiect
- backend/models/project.py
- backend/schemas/task.py
- backend/routers/projects.py
- backend/routers/tasks.py
- backend/routers/activity.py
- frontend/services/project.ts
- frontend/services/task.ts
- frontend/services/activity.ts
- frontend/app/dashboard/activity/page.tsx
- frontend/app/dashboard/board/page.tsx
- frontend/app/dashboard/backlog/page.tsx
- frontend/app/dashboard/tasks/[taskId]/page.tsx
- frontend/app/dashboard/team/page.tsx
- frontend/app/dashboard/calendar/page.tsx
- frontend/components/dashboard/create-task-dialog.tsx

Batch C - Methodology Transition Wizard
- backend/models/project.py
- backend/routers/projects.py
- backend/alembic/versions/3c4d5e6f7a8b_add_project_audit_logs.py
- frontend/services/project.ts
- frontend/app/dashboard/settings/page.tsx
- frontend/components/dashboard/app-sidebar.tsx
- frontend/components/dashboard/create-task-dialog.tsx
- frontend/app/dashboard/board/page.tsx
- frontend/app/dashboard/backlog/page.tsx

Batch D - Permissions reale end-to-end
- backend/utils/permissions.py
- backend/routers/tasks.py
- backend/routers/sprints.py
- backend/routers/projects.py
- backend/routers/calendar.py
- backend/schemas/project.py
- frontend/lib/project-permissions.ts
- frontend/hooks/use-project-permissions.ts
- frontend/app/dashboard/settings/page.tsx
- frontend/app/dashboard/team/page.tsx
- frontend/app/dashboard/board/page.tsx
- frontend/app/dashboard/backlog/page.tsx
- frontend/app/dashboard/tasks/[taskId]/page.tsx
- frontend/app/dashboard/calendar/page.tsx

Batch E - Urmatoarea prioritate recomandata
Calendar V2:
- backend/models/project.py
- backend/schemas/calendar.py
- backend/routers/calendar.py
- backend/routers/projects.py
- frontend/services/calendar.ts
- frontend/app/dashboard/calendar/page.tsx
- frontend/components/dashboard/app-sidebar.tsx
- frontend/hooks/use-project-permissions.ts

Status Calendar V2 27 mai 2026:
- edit event UI: implementat.
- day view: implementat.
- sprint start/end virtual events: implementat frontend, pe baza getProjectSprints.
- reminder flow/recurring events: urmatorul pas ramas.

Batch F - AI V2 recomandat dupa Calendar
- backend/services/ai_service.py
- backend/services/documentation_service.py
- backend/schemas/task.py
- backend/routers/tasks.py
- backend/routers/sprints.py
- backend/routers/projects.py
- backend/routers/documentation.py
- backend/routers/github.py
- frontend/components/dashboard/create-task-dialog.tsx
- frontend/app/dashboard/tasks/[taskId]/page.tsx
- frontend/app/dashboard/backlog/page.tsx
- frontend/app/dashboard/reports/page.tsx
- frontend/app/dashboard/documentation/page.tsx

Batch G - Branding/iconita batch 27 mai 2026
- frontend/components/brand-mark.tsx
- frontend/app/icon.svg
- frontend/app/apple-icon.svg
- frontend/app/manifest.webmanifest
- frontend/app/layout.tsx
- frontend/app/page.tsx
- frontend/components/dashboard/app-sidebar.tsx

Batch H - WebSocket / realtime batch 27 mai 2026
- backend/realtime.py
- backend/main.py
- backend/utils/notifications.py
- backend/routers/notifications.py
- backend/routers/tasks.py
- backend/routers/calendar.py
- backend/routers/sprints.py
- backend/routers/auth.py
- frontend/components/realtime/realtime-bridge.tsx
- frontend/hooks/use-realtime-event.ts
- frontend/app/dashboard/layout.tsx
- frontend/components/dashboard/app-sidebar.tsx
- frontend/app/dashboard/notifications/page.tsx
- frontend/app/dashboard/calendar/page.tsx
- frontend/app/dashboard/page.tsx
- frontend/app/dashboard/board/page.tsx
- frontend/app/dashboard/backlog/page.tsx
- frontend/app/dashboard/tasks/[taskId]/page.tsx
- frontend/app/dashboard/team/page.tsx
- frontend/store/use-presence-store.ts

Batch I - Calendar automatic reminders batch 27 mai 2026
- backend/config.py
- backend/main.py
- backend/utils/notifications.py
- backend/routers/notifications.py
- frontend/app/dashboard/notifications/page.tsx

Batch J - Reporting V2 batch 27 mai 2026
- backend/routers/projects.py
- frontend/services/project.ts
- frontend/app/dashboard/reports/page.tsx

Batch K - Team Hierarchy V1 batch 28 mai 2026
- backend/alembic/versions/9c0d1e2f3a4b_add_project_teams.py
- backend/models/project.py
- backend/models/__init__.py
- backend/main.py
- backend/routers/teams.py
- backend/routers/projects.py
- backend/routers/tasks.py
- backend/schemas/team.py
- backend/schemas/project.py
- backend/schemas/task.py
- frontend/services/project.ts
- frontend/services/task.ts
- frontend/app/dashboard/team/page.tsx
- frontend/app/dashboard/settings/page.tsx
- frontend/app/dashboard/tasks/[taskId]/page.tsx
- frontend/components/dashboard/create-task-dialog.tsx

Batch L - Calendar Recurrence V2 batch 29 mai 2026
- backend/alembic/versions/ad1e2f3a4b5c_add_calendar_recurrence.py
- backend/models/project.py
- backend/schemas/calendar.py
- backend/routers/calendar.py
- frontend/services/calendar.ts
- frontend/app/dashboard/calendar/page.tsx

Batch M - Global Admin V1/V2 batch 29 mai 2026
Implementat:
- flag is_global_admin pe user; primul utilizator existent este promovat prin migrare, iar primul cont nou dintr-o baza goala devine automat Global Admin.
- consola separata /admin, cu layout propriu, sidebar propriu si fara dependenta de workspace/proiect curent.
- /dashboard/admin a ramas doar redirect legacy catre /admin.
- in sidebar-ul de proiect exista doar o iesire jos catre "Global Admin Console", vizibila doar pentru Global Admin.
- metrici platforma: users, projects, tasks, tichete deschise, erori HTTP 4xx/5xx in ultimele 24h, status AI configurat.
- lista proiecte globale cu owner, metodologie, membri si task-uri.
- lista utilizatori in consola, cu activare/dezactivare si promovare/retragere Global Admin; exista protectie sa nu ramana platforma fara Global Admin si sa nu iti dezactivezi propriul cont.
- suport tickets: tabela backend, creare din /dashboard/support de catre utilizatori, listare in /admin, schimbare status si prioritate din consola.
- log automat de erori HTTP 4xx/5xx prin middleware FastAPI.

Fisiere batch M:
- backend/alembic/versions/be2f3a4b5c6d_add_global_admin_console.py
- backend/models/user.py
- backend/models/admin.py
- backend/models/__init__.py
- backend/schemas/auth.py
- backend/schemas/user.py
- backend/schemas/admin.py
- backend/routers/auth.py
- backend/routers/admin.py
- backend/main.py
- frontend/services/auth.ts
- frontend/store/use-auth-store.ts
- frontend/services/admin.ts
- frontend/components/dashboard/app-sidebar.tsx
- frontend/app/admin/layout.tsx
- frontend/app/admin/page.tsx
- frontend/app/dashboard/admin/page.tsx
- frontend/app/dashboard/support/page.tsx

Verificari batch M:
- .venv/bin/alembic -c backend/alembic.ini upgrade head: OK
- .venv/bin/python -m compileall backend: OK
- npm run lint: OK
- npm run build: OK, ramane doar warning-ul existent Next `--localstorage-file` fara path valid.
- ./start.sh: OK
- curl http://127.0.0.1:8000/health: OK
- curl -I http://127.0.0.1:3000/admin: 200 OK
- curl -I http://127.0.0.1:3000/dashboard/support: 200 OK

Batch N - Support Ticketing si Global Admin UX polish batch 29 mai 2026
Implementat:
- suport tickets au acum comentarii persistente in baza de date.
- utilizatorul vede dupa trimitere tichetul creat in /dashboard/support, cu status/progres, lista tichete proprii si conversatie.
- utilizatorul poate adauga comentarii pe tichetul propriu.
- Global Admin poate raspunde direct pe cardurile din /admin; comentariile admin sunt marcate separat.
- /admin Support Desk a fost refacut ca board pe statusuri: OPEN, IN_PROGRESS, RESOLVED, CLOSED.
- Global Admin poate schimba status si prioritate din board.
- sidebar-ul /admin a fost redesenat ca operations rail cu zone clare: Command Center, Identity, Project Registry, Support Desk, System Health.
- acces backend controlat: userii vad doar tichetele lor, Global Admin vede toate tichetele.

Fisiere batch N:
- backend/alembic/versions/cf3a4b5c6d7e_add_support_ticket_comments.py
- backend/models/admin.py
- backend/models/__init__.py
- backend/schemas/admin.py
- backend/routers/admin.py
- frontend/services/admin.ts
- frontend/app/dashboard/support/page.tsx
- frontend/app/admin/layout.tsx
- frontend/app/admin/page.tsx

Verificari batch N:
- .venv/bin/alembic -c backend/alembic.ini upgrade head: OK
- .venv/bin/python -m compileall backend: OK
- npm run lint: OK
- npm run build: OK, ramane doar warning-ul existent Next `--localstorage-file` fara path valid.
- ./start.sh: OK
- curl http://127.0.0.1:8000/health: OK
- curl -I http://127.0.0.1:3000/admin: 200 OK
- curl -I http://127.0.0.1:3000/dashboard/support: 200 OK

Batch O - Methodology Transition Hardening batch 29 mai 2026
Implementat:
- schimbarea metodologiei se face doar prin endpoint-ul dedicat /projects/{id}/methodology-transition; update-ul simplu de proiect refuza modificari directe de methodology.
- tranzitia spre KANBAN poate inchide automat sprintul activ si muta task-urile neterminate in flow, fara sa blocheze schimbarea.
- backend-ul refuza in KANBAN story_points si sprint_id pe create/update task, ca sa nu ramana date invalide din UI stale sau requesturi manuale.
- /dashboard/backlog afiseaza un ecran dedicat Kanban Flow si link catre Board daca proiectul este KANBAN.
- Board-ul ascunde story points pentru KANBAN si le pastreaza vizibile pentru SCRUM/SCRUMBAN.
- proiect.changed se emite prin realtime la schimbari de settings/metodologie.
- RealtimeBridge actualizeaza currentProject in store cand primeste project.changed, deci sidebarul/metodologia se sincronizeaza fara refresh manual.

Fisiere batch O:
- backend/routers/projects.py
- backend/routers/tasks.py
- frontend/components/realtime/realtime-bridge.tsx
- frontend/app/dashboard/backlog/page.tsx
- frontend/app/dashboard/board/page.tsx

Verificari batch O:
- .venv/bin/python -m compileall backend: OK
- npm run lint: OK
- npm run build: OK, ramane doar warning-ul existent Next `--localstorage-file` fara path valid.
- ./start.sh: OK
- curl http://127.0.0.1:8000/health: OK
- curl -I http://127.0.0.1:3000/dashboard/settings: 200 OK
- curl -I http://127.0.0.1:3000/dashboard/backlog: 200 OK
- curl -I http://127.0.0.1:3000/dashboard/board: 200 OK

Batch P - WIP Limits pentru Kanban/Scrumban batch 29 mai 2026
Implementat:
- proiectele au workflow_config persistat in baza de date.
- endpoint nou PUT /projects/{project_id}/workflow pentru configurarea limitelor WIP.
- settings afiseaza card WIP limits pentru KANBAN si SCRUMBAN.
- limite configurabile per status: TODO, IN_PROGRESS, REVIEW, DONE; camp gol inseamna fara limita.
- board-ul incarca workflow_config din project detail.
- board-ul afiseaza badge WIP x/y si border rosu + icon de alerta cand o coloana depaseste limita.
- realtime project.changed sincronizeaza si workflow_config in currentProject.
- settings marcheaza permisiunile de Sprint ca "Disabled by Kanban" si dezactiveaza toggle-urile cand metodologia este KANBAN.

Fisiere batch P:
- backend/alembic/versions/d0e1f2a3b4c5_add_project_workflow_config.py
- backend/models/project.py
- backend/routers/projects.py
- frontend/services/project.ts
- frontend/components/realtime/realtime-bridge.tsx
- frontend/app/dashboard/settings/page.tsx
- frontend/app/dashboard/board/page.tsx

Verificari batch P:
- .venv/bin/alembic -c backend/alembic.ini upgrade head: OK
- .venv/bin/python -m compileall backend: OK
- npm run lint: OK
- npm run build: OK, ramane doar warning-ul existent Next `--localstorage-file` fara path valid.
- ./start.sh: OK
- curl http://127.0.0.1:8000/health: OK
- curl -I http://127.0.0.1:3000/dashboard/settings: 200 OK
- curl -I http://127.0.0.1:3000/dashboard/board: 200 OK

Batch Q - Board reload loop fix si redesign partial batch 29 mai 2026
Problema observata:
- backend.log arata requesturi repetate in bucla pe /projects/mine, /projects/1, /sprints/project/1 si /tasks/project/1?view=board.
- cauza principala era in frontend/app/dashboard/board/page.tsx: loadBoard facea setCurrentProject(freshProject) la fiecare incarcare; store-ul se schimba cu un obiect nou, callback-ul loadBoard se recrea si useEffect-ul pornea din nou.

Implementat:
- functie projectSnapshotChanged ca setCurrentProject sa fie apelat doar cand proiectul chiar s-a schimbat.
- board-ul pastreaza sincronizarea cu project detail, dar nu mai retrigger-uie incarcarea in lant.
- header board redesenat: titlu cu icon, metodologie/status, metrici Active/In progress/Review/Done si badge Flow healthy/WIP alerts.
- eliminat butonul inert "Filters".
- coloane si task cards curatate vizual: border/radius mai profesionist, badges compacte, WIP alert in headerul coloanei.
- board-ul ramane compatibil cu SCRUM, KANBAN si SCRUMBAN.

Fisiere batch Q:
- frontend/app/dashboard/board/page.tsx

Verificari batch Q:
- .venv/bin/python -m compileall backend: OK
- npm run lint: OK
- npm run build: OK, ramane doar warning-ul existent Next `--localstorage-file` fara path valid.
- ./start.sh: OK
- curl http://127.0.0.1:8000/health: OK
- curl -I http://127.0.0.1:3000/dashboard/board: 200 OK

Batch R - Global Admin AI Usage si export CSV batch 29 mai 2026
Implementat:
- tabel nou ai_usage_logs pentru contorizarea reala a cererilor AI.
- helper backend record_ai_usage, folosit de endpoint-urile principale AI: methodology advisor, AI roles, task generate, spec refiner, poker estimator, release notes si workload balancer.
- consola Global Admin afiseaza AI requests total si AI requests in ultimele 24h.
- endpoint nou GET /admin/ai-usage pentru auditul ultimelor evenimente AI.
- sectiune noua AI Usage in /admin, cu feature, provider/source, proiect, utilizator, status si data.
- sidebar-ul /admin are link dedicat catre AI Usage.
- export CSV din consola pentru projects, users si ai-usage.

Fisiere batch R:
- backend/alembic/versions/e1f2a3b4c5d6_add_ai_usage_logs.py
- backend/models/admin.py
- backend/models/__init__.py
- backend/schemas/admin.py
- backend/utils/ai_usage.py
- backend/routers/admin.py
- backend/routers/projects.py
- backend/routers/sprints.py
- backend/routers/tasks.py
- frontend/services/admin.ts
- frontend/app/admin/layout.tsx
- frontend/app/admin/page.tsx

Verificari batch R:
- .venv/bin/alembic -c backend/alembic.ini upgrade head: OK
- .venv/bin/python -m compileall backend: OK
- npm run lint: OK
- npm run build: OK, ramane doar warning-ul existent Next `--localstorage-file` fara path valid.
- ./start.sh: OK
- curl http://127.0.0.1:8000/health: OK
- curl -I http://127.0.0.1:3000/admin: 200 OK
- backend.log: /admin/overview, /admin/projects, /admin/users, /admin/tickets, /admin/errors si /admin/ai-usage raspund 200 OK.

Batch S - Global Admin redesign si proiecte arhivabile batch 29 mai 2026
Implementat:
- proiectele au flag persistent is_archived.
- /projects/mine nu mai returneaza proiectele arhivate, ca utilizatorii sa nu le vada in workspace-ul normal.
- endpoint Global Admin PUT /admin/projects/{project_id}/archive pentru arhivare/dezarhivare.
- endpoint Global Admin DELETE /admin/projects/{project_id} cu confirmare pe cheia proiectului.
- stergerea din Global Admin curata task-uri, subtasks, comments, audit logs, sprints, invitatii, membri, echipe, roluri, calendar events, documentation pages si GitHub events; AI usage logs si notificari raman pentru audit, dar se detaseaza de proiect.
- /admin sidebar refacut: mai compact, fara cardul inutil "Separate Admin Surface", navigatie cu hash activ pe sectiuni.
- /admin header refacut ca operation center cu status strip: active projects, archived, critical tickets, server errors.
- Project Registry refacut ca tabel operational, cu filtre ACTIVE/ARCHIVED/ALL, export CSV, archive/restore si delete.
- delete project foloseste modal Radix/shadcn, nu confirm nativ de browser.
- export CSV pentru users si AI usage mutat contextual in sectiunile lor.

Fisiere batch S:
- backend/alembic/versions/f2a3b4c5d6e7_add_project_archive_flag.py
- backend/models/project.py
- backend/schemas/project.py
- backend/schemas/admin.py
- backend/routers/projects.py
- backend/routers/admin.py
- frontend/services/admin.ts
- frontend/app/admin/layout.tsx
- frontend/app/admin/page.tsx

Verificari batch S:
- .venv/bin/alembic -c backend/alembic.ini upgrade head: OK
- .venv/bin/python -m compileall backend: OK
- npm run lint: OK
- npm run build: OK, ramane doar warning-ul existent Next `--localstorage-file` fara path valid.
- ./start.sh: OK
- curl http://127.0.0.1:8000/health: OK
- curl -I http://127.0.0.1:3000/admin: 200 OK

Batch T - Global Admin sidebar redesign operational batch 29 mai 2026
Implementat:
- sidebar-ul /admin a fost schimbat structural, nu doar stilizat: icon rail separat + panou operational.
- icon rail rapid pentru Command Center, Identity, Project Registry, Support Desk, AI Usage si System Health.
- panoul lateral afiseaza status Operational si modul AI curent: configurat sau fallback.
- panoul lateral incarca metrici live din /admin/overview: users, projects, open tickets, errors last 24h.
- zona Work areas are descrieri clare si indicator vizual activ pentru hash-ul curent.
- zona Quick Export ofera exporturi directe CSV pentru projects, users si AI usage.
- daca exista erori HTTP in ultimele 24h, sidebar-ul afiseaza un callout vizibil.
- user profile si Back to workspace au ramas jos, iar Sign Out a fost mutat in rail ca actiune globala.
- background-ul /admin a fost intunecat si uniformizat cu noua suprafata operationala.

Fisiere batch T:
- frontend/app/admin/layout.tsx

Verificari batch T:
- .venv/bin/python -m compileall backend: OK
- npm run lint: OK
- npm run build: OK, ramane doar warning-ul existent Next `--localstorage-file` fara path valid.
- ./start.sh: OK
- curl http://127.0.0.1:8000/health: OK
- curl -I http://127.0.0.1:3000/admin: 200 OK

Batch U - Admin sidebar fix, support delete si proiect arhivat read-only batch 29 mai 2026
Implementat:
- /admin sidebar nu mai dubleaza navigatia: a ramas o singura zona Work areas, cu icon + text.
- starea activa din sidebar se actualizeaza si la click pe hash-uri, nu doar la hashchange.
- link-ul activ are culoare evidenta: fundal albastru si icon alb.
- suport backend: DELETE /admin/tickets/{ticket_id} pentru stergere tichet; utilizatorul poate sterge doar tichetele proprii, Global Admin poate sterge orice tichet.
- suport backend: DELETE /admin/tickets/{ticket_id}/comments/{comment_id}; utilizatorul poate sterge doar comentariile proprii, Global Admin poate sterge orice comentariu.
- /dashboard/support: utilizatorul poate sterge tichetul propriu si comentariile proprii.
- /dashboard/support: conversatia afiseaza toate comentariile intr-o zona scrollabila, nu se mai opreste la 2-3 comentarii.
- /admin Support Desk: Global Admin vede toate comentariile unui tichet intr-o zona scrollabila, poate sterge comentarii si tichete.
- proiectele arhivate raman accesibile in /projects/mine.
- /dashboard/layout afiseaza banner persistent cand proiectul curent este arhivat.
- continutul dashboard-ului pentru proiect arhivat este read-only vizual si functional prin dezactivarea interactiunilor din main content.
- realtime project.changed pentru arhivare trimite acum proiectul actualizat, inclusiv is_archived, ca bannerul/read-only sa se sincronizeze fara refresh.

Fisiere batch U:
- backend/routers/admin.py
- backend/routers/projects.py
- frontend/app/admin/layout.tsx
- frontend/app/admin/page.tsx
- frontend/app/dashboard/layout.tsx
- frontend/app/dashboard/support/page.tsx
- frontend/components/realtime/realtime-bridge.tsx
- frontend/services/admin.ts
- frontend/services/project.ts

Verificari batch U:
- .venv/bin/python -m compileall backend: OK
- npm run lint: OK
- npm run build: OK, ramane doar warning-ul existent Next `--localstorage-file` fara path valid.
- ./start.sh: OK
- curl http://127.0.0.1:8000/health: OK
- curl -I http://127.0.0.1:3000/admin: 200 OK
- curl -I http://127.0.0.1:3000/dashboard/support: 200 OK

Batch V - Fix follow-up sidebar si arhivare support-only batch 29 mai 2026
Implementat:
- /admin sidebar nu mai marcheaza nicio sectiune ca activa; toate linkurile au acelasi stil neutru si doar hover state.
- "Back to workspace" din /admin este acum buton explicit cu router.push("/dashboard"), nu link care putea fi afectat de starea hash-ului.
- /dashboard/layout permite interactiuni pe /dashboard/support chiar daca proiectul curent este arhivat.
- pentru proiect arhivat, restul paginilor din dashboard raman read-only/dezactivate: board, backlog, team, settings etc.
- dashboard layout sincronizeaza snapshot-ul proiectului din /projects/mine chiar daca exista deja currentProject in store, ca is_archived sa nu ramana stale.
- _serialize_project include is_archived, deci getProject/detail, dashboard summaries si realtime payload-urile au starea corecta.

Fisiere batch V:
- backend/routers/projects.py
- frontend/app/admin/layout.tsx
- frontend/app/dashboard/layout.tsx

Verificari batch V:
- .venv/bin/python -m compileall backend: OK
- npm run lint: OK
- npm run build: OK, ramane doar warning-ul existent Next `--localstorage-file` fara path valid.
- ./start.sh: OK
- curl http://127.0.0.1:8000/health: OK
- curl -I http://127.0.0.1:3000/admin: 200 OK
- curl -I http://127.0.0.1:3000/dashboard/support: 200 OK

Batch W - Admin sidebar accesibil si permissions polish Kanban batch 29 mai 2026
Implementat:
- /admin sidebar a fost compactat ca sa incapa in viewport: Quick Export a fost scos din lateral, iar metricile si nav-ul au spacing redus.
- "Back to workspace" este acum sus in sidebar, imediat sub brand, ca actiune primara mereu vizibila.
- footer-ul din sidebar ramane doar pentru profil si Sign Out, deci nu mai impinge actiunile importante sub ecran.
- backend-ul normalizeaza permisiunile de sprint pentru proiectele KANBAN: SPRINT_CREATE, SPRINT_START si SPRINT_CLOSE sunt fortate false.
- normalizarea se aplica la my-permissions, lista de roluri, create role, update role si endpoint-ul dedicat de update permissions.
- serialize_member intoarce rolurile normalizate in functie de metodologia proiectului.
- UI-ul continua sa marcheze permisiunile Sprint ca "Disabled by Kanban", dar acum backend-ul impune aceeasi regula.

Fisiere batch W:
- frontend/app/admin/layout.tsx
- backend/routers/projects.py

Verificari batch W:
- .venv/bin/python -m compileall backend: OK
- npm run lint: OK
- npm run build: OK, ramane doar warning-ul existent Next `--localstorage-file` fara path valid.
- ./start.sh: OK
- curl http://127.0.0.1:8000/health: OK
- curl -I http://127.0.0.1:3000/admin: 200 OK
- curl -I http://127.0.0.1:3000/dashboard: 200 OK
- curl -I http://127.0.0.1:3000/dashboard/support: 200 OK

Batch X - AI Usage filters pentru Global Admin batch 29 mai 2026
Implementat:
- endpoint-ul GET /admin/ai-usage accepta filtre optionale: feature, status, project_id, days si limit.
- filtrarea backend limiteaza corect rezultatele dupa feature, status SUCCESS/ERROR, proiect si perioada.
- frontend/services/admin.ts are tip AdminAiUsageFilters si trimite query params catre endpoint.
- sectiunea AI Usage din /admin are filtre vizibile pentru Feature, Status, Project si Period.
- AI Usage poate fi filtrat si combinat cu search-ul global existent.
- Reset curata rapid toate filtrele AI Usage.

Fisiere batch X:
- backend/routers/admin.py
- frontend/services/admin.ts
- frontend/app/admin/page.tsx

Verificari batch X:
- .venv/bin/python -m compileall backend: OK
- npm run lint: OK
- npm run build: OK, ramane doar warning-ul existent Next `--localstorage-file` fara path valid.
- ./start.sh: OK
- curl http://127.0.0.1:8000/health: OK
- curl -I http://127.0.0.1:3000/admin: 200 OK

Batch Y - Plan unificat si Global Admin PDF report batch 29 mai 2026
Implementat:
- cele doua fisiere PLAN_CONTINUARE_LICENTA_SDLC_HUB*.txt au fost contopite intr-un singur document canonic: PLAN_CONTINUARE_LICENTA_SDLC_HUB.txt.
- PLAN_CONTINUARE_LICENTA_SDLC_HUB_1.txt a fost eliminat ca duplicat.
- planul de verificare finala ramane manual, direct in browser.
- backend-ul are endpoint nou GET /admin/export/platform.pdf pentru raport Global Admin.
- raportul PDF include snapshot platforma, proiecte, support/incidents, AI usage si error trail.
- frontend/services/admin.ts are downloadAdminPlatformPdf.
- /admin are buton PDF Report in header, langa search si Refresh.

Fisiere batch Y:
- PLAN_CONTINUARE_LICENTA_SDLC_HUB.txt
- backend/routers/admin.py
- frontend/services/admin.ts
- frontend/app/admin/page.tsx

Verificari batch Y:
- .venv/bin/python -m compileall backend: OK
- npm run lint: OK
- npm run build: OK, ramane doar warning-ul existent Next `--localstorage-file` fara path valid.
- ./start.sh: OK
- curl http://127.0.0.1:8000/health: OK
- curl -I http://127.0.0.1:3000/admin: 200 OK
- curl cu token Global Admin la /admin/export/platform.pdf: 200 application/pdf.

Batch Z - AI V2 polish: estimator fallback, subtasks SDLC si Release Notes pe sprint batch 29 mai 2026
Implementat:
- reparat fallback-ul local pentru Poker Estimator; anterior putea intoarce None cand Gemini nu era disponibil.
- fallback-ul de subtasks genereaza acum checklist pe model SDLC: Planning, Analysis, Design, Implementation, Integration, Testing.
- pagina TaskId salveaza imediat story points estimate in backend prin updateTask, cu audit/realtime, nu doar local.
- Backlog/Sprint Planning are buton Release Notes pe fiecare sprint pentru userii cu AI_USE.
- Release Notes se deschid intr-un modal modern cu summary, source si markdown scrollabil.

Fisiere batch Z:
- backend/services/ai_service.py
- frontend/app/dashboard/backlog/page.tsx
- frontend/app/dashboard/tasks/[taskId]/page.tsx

Verificari batch Z:
- .venv/bin/python -m compileall backend: OK
- npm run lint: OK
- npm run build: OK, ramane doar warning-ul existent Next `--localstorage-file` fara path valid.
- ./start.sh: OK
- curl http://127.0.0.1:8000/health: OK
- curl -I http://127.0.0.1:3000/dashboard/backlog: 200 OK
- curl -I http://127.0.0.1:3000/dashboard/tasks/1: 200 OK
- POST /tasks/ai-estimate cu token Global Admin: OK, raspuns cu story_points/confidence/reasoning/risk_factors.

Batch AA - AI V2 apply refined spec si fallback feedback batch 29 mai 2026
Implementat:
- TaskId foloseste acum "Refine & save"; spec-ul rafinat se salveaza imediat in backend prin updateTask.
- salvarea spec-ului rafinat intra in audit/realtime, la fel ca modificarile normale de descriere.
- TaskId afiseaza un mesaj vizibil dupa refine: succes Gemini sau fallback local, cu source badge.
- subtasks sugerate pastreaza sursa ultimei rafinari AI/fallback.
- Create Issue afiseaza explicit daca spec-ul a fost generat cu Gemini sau cu fallback local.
- feedback-ul de fallback explica faptul ca Gemini a fost indisponibil sau raspunsul a fost invalid.

Fisiere batch AA:
- frontend/app/dashboard/tasks/[taskId]/page.tsx
- frontend/components/dashboard/create-task-dialog.tsx

Verificari batch AA:
- .venv/bin/python -m compileall backend: OK
- npm run lint: OK
- npm run build: OK, ramane doar warning-ul existent Next `--localstorage-file` fara path valid.
- ./start.sh: OK
- curl http://127.0.0.1:8000/health: OK
- curl -I http://127.0.0.1:3000/dashboard/tasks/1: 200 OK
- curl -I http://127.0.0.1:3000/dashboard/backlog: 200 OK

Batch AB - Project-owned AI key criptat batch 29 mai 2026
Implementat:
- proiectele au acum setari AI per proiect: ai_provider_mode, ai_provider si ai_api_key_encrypted.
- migrare Alembic noua: a3b4c5d6e7f8_add_project_ai_settings.py.
- cheia custom Gemini a proiectului este criptata in DB cu Fernet, cheia de criptare fiind derivata din SECRET_KEY.
- cheia nu este trimisa niciodata inapoi in frontend; UI primeste doar has_project_key/platform_configured.
- owner-ul proiectului poate alege intre Platform AI key si Project-owned key din Project Settings.
- owner-ul poate salva cheia, testa providerul si sterge cheia proiectului.
- endpoint-uri noi: GET/PUT/POST test pe /projects/{project_id}/ai-settings.
- task AI endpoints si sprint Release Notes folosesc cheia proiectului cand proiectul este setat pe PROJECT.
- daca cheia proiectului nu poate fi decriptata, backend-ul nu cade silentios pe cheia platformei pentru acel proiect.

Fisiere batch AB:
- backend/models/project.py
- backend/alembic/versions/a3b4c5d6e7f8_add_project_ai_settings.py
- backend/utils/secret_crypto.py
- backend/services/ai_service.py
- backend/routers/projects.py
- backend/routers/tasks.py
- backend/routers/sprints.py
- frontend/services/project.ts
- frontend/app/dashboard/settings/page.tsx

Verificari batch AB:
- .venv/bin/python -m compileall backend: OK
- .venv/bin/alembic -c backend/alembic.ini upgrade head: OK
- npm run lint: OK
- npm run build: OK, ramane doar warning-ul existent Next `--localstorage-file` fara path valid.
- ./start.sh: OK
- curl http://127.0.0.1:8000/health: OK
- curl -I http://127.0.0.1:3000/dashboard/settings: 200 OK
- GET /projects/1/ai-settings cu token Global Admin/owner: OK.
- PUT /projects/1/ai-settings mode PLATFORM: OK.

Batch AC - Project-owned AI provider generic si Release Notes handoff batch 29 mai 2026
Implementat:
- Project-owned AI nu mai este limitat la Gemini.
- owner-ul poate denumi providerul AI din Project Settings.
- proiectul retine provider type: GEMINI sau OPENAI_COMPATIBLE.
- pentru provider custom/OpenAI-compatible se pot seta base_url si model.
- cheia ramane criptata in DB; frontend-ul primeste doar has_project_key si metadate non-secrete.
- backend/services/ai_service.py are adaptor pentru Gemini si adaptor HTTP pentru OpenAI-compatible chat completions.
- task AI endpoints si sprint Release Notes folosesc providerul proiectului cand proiectul este setat pe PROJECT.
- adaugata migrare Alembic: b4c5d6e7f8a9_add_generic_project_ai_provider.py.
- Release Notes din Backlog/Sprint Planning au actiuni: Copy markdown, Export .md, Publish to docs.
- Release Notes din Reports au aceleasi actiuni: Copy markdown, Export .md, Publish to docs.
- Publish to docs creeaza pagina in Documentation/Wiki cu markdown-ul generat.

Fisiere batch AC:
- backend/models/project.py
- backend/alembic/versions/b4c5d6e7f8a9_add_generic_project_ai_provider.py
- backend/services/ai_service.py
- backend/routers/projects.py
- backend/routers/tasks.py
- backend/routers/sprints.py
- frontend/services/project.ts
- frontend/app/dashboard/settings/page.tsx
- frontend/app/dashboard/backlog/page.tsx
- frontend/app/dashboard/reports/page.tsx

Verificari batch AC:
- .venv/bin/python -m compileall backend: OK
- .venv/bin/alembic -c backend/alembic.ini upgrade head: OK
- npm run lint: OK
- npm run build: OK, ramane doar warning-ul existent Next `--localstorage-file` fara path valid.
- ./start.sh: OK
- curl http://127.0.0.1:8000/health: OK
- curl -I http://127.0.0.1:3000/dashboard/settings: 200 OK
- curl -I http://127.0.0.1:3000/dashboard/backlog: 200 OK
- curl -I http://127.0.0.1:3000/dashboard/reports: 200 OK
- GET /projects/1/ai-settings cu token owner: OK.
- PUT /projects/1/ai-settings cu provider OPENAI_COMPATIBLE, provider_name, base_url, model si api_key: OK, cheia nu este returnata.
- proiectul demo a fost readus pe PLATFORM/GEMINI dupa smoke.

Batch AD - AI Usage ERROR polish si provider labels batch 30 mai 2026
Context:
- raspunsul primit din ChatGPT web a confirmat ca aplicatia este aproape feature complete si ca urmatoarea directie buna este polish/stabilizare, nu module mari noi.

Implementat:
- cand AI-ul raspunde invalid, providerul custom e configurat gresit sau apare timeout/error, fallback-ul ramane disponibil pentru user.
- acele cazuri sunt inregistrate in AI Usage cu status ERROR si detaliu de eroare, nu ca SUCCESS simplu.
- adaugat helper frontend formatAiSource/isAiFallback pentru etichete prietenoase.
- UI-ul nu mai afiseaza surse brute precum project_openai_compatible:Demo, ci etichete precum Demo (Openai Compatible), Gemini sau Fallback after provider error.
- aplicat provider labels in Create Issue, TaskId, Backlog Release Notes si Reports Release Notes.
- fallback text din AI service nu mai mentioneaza doar GEMINI_API_KEY, ci platform/project AI provider.

Fisiere batch AD:
- backend/services/ai_service.py
- backend/routers/tasks.py
- backend/routers/sprints.py
- frontend/lib/ai-source.ts
- frontend/components/dashboard/create-task-dialog.tsx
- frontend/app/dashboard/tasks/[taskId]/page.tsx
- frontend/app/dashboard/backlog/page.tsx
- frontend/app/dashboard/reports/page.tsx
- PLAN_CONTINUARE_LICENTA_SDLC_HUB.txt

Verificari batch AD:
- .venv/bin/python -m compileall backend: OK
- npm run lint: OK
- npm run build: OK, ramane doar warning-ul existent Next `--localstorage-file` fara path valid.
- ./start.sh: OK
- curl http://127.0.0.1:8000/health: OK
- curl -I http://127.0.0.1:3000/dashboard/settings: 200 OK
- curl -I http://127.0.0.1:3000/dashboard/backlog: 200 OK
- curl -I http://127.0.0.1:3000/dashboard/reports: 200 OK

Batch AE - AI Risk Radar profund si Workload explanations batch 30 mai 2026
Implementat:
- Dashboard AI Risk Radar nu mai afiseaza doar carduri generice.
- Backend-ul calculeaza semnale concrete din:
  - comentarii recente cu termeni de blocaj/risc: blocked, stuck, dependency, blocaj, blocat, nu merge, eroare etc.
  - task-uri overdue, cu severitate in functie de prioritate.
  - task-uri vechi in Review sau In Progress, pe baza ultimului audit log de status.
  - task-uri critice neasignate.
  - lipsa sprintului activ pentru SCRUM/SCRUMBAN.
- Risk cards pot include acum task_id/task_key/status/priority/assignee/category/age_days si sunt clickabile catre TaskId.
- Team health score tine cont si de overdue, blocker comments si stale flow, nu doar unassigned/critical/review.
- Workload Balancer include semnale contextuale pe fiecare membru:
  - overdue tasks, critical items, stale review, long-running tasks, due this week, active story points.
- Workload summary are metrica noua Stale flow.
- Sugestiile de workload explica de ce se propune mutarea/asignarea, folosind semnalele de capacitate, nu doar scorul brut.

Fisiere batch AE:
- backend/routers/projects.py
- frontend/services/project.ts
- frontend/app/dashboard/page.tsx
- frontend/app/dashboard/workload/page.tsx
- PLAN_CONTINUARE_LICENTA_SDLC_HUB.txt

Verificari batch AE:
- .venv/bin/python -m compileall backend: OK
- npm run lint: OK
- npm run build: OK, ramane doar warning-ul existent Next `--localstorage-file` fara path valid.
- ./start.sh: OK
- curl http://127.0.0.1:8000/health: OK
- curl -I http://127.0.0.1:3000/dashboard: 200 OK
- curl -I http://127.0.0.1:3000/dashboard/workload: 200 OK

Batch AF - Calendar V2 reminder flow polish batch 30 mai 2026
Implementat:
- endpoint-ul /notifications/generate-reminders genereaza acum intr-un singur sweep:
  - remindere pentru task-uri due soon / overdue ale utilizatorului curent.
  - remindere pentru calendar events ale utilizatorului curent care incep in urmatoarele 60 de minute.
- raspunsul endpoint-ului este defalcat:
  - created total.
  - due_task_created.
  - calendar_created.
  - calendar_lookahead_minutes.
- generate_calendar_event_reminders suporta acum filtrare dupa project_id si user_id, util pentru flow manual din Calendar.
- butonul din Calendar a fost redenumit Smart reminders.
- dupa rulare, Calendar afiseaza un panou clar "Reminder sweep completed" cu total/tasks/events.
- flow-ul existent de Daily Standup recurring ramane functional:
  - presetul seteaza DAILY, 09:15-09:30, weekdays, 14 zile si toti membrii ca attendees.
  - backend-ul creeaza seria recurenta cu recurrence_series_id si limita de 60 occurrence-uri.

Fisiere batch AF:
- backend/utils/notifications.py
- backend/routers/notifications.py
- frontend/services/notification.ts
- frontend/app/dashboard/calendar/page.tsx
- PLAN_CONTINUARE_LICENTA_SDLC_HUB.txt

Verificari batch AF:
- .venv/bin/python -m compileall backend: OK
- npm run lint: OK
- npm run build: OK, ramane doar warning-ul existent Next `--localstorage-file` fara path valid.
- ./start.sh: OK
- curl http://127.0.0.1:8000/health: OK
- curl -I http://127.0.0.1:3000/dashboard/calendar: 200 OK

Batch AG - Work Engine List View batch 30 mai 2026
Implementat:
- pagina noua /dashboard/tasks pentru List View tabelar.
- link nou "Tasks" in sidebar, langa Board.
- List View incarca toate task-urile proiectului si functioneaza pentru SCRUM/KANBAN/SCRUMBAN.
- filtre:
  - search dupa key/title/assignee/team.
  - status.
  - priority.
  - assignee, inclusiv Unassigned.
  - sprint/planning, inclusiv Backlog only.
- metrici rapide: Active, Done, Overdue, Unassigned.
- tabel profesional cu key, titlu, story points, status, priority, assignee, sprint/backlog, due date si link catre TaskId.
- utilizatorii cu permisiuni pot schimba statusul si assignee direct din tabel.
- create issue este disponibil din List View pentru rolurile cu TASK_CREATE.
- tipul frontend Task.assignee_id accepta acum null pentru unassign corect prin API.

Fisiere batch AG:
- frontend/app/dashboard/tasks/page.tsx
- frontend/components/dashboard/app-sidebar.tsx
- frontend/services/task.ts
- PLAN_CONTINUARE_LICENTA_SDLC_HUB.txt

Verificari batch AG:
- npm run lint: OK
- npm run build: OK, ramane doar warning-ul existent Next `--localstorage-file` fara path valid.
- ./start.sh: OK
- curl http://127.0.0.1:8000/health: OK
- curl -I http://127.0.0.1:3000/dashboard/tasks: 200 OK

Batch AH - Tasks List View overdue filter si Create Issue fix batch 30 mai 2026
Implementat:
- in /dashboard/tasks exista acum filtru dedicat pentru due date:
  - All due dates.
  - Overdue.
  - Due in 7 days.
  - No date.
- adaugat buton rapid Overdue in header-ul tabelului, cu badge numeric si stare activa.
- filtrul Overdue foloseste aceeasi regula ca metrica: task-ul are due_date in trecut si nu este DONE.
- Create Issue din /dashboard/tasks foloseste acum permisiunile proiectului curent, nu hook-ul fara projectId.
- cand API-ul refuza crearea task-ului, dialogul afiseaza mesajul real din backend prin getApiErrorMessage.

Fisiere batch AH:
- frontend/app/dashboard/tasks/page.tsx
- frontend/components/dashboard/create-task-dialog.tsx
- PLAN_CONTINUARE_LICENTA_SDLC_HUB.txt

Verificari batch AH:
- npm run lint: OK
- npm run build: OK, ramane doar warning-ul existent Next `--localstorage-file` fara path valid.
- ./start.sh: OK
- curl http://127.0.0.1:8000/health: OK
- curl -I http://127.0.0.1:3000/dashboard/tasks: 200 OK

Batch AI - TaskId design polish si SDLC checklist batch 30 mai 2026
Implementat:
- reparat layout-ul din pagina TaskId:
  - butoanele Save changes si Refine & save sunt grupate si responsive in header.
  - continutul principal foloseste minmax(0, 1fr), ca textele lungi sa nu impinga panoul lateral.
  - comentariile lungi au break-words si nu mai rup cardurile.
  - panoul Properties ramane sticky fara scrollbar intern pe tot aside-ul.
- adaugat checklist SDLC predefinit in Create Issue:
  - Planificare.
  - Analiza.
  - Design.
  - Implementare.
  - Integrare.
  - Testare.
- utilizatorul poate pregati checklist-ul SDLC inainte de creare, iar task-ul se creeaza direct cu subtasks.
- adaugat buton SDLC checklist in pagina TaskId, cu deduplicare fata de subtasks deja existente.

Fisiere batch AI:
- frontend/app/dashboard/tasks/[taskId]/page.tsx
- frontend/components/dashboard/create-task-dialog.tsx
- PLAN_CONTINUARE_LICENTA_SDLC_HUB.txt

Verificari batch AI:
- npm run lint: OK
- npm run build: OK, ramane doar warning-ul existent Next `--localstorage-file` fara path valid.
- ./start.sh: OK
- curl http://127.0.0.1:8000/health: OK
- curl -I http://127.0.0.1:3000/dashboard/tasks: 200 OK
- curl -I http://127.0.0.1:3000/dashboard/tasks/44: 200 OK

Batch AJ - Tasks List View design repair batch 30 mai 2026
Context:
- utilizatorul a clarificat ca problema de design era in pagina /dashboard/tasks, nu in pagina TaskId.

Implementat:
- refacut header-ul de filtre din /dashboard/tasks ca sa nu mai fie inghesuit.
- filtrele sunt acum intr-un rand dedicat, responsive:
  - search pe latime mai mare.
  - status.
  - priority.
  - assignee.
  - planning/sprint.
  - due date.
- toate select-urile au latime completa in celula lor, astfel incat layout-ul ramane stabil pe desktop si pe viewport-uri mai mici.
- metrica Overdue este acum actionabila:
  - click pe card activeaza/dezactiveaza filtrul Overdue.
  - cardul primeste stare vizuala activa cand filtrul este aplicat.
- adaugat Clear filters:
  - dezactivat cand nu exista filtre active.
  - afiseaza numarul de filtre aplicate.
- pastrat butonul rapid Overdue in header, cu badge numeric si stare activa.

Fisiere batch AJ:
- frontend/app/dashboard/tasks/page.tsx
- PLAN_CONTINUARE_LICENTA_SDLC_HUB.txt

Verificari batch AJ:
- npm run lint: OK
- npm run build: OK, ramane doar warning-ul existent Next `--localstorage-file` fara path valid.
- ./start.sh: OK
- curl http://127.0.0.1:8000/health: OK
- curl -I http://127.0.0.1:3000/dashboard/tasks: 200 OK

Batch AK - Task Detail layout repair batch 30 mai 2026
Context:
- utilizatorul a semnalat problema vizuala pe /dashboard/tasks/63: cardul sticky Properties se suprapunea peste Audit Log la scroll.

Implementat:
- pagina TaskId foloseste acum container mai lat, max-w-[1500px], pentru o compozitie mai aerisita.
- grid-ul principal are coloana dreapta mai controlata:
  - 340px pe xl.
  - 380px pe 2xl.
- Properties ramane sticky in dreapta, dar este singur in stack-ul sticky impreuna cu butonul "Open on board".
- Audit Log a fost mutat din sidebar in coloana principala, sub Comments:
  - nu se mai suprapune cu Properties.
  - are spatiu real pentru text, avatar si valori vechi/noi.
  - afiseaza stare goala clara cand nu exista evenimente.

Fisiere batch AK:
- frontend/app/dashboard/tasks/[taskId]/page.tsx
- PLAN_CONTINUARE_LICENTA_SDLC_HUB.txt

Verificari batch AK:
- npm run lint: OK
- npm run build: OK, ramane doar warning-ul existent Next `--localstorage-file` fara path valid.
- curl http://127.0.0.1:8000/health: OK
- curl -I http://127.0.0.1:3000/dashboard/tasks/63: 200 OK

Batch AL - Project Audit Log UI batch 30 mai 2026
Context:
- dupa fix-ul de Task Detail, urmatoarea bucata utila din plan a fost facuta: audit-ul de proiect/metodologie exista in backend, dar nu era vizibil clar in UI.

Implementat:
- endpoint backend nou:
  - GET /projects/{project_id}/audit-logs?limit=40.
  - necesita membru in proiect si SETTINGS_MANAGE.
  - returneaza actor, avatar, action, field, old_value, new_value, metadata si created_at.
- update_project_settings scrie acum ProjectAuditLog pentru:
  - schimbare nume proiect.
  - schimbare descriere proiect.
- Settings incarca auditLogs pentru proiectul curent.
- Settings afiseaza card "Project audit trail":
  - ultimele 8 evenimente.
  - avatar actor.
  - label human-readable pentru methodology/workflow/AI/settings.
  - field si old/new values.
  - stare goala cand nu exista evenimente.
- dupa save project, save WIP limits si save/remove AI key, audit trail-ul se reincarca.

Fisiere batch AL:
- backend/routers/projects.py
- frontend/services/project.ts
- frontend/app/dashboard/settings/page.tsx
- PLAN_CONTINUARE_LICENTA_SDLC_HUB.txt

Verificari batch AL:
- .venv/bin/python -m compileall backend: OK
- npm run lint: OK
- npm run build: OK, ramane doar warning-ul existent Next `--localstorage-file` fara path valid.
- ./start.sh: OK
- curl http://127.0.0.1:8000/health: OK
- curl -I http://127.0.0.1:3000/dashboard/tasks/63: 200 OK
- curl -I http://127.0.0.1:3000/dashboard/settings: 200 OK
- curl -i http://127.0.0.1:8000/projects/1/audit-logs fara token: 401 OK, endpointul este protejat.

Batch AM - Fresh clone startup recovery batch 30 mai 2026
Context:
- dupa reclonare de pe GitHub proiectul nu mai pornea pentru ca fisierele .env locale nu sunt versionate, frontend-ul nu avea node_modules, iar backend-ul incerca PostgreSQL cu credentiale locale gresite.

Implementat:
- adaugat .env local pentru masina curenta:
  - DATABASE_URL=sqlite:///./sdlc_hub_dev.db
  - mail/AI optionale lasate goale pentru fallback development.
- adaugat .env.example pentru clone-uri viitoare.
- adaugat frontend/.env.local local cu NEXT_PUBLIC_API_URL=http://127.0.0.1:8000.
- adaugat frontend/.env.local.example si exceptie in frontend/.gitignore ca exemplul sa poata fi versionat.
- backend/config.py are acum fallback DATABASE_URL pe SQLite daca nu exista niciun .env.
- .gitignore exclude fisiere locale de DB:
  - *.db
  - *.sqlite
  - *.sqlite3
- README.md contine pasi clari de setup dupa clone:
  - creare venv.
  - pip install requirements.
  - copiere .env.example.
  - npm install.
  - alembic upgrade head.
  - ./start.sh.
- migrarile Alembic problematice au fost facute compatibile cu SQLite:
  - 9c0d1e2f3a4b_add_project_teams.py foloseste batch_alter_table pentru FK-uri.
  - f2a3b4c5d6e7_add_project_archive_flag.py nu mai incearca DROP DEFAULT pe SQLite.
  - a3b4c5d6e7f8_add_project_ai_settings.py nu mai incearca DROP DEFAULT pe SQLite.
- rulat npm install in frontend pentru refacerea node_modules local.
- creat DB local sdlc_hub_dev.db prin migrari Alembic.

Fisiere batch AM:
- .env.example
- README.md
- .gitignore
- backend/config.py
- backend/alembic/versions/9c0d1e2f3a4b_add_project_teams.py
- backend/alembic/versions/f2a3b4c5d6e7_add_project_archive_flag.py
- backend/alembic/versions/a3b4c5d6e7f8_add_project_ai_settings.py
- frontend/.gitignore
- frontend/.env.local.example
- PLAN_CONTINUARE_LICENTA_SDLC_HUB.txt

Fisiere locale neversionate create:
- .env
- frontend/.env.local
- sdlc_hub_dev.db
- frontend/node_modules/

Verificari batch AM:
- .venv/bin/alembic -c backend/alembic.ini upgrade head: OK pe SQLite.
- .venv/bin/alembic -c backend/alembic.ini current: b4c5d6e7f8a9 (head).
- npm install: OK.
- .venv/bin/python -m compileall backend: OK.
- npm run lint: OK.
- npm run build: OK, ramane doar warning-ul existent Next `--localstorage-file` fara path valid.
- ./start.sh: OK.
- curl http://127.0.0.1:8000/health: OK.
- curl -I http://127.0.0.1:3000: 200 OK.

Batch AN - PostgreSQL restore si SMTP diagnostics batch 30 mai 2026
Context:
- utilizatorul a cerut explicit ca proiectul sa ruleze pe PostgreSQL, nu SQLite, si sa fie rezolvata partea de email.

Implementat / verificat:
- PostgreSQL local 16/main ruleaza prin systemd:
  - serviciu activ cu postgres PID 313.
  - pg_lsclusters poate raporta inconsistent `down`/owner nobody, dar pg_ctl/systemd confirma procesul activ.
- verificat ca exista userii PostgreSQL:
  - postgres.
  - sdlc_user.
- verificat ca exista baza:
  - sdlchub.
- verificat conexiunea Python/psycopg2:
  - postgresql://sdlc_user:sdlc_password@127.0.0.1:5432/sdlchub conecteaza cu succes.
- .env local a fost trecut inapoi pe PostgreSQL:
  - DATABASE_URL=postgresql+psycopg2://sdlc_user:sdlc_password@127.0.0.1:5432/sdlchub.
- .env.example a fost actualizat sa foloseasca PostgreSQL ca default.
- backend/config.py are din nou fallback PostgreSQL.
- Alembic ruleaza pe PostgreSQL:
  - current: b4c5d6e7f8a9 (head).
- README.md documenteaza setup-ul PostgreSQL si configurarea Gmail SMTP.
- adaugat scripts/test_smtp.py:
  - verifica MAIL_USERNAME, MAIL_PASSWORD, MAIL_FROM, MAIL_SERVER.
  - trimite email de test prin backend/utils/email.py cand credentialele exista.
- verificat status SMTP curent:
  - MAIL_SERVER=smtp.gmail.com.
  - lipsesc MAIL_USERNAME, MAIL_PASSWORD, MAIL_FROM.
  - de aceea backend-ul afiseaza corect: SMTP nu este configurat complet.

Important pentru mail:
- Pentru Gmail SMTP nu functioneaza parola normala a contului.
- Trebuie App Password generat din Google Account -> Security -> 2-Step Verification -> App passwords.
- Dupa completarea .env:
  - MAIL_USERNAME=adresa@gmail.com
  - MAIL_PASSWORD=app_password_google
  - MAIL_FROM=adresa@gmail.com
  - MAIL_SERVER=smtp.gmail.com
  - MAIL_PORT=587
  - MAIL_STARTTLS=true
  - MAIL_SSL_TLS=false
- apoi se ruleaza:
  - .venv/bin/python scripts/test_smtp.py adresa@gmail.com
  - ./start.sh

Fisiere batch AN:
- .env local
- .env.example
- README.md
- backend/config.py
- scripts/test_smtp.py
- PLAN_CONTINUARE_LICENTA_SDLC_HUB.txt

Verificari batch AN:
- .venv/bin/alembic -c backend/alembic.ini upgrade head: OK pe PostgreSQL.
- .venv/bin/alembic -c backend/alembic.ini current: b4c5d6e7f8a9 (head).
- .venv/bin/python -c psycopg2 connect la sdlc_user@sdlchub: OK.
- ./start.sh: OK.
- curl http://127.0.0.1:8000/health: OK.
- curl -I http://127.0.0.1:3000: 200 OK.
- .venv/bin/python scripts/test_smtp.py slavoliu.preda24@gmail.com: detecteaza corect lipsa MAIL_USERNAME, MAIL_PASSWORD, MAIL_FROM.
- .venv/bin/python -m compileall backend scripts/test_smtp.py: OK.
- npm run lint: OK.

Batch AO - SMTP real fix si restart backend batch 30 mai 2026
Context:
- utilizatorul a completat variabilele SMTP in .env, dar emailurile tot nu plecau.

Diagnostic:
- shell-ul citea corect noile valori:
  - MAIL_USERNAME prezent.
  - MAIL_PASSWORD prezent.
  - MAIL_FROM prezent.
  - MAIL_SERVER=smtp.gmail.com.
  - MAIL_PORT=587.
  - MAIL_STARTTLS=true.
  - MAIL_SSL_TLS=false.
- backend-ul activ fusese pornit inainte de modificarea .env, iar logul vechi arata in continuare:
  - SMTP disabled, skipping project invitation email...
- dupa restart, backend-ul nu mai afiseaza "SMTP nu este configurat complet".

Implementat:
- adaugat MAIL_TIMEOUT in config:
  - backend/config.py: mail_timeout=20.
  - backend/utils/email.py trimite TIMEOUT catre FastMail ConnectionConfig.
  - .env si .env.example au MAIL_TIMEOUT=20.
  - README.md documenteaza MAIL_TIMEOUT.
- rulat test SMTP real din afara sandboxului de retea:
  - .venv/bin/python scripts/test_smtp.py slavoliu.preda24@gmail.com
  - rezultat: SMTP test email sent to slavoliu.preda24@gmail.com.
- repornit aplicatia cu ./start.sh dupa setarea credentialelor.

Fisiere batch AO:
- backend/config.py
- backend/utils/email.py
- .env local
- .env.example
- README.md
- PLAN_CONTINUARE_LICENTA_SDLC_HUB.txt

Verificari batch AO:
- test config mail: MAIL_USERNAME/MAIL_PASSWORD/MAIL_FROM sunt prezente, fara afisarea secretelor.
- .venv/bin/python scripts/test_smtp.py slavoliu.preda24@gmail.com: OK, email trimis.
- ./start.sh: OK.
- backend log dupa restart: nu mai apare warning SMTP disabled.
- curl http://127.0.0.1:8000/health: OK.
- curl -I http://127.0.0.1:3000: 200 OK.
- .venv/bin/python -m compileall backend scripts/test_smtp.py: OK.

Batch AP - Email notifications pentru notificari in-app batch 30 mai 2026
Context:
- dupa ce SMTP-ul real a fost reparat, urmatorul punct din plan a fost canalul email pentru task assigned, @mentions si calendar/meeting reminders.

Implementat:
- backend/utils/email.py:
  - adaugat send_notification_email_sync pentru rute sync.
  - foloseste SMTP real cu STARTTLS/SSL in functie de .env.
  - foloseste MAIL_TIMEOUT.
  - genereaza email HTML simplu cu link absolut catre SDLC Hub.
  - escape pentru title/body/link ca sa nu introducem HTML nesigur din comentarii/task-uri.
- backend/utils/notifications.py:
  - create_notification trimite acum si email pentru:
    - TASK_ASSIGNED.
    - MENTION.
    - CALENDAR_INVITE.
    - CALENDAR_REMINDER.
    - TASK_DUE_SOON.
    - TASK_OVERDUE.
  - daca emailul esueaza, notificarea in-app ramane creata si se logheaza warning, fara sa blocheze actiunea utilizatorului.
- canalul email este centralizat in create_notification, deci task assign, mentions, calendar attendees si reminders folosesc aceeasi logica.

Fisiere batch AP:
- backend/utils/email.py
- backend/utils/notifications.py
- PLAN_CONTINUARE_LICENTA_SDLC_HUB.txt

Verificari batch AP:
- .venv/bin/python -m compileall backend scripts/test_smtp.py: OK.
- import check pentru create_notification + send_notification_email_sync: OK.
- test integrat fara email real:
  - monkeypatch send_notification_email_sync.
  - create_notification cu TASK_ASSIGNED.
  - confirmat ca hook-ul email primeste recipientul corect.
  - rollback DB dupa test.
- ./start.sh: OK.
- curl http://127.0.0.1:8000/health: OK.
- curl -I http://127.0.0.1:3000: 200 OK.

Batch AQ - Documentation revision history batch 30 mai 2026
Context:
- in plan, Documentation/Wiki era implementat functional, dar lipsea istoricul editarilor pe paginile de documentatie.
- auto-generarea documentatiei la task Done era deja implementata in backend/tasks.py, desi sectiunea veche din plan o marca partial lipsa.

Implementat:
- model nou DocumentationRevision:
  - page_id.
  - project_id.
  - task_id.
  - title snapshot.
  - content snapshot.
  - action.
  - actor_id.
  - created_at.
- migrare Alembic noua:
  - c5d6e7f8a9b0_add_documentation_revisions.py.
- endpoint nou:
  - GET /documentation/{page_id}/history.
  - necesita membru in proiect.
  - returneaza snapshot-urile paginii in ordine descrescatoare.
- backend salveaza snapshot inainte de update la pagina de documentatie.
- backend salveaza snapshot si la delete, inainte de stergerea paginii.
- frontend/services/documentation.ts expune DocumentationRevision si getDocumentationPageHistory.
- /dashboard/documentation afiseaza acum card "Version history":
  - numar snapshot-uri.
  - actiune.
  - data.
  - titlu snapshot.
  - dimensiune continut.
  - stare goala cand pagina nu are revizii.
- la selectarea unei pagini, la save si la generate from task, istoricul se reincarca.

Fisiere batch AQ:
- backend/models/documentation.py
- backend/alembic/versions/c5d6e7f8a9b0_add_documentation_revisions.py
- backend/schemas/documentation.py
- backend/routers/documentation.py
- frontend/services/documentation.ts
- frontend/app/dashboard/documentation/page.tsx
- PLAN_CONTINUARE_LICENTA_SDLC_HUB.txt

Verificari batch AQ:
- .venv/bin/alembic -c backend/alembic.ini upgrade head: OK.
- .venv/bin/alembic -c backend/alembic.ini current: c5d6e7f8a9b0 (head).
- verificat prin SQLAlchemy inspect ca tabela documentation_revisions exista: True.
- .venv/bin/python -m compileall backend: OK.
- npm run lint: OK.
- npm run build: OK, ramane doar warning-ul existent Next `--localstorage-file` fara path valid.
- ./start.sh: OK.
- curl http://127.0.0.1:8000/health: OK.
- curl -I http://127.0.0.1:3000/dashboard/documentation: 200 OK.

Batch AR - Account notification preferences batch 30 mai 2026
Context:
- dupa repararea SMTP si legarea emailului la notificari, lipsea controlul utilizatorului asupra canalelor si categoriilor de notificari.

Implementat:
- coloane noi pe users:
  - notification_in_app_enabled.
  - notification_email_enabled.
  - notify_task_assignments.
  - notify_mentions.
  - notify_calendar.
  - notify_due_dates.
  - notify_ai_risk.
- migrare Alembic noua:
  - d6e7f8a9b0c1_add_user_notification_preferences.py.
- endpoint nou:
  - PUT /auth/me/notification-preferences.
  - returneaza AuthUser actualizat.
- AuthUser/UserOut expun preferintele catre frontend.
- create_notification respecta preferintele:
  - categoria dezactivata opreste complet notificarea.
  - canalul email dezactivat opreste emailul.
  - canalul in-app dezactivat opreste toast/unread broadcast, dar pastreaza o inregistrare read pentru deduplicare scheduler/reminders.
  - AI_RISK poate trimite email daca email channel si notify_ai_risk sunt active.
- generate AI risk notifications foloseste create_notification centralizat, deci respecta aceleasi reguli.
- RealtimeBridge actualizeaza store-ul de user cand preferintele se schimba.
- Account Settings are card nou Notifications:
  - toggle pentru In-app channel.
  - toggle pentru Email channel.
  - toggle pentru task assignments, mentions, calendar, due dates si AI risk radar.

Fisiere batch AR:
- backend/models/user.py
- backend/alembic/versions/d6e7f8a9b0c1_add_user_notification_preferences.py
- backend/schemas/auth.py
- backend/schemas/user.py
- backend/routers/auth.py
- backend/routers/notifications.py
- backend/utils/notifications.py
- frontend/services/auth.ts
- frontend/store/use-auth-store.ts
- frontend/components/realtime/realtime-bridge.tsx
- frontend/app/dashboard/account/page.tsx
- PLAN_CONTINUARE_LICENTA_SDLC_HUB.txt

Verificari batch AR:
- .venv/bin/python -m compileall backend: OK.
- .venv/bin/alembic -c backend/alembic.ini upgrade head: OK.
- .venv/bin/alembic -c backend/alembic.ini current: d6e7f8a9b0c1 (head).
- verificat prin SQLAlchemy inspect ca users are notification_email_enabled si notify_ai_risk: True True.
- npm run lint: OK.
- npm run build: OK, ramane doar warning-ul existent Next `--localstorage-file` fara path valid.
- ./start.sh: OK.
- curl http://127.0.0.1:8000/health: OK.
- curl -I http://127.0.0.1:3000/dashboard/account: 200 OK.
- log backend dupa restart: PUT /auth/me/notification-preferences raspunde 200 OK.

Batch AS - Task governance delete si estimate invalidation batch 30 mai 2026
Context:
- din lista stricta de functionalitati ramase, Task Management avea doua gauri clare: delete task cu guvernanta si invalidarea estimarii cu motiv + audit special.

Implementat:
- schema backend noua:
  - TaskDeleteRequest cu reason si confirm_key obligatorii.
  - EstimateInvalidationRequest cu reason obligatoriu.
- endpoint nou:
  - POST /tasks/{task_id}/estimate/invalidate.
  - cere TASK_UPDATE.
  - permite doar Project Owner, Project Admin, Product Owner, Scrum Master, Project Manager sau Tech Lead.
  - refuza KANBAN si task-urile fara estimare.
  - seteaza story_points = null.
  - scrie audit special ESTIMATE_INVALIDATED cu vechea estimare si motivul.
  - trimite task.changed realtime.
- endpoint nou:
  - DELETE /tasks/{task_id}.
  - cere TASK_DELETE.
  - permite doar Project Owner, Project Admin, Product Owner, Scrum Master, Project Manager sau Tech Lead.
  - cere confirmare prin tastarea cheii task-ului, de ex. GFW-12.
  - cere motiv obligatoriu.
  - sterge task-ul si pastreaza evenimentul TASK_DELETED in ProjectAuditLog cu metadata JSON: task_id, task_key, titlu, motiv, assignee, status, story_points.
  - trimite task.changed si project.changed realtime.
- frontend/services/task.ts expune:
  - invalidateTaskEstimate.
  - deleteTask.
- pagina /dashboard/tasks/[taskId]:
  - buton Delete in header daca userul are TASK_DELETE.
  - modal modern de delete cu motiv + confirmare prin cheia task-ului.
  - buton Invalidate estimate in Properties cand task-ul are story points si metodologia permite.
  - modal modern pentru invalidarea estimarii cu motiv.
  - actionLabel include ESTIMATE_INVALIDATED si DOCUMENTATION_GENERATED.

Fisiere batch AS:
- backend/schemas/task.py
- backend/routers/tasks.py
- frontend/services/task.ts
- frontend/app/dashboard/tasks/[taskId]/page.tsx
- PLAN_CONTINUARE_LICENTA_SDLC_HUB.txt

Verificari batch AS:
- .venv/bin/python -m compileall backend: OK.
- npm run lint: OK.
- npm run build: OK, ramane doar warning-ul existent Next `--localstorage-file` fara path valid.
- ./start.sh: OK.
- curl http://127.0.0.1:8000/health: OK.
- curl -I http://127.0.0.1:3000/dashboard/tasks/1: 200 OK.

Batch AT - Subtask edit/delete controls batch 30 mai 2026
Context:
- utilizatorul a cerut explicit ca subtask-urile sa poata fi editate si sterse, nu doar bifate.

Implementat:
- endpoint backend nou:
  - DELETE /tasks/{task_id}/subtasks/{subtask_id}.
  - cere TASK_UPDATE.
  - sterge subtask-ul.
  - scrie audit log SUBTASK_DELETED cu titlul vechi.
  - trimite task.changed realtime.
- update-ul existent de subtask title/is_done ramane folosit pentru editarea titlului.
- frontend/services/task.ts expune deleteSubtask.
- pagina /dashboard/tasks/[taskId]:
  - fiecare subtask are meniu cu Edit si Delete.
  - editarea titlului se face inline, cu Save/Cancel.
  - delete foloseste ConfirmDialog modern.
  - audit label include SUBTASK_DELETED.

Fisiere batch AT:
- backend/routers/tasks.py
- frontend/services/task.ts
- frontend/app/dashboard/tasks/[taskId]/page.tsx
- PLAN_CONTINUARE_LICENTA_SDLC_HUB.txt

Verificari batch AT:
- .venv/bin/python -m compileall backend: OK.
- npm run lint: OK.
- npm run build: OK, ramane doar warning-ul existent Next `--localstorage-file` fara path valid.
- ./start.sh: OK.
- curl http://127.0.0.1:8000/health: OK.
- curl -I http://127.0.0.1:3000/dashboard/tasks/1: 200 OK.

Batch AU - Team hierarchy tree si My Team view batch 30 mai 2026
Context:
- din lista stricta ramanea o componenta vizuala de tip arbore ierarhic si o vizualizare "echipa mea" pentru roluri de coordonare.

Implementat:
- /dashboard/team foloseste acum un arbore vizual recursiv pentru echipe si subechipe:
  - linii verticale/orizontale pentru niveluri.
  - noduri cu nume, descriere, membri, task-uri.
  - avataruri pentru membrii directi ai echipei.
  - badge "My team" pe nodul echipei utilizatorului curent.
- card "My team view" pentru Project Owner, Project Admin, Project Manager, Product Owner, Scrum Master, Flow Manager si Tech Lead:
  - echipa curenta.
  - membri directi.
  - numar subechipe.
  - numar task-uri asignate echipei.
- actiunile existente de creare/stergere/asignare echipa raman functionale.

Fisiere batch AU:
- frontend/app/dashboard/team/page.tsx
- PLAN_CONTINUARE_LICENTA_SDLC_HUB.txt

Verificari batch AU:
- .venv/bin/python -m compileall backend: OK.
- npm run lint: OK.
- npm run build: OK, ramane doar warning-ul existent Next `--localstorage-file` fara path valid.
- ./start.sh: OK.
- curl http://127.0.0.1:8000/health: OK.
- curl -I http://127.0.0.1:3000/dashboard/team: 200 OK.

Batch AV - Board dynamic columns per project batch 30 mai 2026
Context:
- din lista stricta ramanea posibilitatea de a configura coloane dinamice per proiect pe Board.
- statusurile task-urilor sunt inca un enum stabil backend: TODO, IN_PROGRESS, REVIEW, DONE. Pentru a evita o migrare riscanta a istoricului, configurarea dinamica se aplica peste aceste statusuri: label, ordine, vizibilitate si culoare.

Implementat:
- workflow_config suporta acum columns pe langa wip_limits:
  - key.
  - label.
  - enabled.
  - order.
  - color.
- backend/routers/projects.py:
  - DEFAULT_WORKFLOW_CONFIG include columns default.
  - _parse_workflow_config normalizeaza columns.
  - PUT /projects/{project_id}/workflow accepta columns si le salveaza in workflow_config.
  - audit PROJECT_WORKFLOW_UPDATED pastreaza old_value si new_value.
- frontend/services/project.ts:
  - ProjectWorkflowConfig include columns.
  - ProjectWorkflowColumn tipizat.
- /dashboard/settings:
  - cardul Board workflow este disponibil pentru toate metodologiile.
  - se pot redenumi coloanele.
  - se poate schimba ordinea cu Up/Down.
  - se pot ascunde/afisa coloane; Done ramane vizibila pentru auditabilitate.
  - Save board workflow salveaza atat columns cat si wip_limits.
- /dashboard/board:
  - citeste workflow_config.columns.
  - ordoneaza coloanele dupa configurarea proiectului.
  - foloseste label/color configurate.
  - ascunde coloanele dezactivate, dar le afiseaza automat daca exista task-uri in acea stare ca sa nu dispara munca existenta.
  - WIP alerts folosesc lista vizibila de coloane.
  - asculta si project.changed prin realtime ca modificarile de workflow din Settings sa se vada fara refresh manual.

Fisiere batch AV:
- backend/routers/projects.py
- frontend/services/project.ts
- frontend/app/dashboard/settings/page.tsx
- frontend/app/dashboard/board/page.tsx
- PLAN_CONTINUARE_LICENTA_SDLC_HUB.txt

Verificari batch AV:
- .venv/bin/python -m compileall backend: OK.
- npm run lint: OK.
- npm run build: OK, ramane doar warning-ul existent Next `--localstorage-file` fara path valid.
- ./start.sh: OK.
- curl http://127.0.0.1:8000/health: OK.
- curl -I http://127.0.0.1:3000/dashboard/board: 200 OK.
- curl -I http://127.0.0.1:3000/dashboard/settings: 200 OK.

Batch AW - Tiptap Rich Text pentru task description batch 30 mai 2026
Context:
- din lista stricta ramanea descriere de tip Rich Text reala integrata cu Tiptap, in loc de textarea simplu.

Implementat:
- instalate dependinte frontend:
  - @tiptap/react.
  - @tiptap/starter-kit.
  - @tiptap/extension-placeholder.
  - @tiptap/extension-link.
- component nou reutilizabil:
  - frontend/components/ui/rich-text-editor.tsx.
  - toolbar cu bold, italic, strike, liste, quote, code block, link/unlink, undo/redo.
  - link editor inline, fara prompt nativ de browser.
  - compatibil SSR/Next prin immediatelyRender: false.
  - normalizeaza descrierile vechi plain text in paragrafe HTML.
- /dashboard/tasks/[taskId]:
  - campul Description foloseste RichTextEditor.
  - save task pastreaza HTML-ul generat de Tiptap in acelasi camp description.
  - AI Refine continua sa actualizeze descrierea si editorul se sincronizeaza cu noul continut.
- Create issue dialog:
  - campul Description foloseste RichTextEditor.
  - Generate with AI si Refine spec continua sa populeze editorul.

Fisiere batch AW:
- frontend/package.json
- frontend/package-lock.json
- frontend/components/ui/rich-text-editor.tsx
- frontend/components/dashboard/create-task-dialog.tsx
- frontend/app/dashboard/tasks/[taskId]/page.tsx
- PLAN_CONTINUARE_LICENTA_SDLC_HUB.txt

Verificari batch AW:
- npm run lint: OK.
- npm run build: OK, ramane doar warning-ul existent Next `--localstorage-file` fara path valid.

Batch AX - AI comment risk si Workload Balancer AI/DnD batch 30 mai 2026
Context:
- din lista stricta ramanea AI & Workload: analiza automata comentarii, explicatii Workload prin prompt AI real si drag/drop intre membri.

Implementat:
- backend/services/ai_service.py:
  - analyze_comment_risk pentru detectare BLOCKER/BOTTLENECK/DEPENDENCY/SCOPE/QUALITY/DELIVERY.
  - fallback local pe cuvinte cheie RO/EN cand providerul AI nu este disponibil.
  - enhance_workload_suggestions care primeste scorurile deterministe si cere AI-ului explicatii profesionale fara sa schimbe task-urile/utilizatorii recomandati.
- backend/routers/tasks.py:
  - la creare comentariu ruleaza automat analiza de risc.
  - la editare comentariu ruleaza din nou analiza de risc.
  - cand se detecteaza risc:
    - scrie audit special COMMENT_RISK_DETECTED pe task.
    - trimite notificari AI_RISK catre assignee, owner si rolurile de coordonare.
    - include metadata: comment_id, severity, category, confidence, source si recommended_action.
  - foloseste preferintele existente de notificari pentru AI_RISK.
- backend/routers/projects.py:
  - endpointul /projects/{id}/workload/ai-suggestions pastreaza recomandarile deterministe, dar rafineaza summary/reason prin prompt AI real.
  - raspunsul include source ca UI-ul sa arate daca explicatia vine din provider sau fallback.
- frontend/services/project.ts:
  - WorkloadSuggestionsResponse include source.
- frontend/app/dashboard/workload/page.tsx:
  - afiseaza summary AI si source pentru sugestii.
  - adauga drag/drop cu @dnd-kit/core intre cardurile de membri.
  - adauga zona "Drop a task here" pentru dezassignare.
  - pastreaza selectul existent ca alternativa explicita.

Fisiere batch AX:
- backend/services/ai_service.py
- backend/routers/tasks.py
- backend/routers/projects.py
- frontend/services/project.ts
- frontend/app/dashboard/workload/page.tsx
- PLAN_CONTINUARE_LICENTA_SDLC_HUB.txt

Verificari batch AX:
- .venv/bin/python -m compileall backend: OK.
- npm run lint: OK.
- npm run build: OK, ramane doar warning-ul existent Next `--localstorage-file` fara path valid.

Batch AY - DevOps Pull Requests si GitHub user mapping batch 30 mai 2026
Context:
- din lista stricta ramanea o pagina dedicata pentru Pull Requests cu confirmari manuale si mapare automata GitHub user -> SDLC Hub user.

Implementat:
- model/migrare:
  - users.github_username pentru mapare persistenta.
  - github_events.mapped_user_id pentru a lega evenimentele GitHub de utilizatorul SDLC Hub.
  - migrare Alembic e7f8a9b0c1d2_add_github_user_mapping.py.
- backend/routers/github.py:
  - _resolve_github_user cauta sender_login in github_username.
  - daca nu exista, incearca potrivire automata cu local-part din email sau numele complet normalizat.
  - cand potrivirea reuseste, completeaza users.github_username pentru mapari viitoare.
  - commit/PR comments folosesc autorul mapat daca exista, nu doar owner/assignee fallback.
  - GitHubEvent salveaza mapped_user_id.
  - endpoint nou GET /github/pull-requests/project/{project_id}.
  - endpoint nou POST /github/pull-requests/{event_id}/confirm.
  - confirmarea manuala poate muta task-ul in REVIEW sau DONE, scrie audit GITHUB_PR_MANUAL_CONFIRM si adauga comentariu pe task.
  - confirmarea DONE declanseaza si generarea/upsert-ul documentatiei task-ului.
- frontend/services/github.ts:
  - getProjectPullRequests.
  - confirmPullRequestTransition.
- frontend/app/dashboard/devops/page.tsx:
  - buton catre pagina Pull requests.
- frontend/app/dashboard/devops/pull-requests/page.tsx:
  - pagina dedicata cu statistici PR.
  - afisare PR, task, status task, repository, GitHub sender si utilizator SDLC mapat.
  - actiuni Confirm review si Confirm done cu modal si nota optionala.

Fisiere batch AY:
- backend/models/user.py
- backend/models/github.py
- backend/alembic/versions/e7f8a9b0c1d2_add_github_user_mapping.py
- backend/schemas/github.py
- backend/routers/github.py
- frontend/services/github.ts
- frontend/app/dashboard/devops/page.tsx
- frontend/app/dashboard/devops/pull-requests/page.tsx
- PLAN_CONTINUARE_LICENTA_SDLC_HUB.txt

Verificari batch AY:
- .venv/bin/python -m compileall backend: OK.
- .venv/bin/alembic -c backend/alembic.ini upgrade head: OK.
- .venv/bin/alembic -c backend/alembic.ini current: e7f8a9b0c1d2 (head).
- npm run lint: OK.
- npm run build: OK, ramane doar warning-ul existent Next `--localstorage-file` fara path valid.
- ./start.sh: OK.
- curl http://127.0.0.1:8000/health: OK.
- curl -I http://127.0.0.1:3000/dashboard/devops: 200 OK.
- curl -I http://127.0.0.1:3000/dashboard/devops/pull-requests: 200 OK.

Batch AZ - Reporting final, Realtime comments si Calendar availability adaugate din web batch 3 iunie 2026
Context:
- dupa batch AY au fost adaugate functionalitati din interfata web ChatGPT, iar planul ramasese in urma.

Implementat confirmat in cod:
- Reporting:
  - Sprint Burndown.
  - Sprint Burnup.
  - Kanban/flow lead time separat.
  - CFD / Cumulative Flow Diagram istoric.
  - WIP history si current WIP limits.
  - metrici noi in PDF: Flow/Kanban lead time.
- Realtime comments:
  - backend broadcast separat comment.created/comment.updated/comment.deleted.
  - pagina TaskId actualizeaza comentariile incremental, fara refetch complet.
  - typing indicator prin WebSocket cu throttle pe backend.
- Calendar availability:
  - tabela calendar_availability.
  - CRUD availability/time off/vacation/focus/sick leave.
  - integrare in calendar feed.
  - filtre pentru availability si statusurile sale.
  - warning cand un meeting se suprapune peste disponibilitatea participantilor.

Fisiere cheie batch AZ:
- backend/routers/projects.py
- frontend/app/dashboard/reports/page.tsx
- frontend/services/project.ts
- backend/routers/tasks.py
- backend/main.py
- frontend/app/dashboard/tasks/[taskId]/page.tsx
- backend/models/project.py
- backend/routers/calendar.py
- backend/schemas/calendar.py
- frontend/app/dashboard/calendar/page.tsx
- frontend/services/calendar.ts
- backend/alembic/versions/f8a9b0c1d2e3_add_calendar_availability.py

Batch BA - Account Security si GitHub/ngrok integration adaugate din web batch 3 iunie 2026
Context:
- au fost adaugate functionalitatile ramase pentru securitatea contului si o configurare mai completa pentru GitHub webhooks.

Implementat confirmat in cod:
- Account Security:
  - tabele user_sessions si user_security_logs.
  - sesiuni active in Account Settings.
  - revoke session.
  - security log cu evenimente de login/password/session.
  - endpointuri /auth/me/sessions si /auth/me/security-log.
- GitHub/ngrok integration:
  - configurare repository per proiect.
  - endpointuri integration/project/{project_id}.
  - test integration.
  - delete/disconnect integration.
  - ngrok status/start/stop din backend.
  - UI in /dashboard/devops pentru repository, webhook URL, secret hint si pasi de configurare.

Fisiere cheie batch BA:
- backend/models/user.py
- backend/routers/auth.py
- backend/schemas/auth.py
- frontend/components/dashboard/account-security-panel.tsx
- frontend/app/dashboard/account/page.tsx
- frontend/services/auth.ts
- backend/alembic/versions/f9a0b1c2d3e4_add_user_sessions_security_logs.py
- backend/models/github.py
- backend/routers/github.py
- backend/schemas/github.py
- frontend/app/dashboard/devops/page.tsx
- frontend/services/github.ts
- backend/alembic/versions/f9b0c1d2e3f4_add_github_project_integrations.py

Verificari batch AZ/BA facute in Codex pe 3 iunie 2026:
- .venv/bin/alembic -c backend/alembic.ini current: f9b0c1d2e3f4 (head).
- .venv/bin/python -m compileall backend: OK.
- npm run lint: OK.
- npm run build: OK, ramane doar warning-ul existent Next `--localstorage-file` fara path valid.

Starea curenta dupa sincronizare 3 iunie 2026:
- Lista initiala de functionalitati mari este aproape inchisa la nivel de cod.
- Urmatorul pas recomandat nu mai este feature development mare, ci QA functional si polish UI.
- Trebuie verificat manual pe proiect real:
  - login/register/verify/reset password.
  - create project + schimbare metodologie SCRUM/KANBAN/SCRUMBAN.
  - board/backlog/tasks/task detail.
  - reports cu date reale.
  - calendar availability + meeting conflict.
  - devops integration/ngrok/webhook.
  - account security/session revoke.
  - global admin/support/archive.

Urmatoarea prioritate recomandata dupa batch BA:
1. Pornire aplicatie si smoke test pe paginile principale.
2. Audit UI pe paginile cele mai folosite:
   - Dashboard.
   - Tasks list.
   - TaskId.
   - Board.
   - Backlog.
   - Calendar.
   - Reports.
   - DevOps.
   - Account.
   - Global Admin.
3. Reparare bug-uri prinse in testare, pe batch-uri mici si verificate cu lint/build.


Batch BB - Global Admin Console polish/redesign Codex 3 iunie 2026
Context:
- utilizatorul a cerut ca Global Admin sa fie separat, modern, foarte functional si vizibil mai bun ca UI.
- problema principala era ca sidebar-ul parea inutil/greu de inteles, iar Support Tickets era greu de folosit cand existau multe comentarii.

Implementat:
- frontend/app/admin/layout.tsx:
  - sidebar desktop redesenat cu active state real pe sectiuni/hash.
  - buton Back to workspace mai clar si mereu accesibil.
  - shell responsive: sidebar doar pe desktop, header compact pe mobil.
  - carduri rapide Users/Projects/Tickets/Errors si status operational mai coerent.
- frontend/app/admin/page.tsx:
  - Command Center redesenat cu search, PDF report, refresh, quick nav si metrici vizibile.
  - adaugat Operations Pulse pentru semnale de platforma.
  - adaugat Incident Focus pentru tichetele active importante.
  - Support Desk refacut din board aglomerat in triage desk:
    - lista de tichete in stanga.
    - ticket selectat cu detalii in dreapta.
    - conversatie completa scrollabila, nu doar primele comentarii.
    - stergere comentarii direct din conversatie.
    - reply admin contextual.
    - schimbare status si prioritate din panoul lateral.
    - stergere ticket din panoul lateral.
  - UI responsive pentru admin: continutul se strange pe mobil/tableta fara sidebar fix.

Verificari batch BB:
- npm run lint: OK.
- npm run build: OK, ramane doar warning-ul existent Next `--localstorage-file` fara path valid.

Fisiere cheie batch BB:
- frontend/app/admin/layout.tsx
- frontend/app/admin/page.tsx

Urmatoarea prioritate recomandata dupa batch BB:
1. QA vizual pe /admin la 3 viewport-uri: desktop, laptop ingust, mobile.
2. Test functional suport:
   - user creeaza ticket din /dashboard/support.
   - Global Admin raspunde si schimba status/prioritate in /admin.
   - user vede progresul si conversatia completa.
   - user/admin pot sterge comentarii si ticket-uri conform permisiunilor.
3. Urmator polish logic/UI:
   - Global Admin Users/Projects table pe mobile, daca apar date multe.
   - System Health cu detalii extinse pentru erori 500.
   - confirmari/modaluri pentru Delete ticket si Delete comment, daca vrem protectie extra.

Batch BC - Global Admin live nav si seed tickets demo Codex 3 iunie 2026
Context:
- utilizatorul a observat ca linkurile din sidebar-ul Global Admin ramaneau blocate pe Command Center si nu se actualizau live la scroll.
- era nevoie si de seed data pentru multe support tickets, ca UI-ul de admin sa poata fi verificat realist.

Implementat:
- frontend/app/admin/layout.tsx:
  - sidebar-ul admin asculta acum scroll-ul containerului intern `admin-scroll-root`.
  - active state-ul se calculeaza live dupa sectiunea vizibila: Command Center, Identity, Project Registry, Support Desk, AI Usage, System Health.
  - root-ul admin are `scroll-smooth`, iar navigarea pe hash ramane compatibila.
- frontend/app/admin/page.tsx:
  - mici corectii de UI/responsive dupa date multe:
    - user stat cards trec pe o coloana pe mobile.
    - titlurile lungi de tichete se sparg/limiteaza corect.
    - butonul Reply devine full-width pe ecrane mici.
    - priority controls se adapteaza mai bine in panoul lateral.
- scripts/seed_admin_support_demo.py:
  - script nou idempotent pentru seed de support tickets demo.
  - creeaza reporteri demo daca lipsesc.
  - creeaza 12 tichete demo cu statusuri OPEN/IN_PROGRESS/RESOLVED/CLOSED, prioritati diferite si conversatii cu comentarii user/admin.
  - nu sterge date existente si nu dubleaza tichetele demo la rerulare.

Seed rulat in baza locala:
- comanda: `.venv/bin/python scripts/seed_admin_support_demo.py`
- rezultat: `Seed complete. Created 12 demo support tickets.`
- verificare DB dupa seed:
  - total support tickets: 13
  - CLOSED: 2
  - IN_PROGRESS: 4
  - OPEN: 3
  - RESOLVED: 4

Verificari batch BC:
- `.venv/bin/python -m py_compile scripts/seed_admin_support_demo.py`: OK.
- `npm run lint`: OK.
- `npm run build`: OK, ramane doar warning-ul existent Next `--localstorage-file` fara path valid.

Fisiere cheie batch BC:
- frontend/app/admin/layout.tsx
- frontend/app/admin/page.tsx
- scripts/seed_admin_support_demo.py

Urmatoarea prioritate recomandata dupa batch BC:
1. Verificare manuala /admin cu scroll:
   - active state-ul trebuie sa treaca live intre toate zonele din sidebar.
   - click pe linkurile din sidebar trebuie sa faca scroll si sa coloreze zona corecta.
2. Verificare Support Desk cu seed:
   - filtre ALL/OPEN/IN_PROGRESS/RESOLVED/CLOSED.
   - selectie ticket din lista.
   - conversatie lunga scrollabila.
   - schimbare status/prioritate.
   - reply si delete comment/ticket.
3. Urmator polish admin:
   - modal de confirmare pentru delete ticket/comment.
   - detalii extinse pentru Application Errors.
   - daca Users/Projects devin prea lungi, adaugare paginare sau virtualized list.

Batch BD - Global Admin scroll-spy robust Codex 3 iunie 2026
Context:
- dupa batch BC, utilizatorul a confirmat ca sidebar-ul admin tot ramanea blocat pe Command Center la scroll.
- cauza probabila: calculul manual dupa getBoundingClientRect nu era suficient de stabil pentru containerul intern de scroll si sectiunile lungi.

Implementat:
- frontend/app/admin/layout.tsx:
  - scroll-spy refacut cu `IntersectionObserver` legat direct de `admin-scroll-root`.
  - fallback suplimentar pe `scrollTop + clientHeight * 0.32`, ca active state-ul sa functioneze si cand intersectia nu livreaza intrari utile.
  - clickurile din sidebar folosesc `scrollRoot.scrollTo(...)` catre `target.offsetTop`, nu se bazeaza doar pe comportamentul default al anchor-ului.
  - URL-ul se actualizeaza cu `history.replaceState`, dar scroll-ul ramane controlat in containerul admin.

Corectie suplimentara in acelasi batch:
- IntersectionObserver a fost eliminat fiindca putea suprascrie fallback-ul cu sectiunea mare de sus.
- active state-ul este calculat determinist din `getBoundingClientRect().top` pentru fiecare sectiune.
- actualizarea este legata direct de `onScroll` React pe `admin-scroll-root`, plus listener fallback pe `window` si pe container.
- clickurile din sidebar raman controlate manual prin `scrollRoot.scrollTo(...)`, cu fallback `scrollIntoView`.

Verificari batch BD:
- `npm run lint`: OK.
- `npm run build`: OK, ramane doar warning-ul existent Next `--localstorage-file` fara path valid.

Fisiere cheie batch BD:
- frontend/app/admin/layout.tsx

Urmatoarea prioritate recomandata dupa batch BD:
1. Verificare manuala imediata /admin:
   - scroll lent pana la Users, Projects, Support, AI Usage, System Health.
   - active state-ul din sidebar trebuie sa se mute fara click.
   - click pe fiecare link trebuie sa duca in sectiunea respectiva.
2. Dupa confirmare, continuam cu restul de polish/bug-uri:
   - confirmari delete ticket/comment.
   - System Health cu detalii extinse.
   - QA pe schimbare metodologie SCRUM/KANBAN/SCRUMBAN.
   - QA pe archive project read-only.
   - smoke test auth/mail/support/account/devops.

Batch BE - Account grid, Workload AI desk si DevOps one-time setup Codex 3 iunie 2026
Context:
- utilizatorul a cerut polish final in zonele Account, Workload, DevOps, Settings si Project Wizard.
- cerinta importanta: conectarea repository-ului GitHub trebuie sa fie un setup one-time; dupa configurare detaliile se vad in Settings, iar formularul complet nu trebuie repetat ca ecran principal.
- in wizard-ul initial trebuie sa existe optiunea de GitHub, dar sa poata fi sarita si completata ulterior din tab-ul DevOps.

Implementat:
- frontend/app/dashboard/account/page.tsx:
  - pagina Account a fost trecuta din layout main/aside rigid intr-un grid responsive `xl:grid-cols-12`.
  - Profile, My Projects, Security, Notifications, Active Sessions/Security Log si Account Notes sunt distribuite ca panouri de grid.
  - scop: aspect mai coerent pe desktop si mai natural pe mobile/tableta.
- frontend/app/dashboard/workload/page.tsx:
  - generarea de sugestii AI apare acum sus, intr-un panou "AI workload desk".
  - butonul Generate suggestions a fost mutat din header in acest panou.
  - rezultatele, sursa AI/determinista si sumarul apar imediat dupa generare in primul ecran.
  - lista membrilor si drag & drop raman dedesubt pentru redistribuire efectiva.
- frontend/app/dashboard/devops/page.tsx:
  - formularul complet de repository setup apare doar daca integrarea nu este configurata.
  - dupa configurare, pagina afiseaza un cockpit cu:
    - repository.
    - status conexiune.
    - branch default.
    - last delivery.
    - automation badges pentru commits si PRs.
    - webhook endpoint compact.
    - actiuni Check status / Disconnect pentru owner.
  - daca userul nu este owner si nu exista integrare, vede mesaj read-only de not configured.
  - sectiunea de evenimente GitHub este randata doar cand repository-ul este configurat.
- frontend/app/dashboard/settings/page.tsx:
  - Settings incarca integrarea GitHub prin `getProjectGitHubIntegration`.
  - a fost adaugat cardul "Repository integration" in zona de project settings.
  - cardul afiseaza in grid:
    - status setup.
    - repository.
    - default branch.
    - webhook.
    - automation commits/PRs.
  - butonul duce catre `/dashboard/devops` pentru configurare sau inspectie.
- frontend/app/project-wizard/page.tsx:
  - wizard-ul a fost extins de la 4 la 5 pasi.
  - pas nou: "DevOps Setup".
  - bara de progres cu 5 pasi si actiunile finale au primit ajustari responsive pentru mobile/tableta.
  - setup-ul GitHub este optional si porneste dezactivat.
  - daca este activat, owner-ul poate completa:
    - repository `owner/repository`.
    - repository URL.
    - default branch.
    - public backend/ngrok URL.
    - auto-link commits.
    - PR smart transitions.
  - dupa crearea proiectului, wizard-ul incearca `upsertProjectGitHubIntegration`.
  - daca GitHub setup-ul esueaza sau repository-ul este incomplet, proiectul ramane creat si utilizatorul poate continua configurarea din DevOps.

Verificari batch BE:
- `npm run lint`: OK.
- `npm run build`: OK, ramane doar warning-ul existent Next `--localstorage-file` fara path valid.

Fisiere cheie batch BE:
- frontend/app/dashboard/account/page.tsx
- frontend/app/dashboard/workload/page.tsx
- frontend/app/dashboard/devops/page.tsx
- frontend/app/dashboard/settings/page.tsx
- frontend/app/project-wizard/page.tsx
- PLAN_CONTINUARE_LICENTA_SDLC_HUB.txt

Verificari manuale recomandate dupa batch BE:
1. Account:
   - desktop: cardurile trebuie sa fie grid, nu sidebar rigid.
   - mobile: toate cardurile trebuie sa cada pe o singura coloana fara overflow.
2. Workload:
   - buton Generate suggestions trebuie sa fie sus.
   - dupa click trebuie sa apara sumarul si cardurile de sugestii in panoul de sus.
   - drag & drop intre membri trebuie sa ramana functional.
3. DevOps:
   - fara integrare: owner vede formularul complet de setup.
   - cu integrare: owner vede cockpit-ul, nu formularul mare.
   - non-owner vede detalii read-only daca exista integrare.
4. Settings:
   - cardul Repository integration trebuie sa arate datele salvate.
   - butonul Open DevOps/Configure repository trebuie sa duca in `/dashboard/devops`.
5. Project Wizard:
   - pasii trebuie sa fie Identity, AI Interview, Decision, Team & Roles, DevOps Setup.
   - skip GitHub trebuie sa creeze proiectul normal.
   - GitHub enabled cu `owner/repo` trebuie sa salveze integrarea dupa creare.

Batch BF - GitHub Settings editabil, DevOps polish si task development links Codex 3 iunie 2026
Context:
- utilizatorul a cerut ca repository-ul GitHub sa poata fi modificat din Project Settings, nu doar vazut.
- setarile pentru auto-link commit si PR smart transitions trebuie sa fie reale.
- partea de ngrok trebuie sa existe si in Settings.
- pagina DevOps trebuia sa arate mai mult ca un operation center.
- pe fiecare task trebuie sa existe vizibilitate clara pentru commit/PR legate de task, nu doar audit log generic.

Implementat:
- frontend/app/dashboard/settings/page.tsx:
  - cardul Repository integration este acum editabil pentru project owner/global admin.
  - campuri editabile:
    - repository `owner/repository`.
    - repository URL.
    - default branch.
    - public backend/ngrok URL.
  - toggle-uri functionale pentru:
    - Auto-link commits.
    - PR smart transitions.
  - actiuni:
    - Save repository settings.
    - Check status.
    - Disconnect repository.
    - Copy webhook URL.
    - Start/Stop ngrok tunnel.
  - userii non-owner vad read-only si mesaj ca setarile sunt owner-only.
- backend/routers/github.py:
  - `auto_link_commits=False` opreste linkarea automata a commiturilor la task-uri.
  - `auto_transition_prs=False` pastreaza linkarea PR-urilor, dar nu mai muta task-urile automat in Review/Done.
  - adaugat endpoint nou:
    - `GET /github/events/task/{task_id}` pentru evenimente GitHub legate de un singur task.
- frontend/services/github.ts:
  - adaugat `getTaskGitHubEvents(taskId)`.
- frontend/app/dashboard/tasks/[taskId]/page.tsx:
  - adaugata sectiune noua "Development links".
  - afiseaza commits si pull requests legate de task prin GitHub webhook.
  - include repository, sender, data, commit sha scurt, PR number, status/action si link extern GitHub.
  - daca nu exista evenimente, arata instructiune sa se includa cheia taskului in commit/PR/branch.
- frontend/components/dashboard/audit-log-event.tsx:
  - audit log-ul are acum label/icon dedicat pentru:
    - `GITHUB_COMMIT_LINKED`
    - `GITHUB_PR_LINKED`
    - campurile `github.commit` si `github.pull_request`.
- frontend/app/dashboard/devops/page.tsx:
  - header-ul a fost redesenat ca operation center.
  - include status repository, repo curent, push events, pull requests, linked tasks.
  - adaugat buton direct catre Repository settings.
  - eliminate metricile dublate din zona de evenimente.

Verificari batch BF:
- `.venv/bin/python -m py_compile backend/routers/github.py`: OK.
- `npm run lint`: OK.
- `npm run build`: OK, ramane warning-ul existent Next `--localstorage-file` fara path valid.

Fisiere cheie batch BF:
- backend/routers/github.py
- frontend/services/github.ts
- frontend/app/dashboard/settings/page.tsx
- frontend/app/dashboard/devops/page.tsx
- frontend/app/dashboard/tasks/[taskId]/page.tsx
- frontend/components/dashboard/audit-log-event.tsx
- PLAN_CONTINUARE_LICENTA_SDLC_HUB.txt

Verificari manuale recomandate dupa batch BF:
1. Settings / Repository integration:
   - schimba repository-ul si branch-ul, salveaza, refresh, verifica persistenta.
   - porneste ngrok, verifica daca public URL se aplica in camp.
   - copiaza webhook URL.
   - opreste ngrok.
   - dezactiveaza Auto-link commits si verifica webhook push cu task key: nu trebuie sa creeze link task.
   - dezactiveaza PR smart transitions si verifica PR cu task key: trebuie sa apara link/comment/eveniment, dar statusul taskului nu trebuie mutat automat.
2. DevOps:
   - header-ul trebuie sa afiseze repo/status/metrici.
   - butonul Repository settings trebuie sa duca la Project Settings.
   - filtrele de evenimente trebuie sa ramana functionale.
3. Task detail:
   - commit message cu task key trebuie sa apara in Development links si Audit Log.
   - PR title/body/branch cu task key trebuie sa apara in Development links si Audit Log.
   - linkul Open GitHub trebuie sa deschida URL-ul evenimentului.

Batch BG - Task development links sub comentarii si confirmari stergere support Codex 3 iunie 2026
Context:
- utilizatorul a cerut ca "Development links" din pagina taskului sa fie sub comentarii.
- stergerea ticketelor si comentariilor de support trebuie sa fie mai sigura, cu confirmare explicita, atat pentru user cat si pentru Global Admin.

Implementat:
- frontend/app/dashboard/tasks/[taskId]/page.tsx:
  - sectiunea "Development links" este randata dupa cardul "Comments" si inainte de "Audit Log".
  - logica de fetch/afisare a evenimentelor GitHub ramane aceeasi, doar ordinea UI a fost corectata.
- frontend/app/dashboard/support/page.tsx:
  - stergerea unui support ticket deschide confirm dialog, nu mai sterge direct.
  - stergerea unui comentariu de support deschide confirm dialog, nu mai sterge direct.
  - dialogurile au loading state pentru actiunea in curs.
- frontend/app/admin/page.tsx:
  - Global Admin are confirm dialog pentru stergere support ticket.
  - Global Admin are confirm dialog pentru stergere comment din ticket.
  - butoanele de stergere din Support Desk seteaza intentia de stergere si asteapta confirmare.

Verificari batch BG:
- `npm run lint`: OK.
- `npm run build`: OK, ramane warning-ul existent Next `--localstorage-file` fara path valid.

Fisiere cheie batch BG:
- frontend/app/dashboard/tasks/[taskId]/page.tsx
- frontend/app/dashboard/support/page.tsx
- frontend/app/admin/page.tsx
- PLAN_CONTINUARE_LICENTA_SDLC_HUB.txt

Verificari manuale recomandate dupa batch BG:
1. Task detail:
   - Comments trebuie sa fie deasupra Development links.
   - Development links trebuie sa fie deasupra Audit Log.
2. Dashboard Support:
   - Delete ticket trebuie sa ceara confirmare.
   - Delete comment trebuie sa ceara confirmare.
   - Cancel nu trebuie sa stearga nimic.
3. Global Admin / Support Desk:
   - Delete ticket trebuie sa ceara confirmare.
   - Delete comment trebuie sa ceara confirmare.
   - dupa confirmare lista trebuie sa se actualizeze fara reload manual.

Batch BH - Global Admin System Health inspectie erori Codex 3 iunie 2026
Context:
- dupa confirmari support, urmatorul polish util din plan era System Health cu detalii extinse.
- lista de erori din Global Admin arata doar status/metoda/path/user/date, fara context de diagnostic.

Implementat:
- backend/main.py:
  - middleware-ul HTTP error logging foloseste acum coloana existenta `detail`.
  - pentru raspunsuri 4xx/5xx se salveaza context JSON minimal:
    - path.
    - query_keys, fara valorile query ca sa nu fie salvate tokenuri/parole.
    - client_host.
    - user_agent.
    - referer.
    - content_type.
  - pentru exceptii nehandled se logheaza status 500 cu exception class si mesaj limitat, apoi exceptia este ridicata mai departe.
  - nu a fost nevoie de migrare Alembic.
- frontend/app/admin/page.tsx:
  - Application Errors are acum buton Inspect pe fiecare eroare.
  - dialogul de inspectare afiseaza request, actor, timestamp, severitate si contextul extins parsat din JSON.
  - erorile vechi fara detail arata mesaj explicit ca au fost capturate inainte de contextul extins.
  - search-ul din admin cauta acum si in user/detail pentru erori.
  - exista actiune Copy summary pentru triere rapida.

Verificari batch BH:
- `.venv/bin/python -m py_compile backend/main.py`: OK.
- `npm run lint`: OK.
- `npm run build`: OK, ramane warning-ul existent Next `--localstorage-file` fara path valid.

Fisiere cheie batch BH:
- backend/main.py
- frontend/app/admin/page.tsx
- PLAN_CONTINUARE_LICENTA_SDLC_HUB.txt

Verificari manuale recomandate dupa batch BH:
1. Global Admin / System Health:
   - genereaza o eroare 404/401/500 in aplicatie.
   - intra in /admin, sectiunea System Health.
   - foloseste Inspect pe eroare si verifica dialogul cu context.
   - cauta dupa path/user si verifica filtrarea.
2. Confidentialitate:
   - query_keys trebuie sa contina doar numele cheilor query, nu valorile.
   - Authorization/Cookie nu trebuie sa apara in detail.

Batch BI - Fix Task GitHub links si Theme Light/Dark/System Codex 3 iunie 2026
Context:
- utilizatorul a observat in Global Admin/System Health multe 404 pentru `GET /github/events/task/{id}`.
- task-ul GFW-1 trebuia sa afiseze in Task detail aceleasi date GitHub vizibile in DevOps.
- utilizatorul a cerut light/dark toggle, cu default in functie de sistemul fiecaruia.

Implementat:
- frontend/app/dashboard/tasks/[taskId]/page.tsx:
  - pagina Task detail nu mai cheama endpointul `/github/events/task/{id}` direct.
  - incarca evenimentele GitHub din proiect prin endpointul stabil `/github/events/project/{project_id}`.
  - filtreaza local dupa:
    - `task_id`;
    - `task_key`;
    - aparitia cheii taskului in summary.
  - astfel Development links afiseaza evenimentele din DevOps chiar si daca evenimentele vechi au doar task_key sau endpointul de task era stale.
- backend/routers/github.py:
  - endpointul `/github/events/task/{task_id}` a fost imbunatatit.
  - cauta evenimente atat dupa `task_id`, cat si dupa `task_key`, in acelasi proiect.
  - importat `or_` din SQLAlchemy.
- frontend/components/theme-provider.tsx:
  - provider global next-themes cu `defaultTheme="system"` si `enableSystem`.
- frontend/components/theme-mode-toggle.tsx:
  - componenta noua pentru alegere tema: System, Light, Dark.
- frontend/app/layout.tsx:
  - ThemeProvider montat global.
  - eliminat `className="dark"` hardcodat de pe html.
  - adaugat `suppressHydrationWarning`.
- frontend/components/dashboard/app-sidebar.tsx:
  - adaugat buton Theme in zona de jos a sidebarului.
- frontend/app/admin/layout.tsx:
  - adaugat acelasi Theme toggle si in Global Admin Console.
  - pe desktop apare jos langa profil; pe mobile/tableta apare in headerul admin.
- frontend/app/globals.css:
  - adaugat layer light mode pentru clasele dark dominante folosite deja in aplicatie.
  - scop: tema Light sa fie vizibila si utilizabila chiar daca multe pagini au clase Tailwind dark hardcodate.

Verificari batch BI:
- `.venv/bin/python -m py_compile backend/routers/github.py backend/main.py`: OK.
- `npm run lint`: OK.
- `npm run build`: OK, ramane warning-ul existent Next `--localstorage-file` fara path valid.
- `./start.sh`: backend/frontend restartate.
- `curl http://127.0.0.1:8000/health`: 200 OK.
- `curl -i http://127.0.0.1:8000/github/events/task/1`: 401 Not authenticated, deci ruta exista si nu mai este 404.
- `curl -I http://127.0.0.1:3000/dashboard/tasks/1`: 200 OK.

Fisiere cheie batch BI:
- backend/routers/github.py
- frontend/app/dashboard/tasks/[taskId]/page.tsx
- frontend/app/layout.tsx
- frontend/app/globals.css
- frontend/app/admin/layout.tsx
- frontend/components/dashboard/app-sidebar.tsx
- frontend/components/theme-provider.tsx
- frontend/components/theme-mode-toggle.tsx
- PLAN_CONTINUARE_LICENTA_SDLC_HUB.txt

Verificari manuale recomandate dupa batch BI:
1. Task detail / Development links:
   - deschide GFW-1.
   - verifica daca apar commiturile/PR-urile care apar in DevOps.
   - verifica System Health dupa refresh: nu trebuie sa mai apara 404 noi pentru `/github/events/task/...` din pagina taskului.
2. Theme:
   - in sidebar jos, deschide Theme.
   - verifica System, Light si Dark.
   - pe System, schimba tema sistemului de operare/browser si verifica daca UI-ul urmeaza preferinta.
   - verifica rapid Dashboard, Tasks, Task detail, DevOps si Global Admin pe Light, deoarece multe clase au fost convertite prin override global si pot exista zone care mai cer polish punctual.

Batch BJ - Light mode polish real si fix contrast Codex 3 iunie 2026
Context:
- utilizatorul a trimis screenshots cu Light mode unde se vedeau probleme clare:
  - meniurile din sidebar deveneau toate fundal alb/pill din cauza selectorilor prea largi.
  - Global Admin combina sidebar dark cu continut light si avea texte palide pe carduri deschise.
  - Workload/Reports aveau texte accent foarte deschise pe fundaluri light.
  - unele badge-uri/butoane cu `text-blue-100`, `text-amber-100`, `text-emerald-100` etc. ramaneau aproape invizibile in Light.

Implementat:
- frontend/app/globals.css:
  - inlocuit selectorii broad `[class*="..."]` cu token exact `[class~="..."]`.
  - motiv: `[class*="bg-slate-900"]` prindea si clase ca `hover:bg-slate-900`, ceea ce forta fundaluri albe pe elemente care nu trebuiau sa aiba fundal in starea normala.
  - adaugata conversie light pentru background-uri dark exacte:
    - `bg-slate-950`, `bg-slate-950/70`, `bg-slate-900/80`, etc.
    - background-uri arbitrare admin/landing: `bg-[#030712]`, `bg-[#050712]`, `bg-[#080d1a]`, `bg-[#080d1a]/95`, `bg-[#0A0F1C]`.
  - adaugate texte light accesibile pentru:
    - slate/white.
    - blue/cyan.
    - emerald.
    - amber/orange.
    - red/rose.
    - purple/violet.
  - accentele pastel nu mai sunt transformate toate in alb; au background-uri tintuite:
    - blue -> albastru pastel.
    - cyan -> cyan pastel.
    - emerald -> verde pastel.
    - amber -> galben pastel.
    - red/rose -> rosu pastel.
    - purple/violet -> mov pastel.
  - butoanele solide (`bg-blue-600`, `bg-red-600`, etc.) pastreaza text alb in Light.
  - dropdown/select poppers au fundal alb in Light.
  - umbrele slate dark sunt convertite in umbre subtile potrivite pentru Light.

Verificari batch BJ:
- `npm run lint`: OK.
- `npm run build`: OK, ramane warning-ul existent Next `--localstorage-file` fara path valid.
- s-a incercat verificare cu Playwright CLI folosind storage temporar autentificat, dar browserul local Playwright lipseste:
  - eroare: `Executable doesn't exist ... Please run npx playwright install`.
  - nu s-a instalat browserul ca sa nu schimbam mediul acum.
  - storage-ul temporar `/tmp/sdlc-light-storage.json` a fost sters.

Fisiere cheie batch BJ:
- frontend/app/globals.css
- PLAN_CONTINUARE_LICENTA_SDLC_HUB.txt

Verificari manuale recomandate dupa batch BJ:
1. Light mode:
   - Dashboard: meniul lateral nu trebuie sa mai arate ca toate elementele sunt active/pill.
   - Workload: AI workload desk trebuie sa aiba text lizibil.
   - Reports: Bottleneck signal si textele amber/blue trebuie sa fie lizibile.
   - Account: cardurile si form inputs trebuie sa aiba contrast bun.
   - Global Admin: shell-ul nu trebuie sa mai ramana dark cand tema este Light; cardurile de Incident Focus trebuie sa aiba text lizibil.
2. Dark mode:
   - comuta inapoi pe Dark si verifica rapid ca nu s-a degradat tema originala.
3. System:
   - comuta pe System si verifica daca preferinta sistemului este urmarita.
4. Daca vrem verificare automata vizuala mai tarziu:
   - ruleaza `npm exec -- playwright install chromium` doar cand acceptam sa instalam browserul Playwright local.
   - apoi se pot face screenshots autentificate cu `--load-storage`.

Batch BK - Playwright instalat, verificare vizuala Light/Dark si responsive dashboard Codex 3 iunie 2026
Context:
- utilizatorul a cerut sa folosim Playwright pentru verificari UI/UX reale, deoarece Light/Dark in browser parea neschimbat sau inconsistent.
- prima incercare cu Playwright a esuat pentru ca browserul nu era instalat, apoi pentru ca lipseau librarii native Chromium.
- mobile dashboard era rupt: sidebarul lateral ocupa aproape toata latimea si impingea continutul intr-o coloana foarte ingusta.

Implementat:
- frontend/package.json si frontend/package-lock.json:
  - instalat `@playwright/test` ca dev dependency.
  - adaugat script `npm run ui:screenshot` pentru capturi rapide cu Playwright.
- Mediu local:
  - instalat Chromium Playwright.
  - instalate dependinte native necesare pentru Chromium:
    - `libnspr4`
    - `libnss3`
    - `libasound2t64`
  - dupa instalare, capturile Playwright ruleaza fara `LD_LIBRARY_PATH` temporar.
- frontend/app/dashboard/layout.tsx:
  - layoutul dashboard este acum `flex-col` pe mobile si `flex-row` de la `md`.
  - `main` are `min-h-0`, astfel scrollul intern functioneaza corect.
- frontend/components/dashboard/app-sidebar.tsx:
  - sidebarul devine full-width si limitat la `56dvh` pe mobile.
  - de la `md` revine la sidebar lateral fix de `258px`.
  - efect: continutul dashboard nu mai este taiat lateral pe telefoane.

Verificari Playwright batch BK:
- Capturi Light desktop generate:
  - `/tmp/sdlc-dashboard-light.png`
  - `/tmp/sdlc-admin-light.png`
  - `/tmp/sdlc-workload-light.png`
  - `/tmp/sdlc-reports-light.png`
  - `/tmp/sdlc-account-light.png`
- Capturi Dark desktop generate:
  - `/tmp/sdlc-dashboard-dark.png`
  - `/tmp/sdlc-admin-dark.png`
- Capturi mobile Light generate:
  - `/tmp/sdlc-dashboard-mobile-light.png` - confirma bugul initial.
  - `/tmp/sdlc-dashboard-mobile-light-v2.png` - confirma ca nu mai este taiat lateral.
  - `/tmp/sdlc-dashboard-mobile-light-v3.png` - confirma sidebar mai compact pe mobile.
- Observatie:
  - contururile punctate galbene vazute in `view_image` nu sunt CSS real al aplicatiei; verificarea computed style prin Playwright arata `outline-style: none`.

Verificari batch BK:
- `npm run lint`: OK.
- `npm run build`: OK, ramane warning-ul existent Next `--localstorage-file` fara path valid.

Fisiere cheie batch BK:
- frontend/package.json
- frontend/package-lock.json
- frontend/app/dashboard/layout.tsx
- frontend/components/dashboard/app-sidebar.tsx
- PLAN_CONTINUARE_LICENTA_SDLC_HUB.txt

Ce ramane etapizat pentru UI/UX:
1. Responsive complet pe mobile pentru toate paginile mari:
   - Activity
   - Tasks
   - Task detail
   - Board
   - Backlog
   - Team
   - Settings
   - DevOps
   - Reports
   - Workload
   - Global Admin Console
2. Light mode polish punctual pe paginile care mai au clase dark hardcodate si nu sunt acoperite de override-ul global.
3. Global Admin:
   - verificare completa Light/Dark/Mobile cu Playwright.
   - revalidare scroll spy sidebar.
   - verificare Support Desk cu multe tichete si comentarii.
4. Creare set de teste/screenshot-uri Playwright organizat:
   - storage state demo.
   - capturi desktop/mobile Light/Dark.
   - eventual prag vizual manual, nu E2E complet.

Batch BL - Mobile hamburger sidebar si Light polish pentru afisaje ramase Codex 3 iunie 2026
Context:
- utilizatorul a observat ca sidebarul pe mobile ocupa in continuare prea mult ecran si trebuie ascuns intr-un meniu hamburger.
- screenshots Light aratau inca zone cu fundaluri gri/dark:
  - AI Risk Radar si Activity headers.
  - Calendar month grid.
  - Board cards.
  - Task detail rich editor toolbar.
  - Backlog sprint planning.
  - Settings danger/governance/AI cards.

Implementat:
- frontend/components/dashboard/app-sidebar.tsx:
  - mobile sidebarul nu mai este afisat inline.
  - adaugat mobile header compact cu:
    - hamburger menu.
    - brand mark.
    - numele proiectului curent.
    - buton notificari.
  - la tap pe hamburger se deschide un drawer lateral cu overlay si buton de inchidere.
  - la schimbarea rutei drawerul se inchide automat.
  - desktop pastreaza sidebarul clasic fix.
- frontend/app/dashboard/layout.tsx:
  - ramane layout responsive `flex-col` pe mobile si `md:flex-row` pentru desktop.
- frontend/app/globals.css:
  - extins override-ul Light pentru opacitati Tailwind care lipseau:
    - `bg-slate-950/20..95`
    - `bg-slate-900/20..95`
    - `bg-slate-800/10..95`
    - `bg-slate-700/20..90`
  - adaugat tint Light pentru zone destructive:
    - `bg-red-900/20`
    - `bg-red-900/40`
    - `bg-red-950/10`
    - `bg-red-950/20`
    - `bg-rose-950/10`
    - `bg-rose-950/20`
  - efect: boardul, calendarul, headers de carduri, task detail si danger zone nu mai raman gri/dark in Light.

Verificari Playwright batch BL:
- Mobile dashboard inchis:
  - `/tmp/sdlc-mobile-drawer-light.png`
  - confirma header compact + continut vizibil imediat.
- Mobile drawer deschis:
  - `/tmp/sdlc-mobile-drawer-open-light.png`
  - confirma navigatie in drawer, overlay si buton close.
- Desktop Light:
  - `/tmp/sdlc-board-light-v2.png`
  - `/tmp/sdlc-calendar-light-v2.png`
  - `/tmp/sdlc-dashboard-light-v4.png`
  - `/tmp/sdlc-task-detail-light-v2.png`
  - `/tmp/sdlc-backlog-light-v2.png`
  - `/tmp/sdlc-settings-light-v2.png`

Verificari batch BL:
- `npm run lint`: OK.
- `npm run build`: OK, ramane warning-ul existent Next `--localstorage-file` fara path valid.

Fisiere cheie batch BL:
- frontend/components/dashboard/app-sidebar.tsx
- frontend/app/dashboard/layout.tsx
- frontend/app/globals.css
- PLAN_CONTINUARE_LICENTA_SDLC_HUB.txt

Ce ramane dupa batch BL:
1. Verificare Light/Dark pe Global Admin Console cu aceeasi disciplina Playwright.
2. Verificare responsive mobile pentru pagini complexe:
   - Activity
   - Tasks
   - Task detail
   - Board
   - Backlog
   - Calendar
   - Settings
   - DevOps
   - Reports
   - Workload
3. Daca mai apar zone dark in Light, preferabil se repara punctual in componenta, nu doar prin override global.

Batch BM - Fix drawer notificari, hover Light, Project AI provider si Incident Focus Codex 3 iunie 2026
Context:
- utilizatorul a raportat urmatoarele probleme vizuale/UX:
  - in drawer-ul mobile, butonul `X` statea peste butonul de notificari reutilizat din headerul sidebarului.
  - hover/select in Light arata dark/negru pe itemii din sidebar.
  - Project AI provider din Settings avea text violet aproape invizibil in Light.
  - Incident Focus din Global Admin nu ducea la ticket/Support Desk si nu arata selectie clara.
  - unele pagini mobile trebuiau reverificate.

Implementat:
- frontend/components/dashboard/app-sidebar.tsx:
  - butonul de notificari din headerul sidebarului este ascuns in drawer-ul mobile (`hidden md:flex`).
  - pe mobile ramane doar butonul de notificari din headerul compact, deci `X` nu mai sta peste alt control.
- frontend/app/globals.css:
  - adaugat suport Light pentru `text-violet-100` si variantele cu opacitate.
  - adaugat suport Light pentru `bg-violet-500/15` si `bg-purple-500/15`.
  - adaugate override-uri Light pentru hover/focus dark:
    - `hover:bg-slate-950/900/800/700`
    - `hover:text-white`
    - `focus:bg-slate-950/900/800`
    - `focus:text-white`
  - efect: hoverul pe itemii de navigatie nu mai devine dark/negru in Light.
- frontend/app/admin/page.tsx:
  - adaugata functia `focusSupportTicket`.
  - click pe `Open desk` duce la sectiunea Support Desk.
  - click pe un incident:
    - seteaza ticketul selectat.
    - reseteaza filtrul la `ALL`, ca ticketul sa fie vizibil.
    - actualizeaza hash-ul la `#support`.
    - face scroll in containerul admin catre Support Desk.
  - incidentul selectat are highlight vizual in lista Incident Focus.

Verificari Playwright batch BM:
- Mobile drawer hover:
  - `/tmp/sdlc-mobile-drawer-hover-light.png`
  - Workload hover ramane light, nu dark/negru.
- Settings / Project AI provider:
  - `/tmp/sdlc-settings-ai-light-v3.png`
  - textul violet este lizibil in Light.
- Admin / Incident Focus:
  - `/tmp/sdlc-admin-incident-click-light.png`
  - click pe incident seteaza `hash="#support"` si scrollul ajunge la Support Desk.
- Mobile overflow check:
  - verificate rutele:
    - `/dashboard`
    - `/dashboard/tasks`
    - `/dashboard/activity`
    - `/dashboard/tasks/1`
    - `/dashboard/board`
    - `/dashboard/backlog`
    - `/dashboard/calendar`
    - `/dashboard/settings`
    - `/dashboard/devops`
    - `/dashboard/reports`
    - `/dashboard/workload`
    - `/admin`
  - rezultat: overflow orizontal `0` pe viewport 390px.
- Capturi mobile generate:
  - `/tmp/sdlc-mobile-tasks-light.png`
  - `/tmp/sdlc-mobile-activity-light.png`
  - `/tmp/sdlc-mobile-task-detail-light.png`
  - `/tmp/sdlc-mobile-board-light.png`
  - `/tmp/sdlc-mobile-settings-light.png`
  - `/tmp/sdlc-mobile-admin-light.png`

Verificari batch BM:
- `npm run lint`: OK.
- `npm run build`: OK, ramane warning-ul existent Next `--localstorage-file` fara path valid.

Fisiere cheie batch BM:
- frontend/components/dashboard/app-sidebar.tsx
- frontend/app/globals.css
- frontend/app/admin/page.tsx
- PLAN_CONTINUARE_LICENTA_SDLC_HUB.txt

Ce ramane dupa batch BM:
1. Testare manuala pe device real/emulator pentru scroll vertical lung, nu doar first viewport.
2. Polish punctual pe pagini mobile daca apar probleme mai jos in pagina:
   - Board lane navigation.
   - Calendar agenda/filters.
   - Task detail comments/audit log.
   - Settings sectiunile DevOps/WIP/Permissions mai jos in pagina.
3. Dupa finalizarea polish-ului vizual, revenire la functionalitatile incomplete.

Batch BN - Audit vizual automat si responsive cleanup Codex 3 iunie 2026
Context:
- utilizatorul a cerut sa prindem bug-urile vizuale inainte de:
  - skeleton loaders la incarcare.
  - animatie la deschiderea/inchiderea meniului mobile.
  - optimizari de performanta, inclusiv input lag la cautari.
- era nevoie de un audit repetabil, nu doar verificare manuala pe screenshot-uri.

Implementat:
- scripts/visual_audit_playwright.js:
  - script nou Playwright pentru audit UI pe light/dark si mobile/desktop.
  - rute acoperite:
    - `/dashboard`
    - `/dashboard/tasks`
    - `/dashboard/activity`
    - `/dashboard/tasks/1`
    - `/dashboard/board`
    - `/dashboard/backlog`
    - `/dashboard/calendar`
    - `/dashboard/settings`
    - `/dashboard/devops`
    - `/dashboard/reports`
    - `/dashboard/workload`
    - `/dashboard/team`
    - `/dashboard/support`
    - `/dashboard/documentation`
    - `/admin`
  - genereaza screenshot-uri in `/tmp/sdlc-visual-audit`.
  - scrie raport JSON in `/tmp/sdlc-visual-audit/report.json`.
  - verifica:
    - overflow orizontal document.
    - elemente iesite din viewport.
    - request failures.
    - console errors.
    - suprafete dark suspecte in Light.
  - ignoreaza false positive-uri uzuale:
    - fundaluri transparente.
    - scroll orizontal intentionat in mobile pentru zone dedicate.
- frontend/app/dashboard/board/page.tsx:
  - coloanele Kanban nu mai forteaza `360px` pe desktop.
  - coloanele sunt mai compacte pe mobile si flexibile pe desktop.
  - board-ul ramane scrollabil orizontal acolo unde este normal, dar nu mai impinge layout-ul desktop.
- frontend/app/dashboard/devops/page.tsx:
  - adaugat `min-w-0`/latimi fluide la cardurile de repository si webhook.
  - panourile "How this works" si "Webhook endpoint" nu mai forteaza latime desktop pe mobile.
- frontend/app/admin/page.tsx:
  - zonele Operations Pulse, Incident Focus si Support Desk au primit `min-w-0`, `overflow-hidden` si grid-uri mobile corecte.
  - Admin mobile nu mai are panouri late care ies din viewport mai jos in pagina.
- frontend/app/dashboard/workload/page.tsx:
  - containerul principal este `w-full min-w-0 overflow-x-hidden`.
  - cardurile membrilor sunt `min-w-0 max-w-full overflow-hidden`.
  - Workload mobile nu mai produce overflow pe cardurile lungi.
- frontend/app/dashboard/documentation/page.tsx:
  - grid-ul principal foloseste `grid-cols-[minmax(0,1fr)]` pe mobile.
  - Pages/Version history/editor preview au `min-w-0 max-w-full overflow-hidden`.
  - Project Wiki nu mai este impins in afara viewportului de continutul editorului/preview-ului.
- frontend/app/dashboard/team/page.tsx:
  - selectorul Zustand pentru presence foloseste `EMPTY_ONLINE_USER_IDS`.
  - a disparut warning-ul React: `The result of getSnapshot should be cached to avoid an infinite loop`.
- frontend/components/ui/progress.tsx:
  - Progress indicator mutat de la `translateX(-...)` la `scaleX(...)`.
  - elimina elementul raportat in afara viewportului fara sa schimbe aspectul vizual.
- frontend/app/globals.css:
  - focus ring global albastru discret pentru controale interactive.
  - evita outline-ul default punctat/galbui care aparea urat in unele screenshot-uri.

Verificari Playwright batch BN:
- audit complet:
  - comanda:
    - `NODE_PATH=frontend/node_modules SDLC_BASE_URL=http://127.0.0.1:3000 node scripts/visual_audit_playwright.js`
  - rezultat final:
    - `findings: []`
  - raport:
    - `/tmp/sdlc-visual-audit/report.json`
  - screenshot-uri principale:
    - `/tmp/sdlc-visual-audit/light-mobile-dashboard-documentation.png`
    - `/tmp/sdlc-visual-audit/light-mobile-dashboard-workload.png`
    - `/tmp/sdlc-visual-audit/light-mobile-admin.png`
    - `/tmp/sdlc-visual-audit/light-desktop-dashboard-board.png`
    - `/tmp/sdlc-visual-audit/dark-desktop-dashboard-board.png`

Verificari batch BN:
- `npm run lint`: OK.
- `npm run build`: OK.
- ramane warning-ul existent Next:
  - `--localstorage-file was provided without a valid path`.
  - nu blocheaza build-ul, dar merita investigat la polish tehnic.

Fisiere cheie batch BN:
- scripts/visual_audit_playwright.js
- frontend/app/dashboard/board/page.tsx
- frontend/app/dashboard/devops/page.tsx
- frontend/app/admin/page.tsx
- frontend/app/dashboard/workload/page.tsx
- frontend/app/dashboard/documentation/page.tsx
- frontend/app/dashboard/team/page.tsx
- frontend/components/ui/progress.tsx
- frontend/app/globals.css
- PLAN_CONTINUARE_LICENTA_SDLC_HUB.txt

Ce ramane dupa batch BN:
1. Skeleton loaders:
   - Dashboard.
   - Tasks.
   - Task detail.
   - Board.
   - Backlog.
   - Calendar.
   - Workload.
   - Reports.
   - Documentation.
   - DevOps.
   - Admin.
2. Animatie mobile drawer:
   - intrare/iesire meniu hamburger.
   - overlay fade.
   - inchidere fluida la click pe link sau backdrop.
3. Performance pass:
   - cautari cu debounce/useDeferredValue.
   - liste lungi: reducere re-render si eventual virtualizare unde doare.
   - memoizare filtre costisitoare.
   - verificare request-uri duplicate pe pagini.
4. Investigare warning build:
   - `--localstorage-file was provided without a valid path`.
5. Continuat verificari UI/UX manuale dupa fiecare batch, folosind si scriptul de audit.


6.19. BATCH BO - Account cleanup, SDLC checklist removal si skeleton loaders initiale

Context cerere:
- Eliminare card "Account notes" din Account Settings, pentru ca nu aduce valoare vizuala/functionala.
- Eliminare functionalitate "SDLC checklist" din Task Detail si Create Issue.
- Continuare cu urmatorul pas logic: stari de incarcare mai bune prin skeleton loaders.

Implementat:
- frontend/app/dashboard/account/page.tsx:
  - eliminat complet cardul "Account notes".
  - panoul de securitate ocupa acum latimea completa a grid-ului de jos.
  - adaugat skeleton initial pentru pagina Account:
    - header profil.
    - metrici cont.
    - carduri profile/projects.
    - zona sessions/security log.
- frontend/components/dashboard/account-security-panel.tsx:
  - panoul este impartit in grid modern:
    - Active sessions: 7 coloane pe desktop.
    - Security log: 5 coloane pe desktop.
  - inlocuit textele "Loading sessions..." / "Loading security log..." cu skeleton cards.
- frontend/app/dashboard/tasks/[taskId]/page.tsx:
  - eliminat constanta `SDLC_CHECKLIST`.
  - eliminat handler-ul de aplicare checklist SDLC.
  - eliminat butonul "SDLC checklist" din header-ul Subtasks.
  - texte schimbate din "checklist" in "subtasks" acolo unde era vorba de subtasks reale.
  - adaugat skeleton initial pentru Task Detail:
    - header task.
    - coloana principala.
    - properties/sidebar.
- frontend/components/dashboard/create-task-dialog.tsx:
  - eliminat constanta `SDLC_CHECKLIST`.
  - eliminat state-ul `templateSubtasks`.
  - eliminat handler-ul `handleUseSdlcChecklist`.
  - eliminat blocul UI "SDLC checklist".
  - ramas doar fluxul corect: subtasks sugerate de AI din Spec Refiner.
- frontend/components/ui/skeleton.tsx:
  - adaugat component reutilizabil `Skeleton`.
  - suporta light/dark prin clase dedicate:
    - `bg-slate-200/80`
    - `dark:bg-slate-800/75`
- frontend/components/dashboard/workspace-loading-skeleton.tsx:
  - adaugat skeleton comun pentru pagini mari de workspace.
  - variante incluse:
    - header + metric cards + panels.
    - table rows.
    - side panel.
    - board columns skeleton.
- frontend/app/dashboard/tasks/page.tsx:
  - adaugat skeleton initial pentru Tasks List:
    - hero.
    - metrici.
    - filtre.
    - randuri tabel/lista.
- frontend/app/dashboard/page.tsx:
  - adaugat skeleton initial pentru Dashboard:
    - hero.
    - metrici.
    - Delivery Signals.
    - Work Distribution / Activity area.
- frontend/app/dashboard/activity/page.tsx:
  - adaugat skeleton initial pentru Activity:
    - hero.
    - metrici.
    - filters column.
    - timeline cards.
- frontend/app/dashboard/layout.tsx:
  - spinner-ul initial al dashboard shell-ului a fost inlocuit cu un skeleton de layout:
    - sidebar placeholder pe desktop.
    - content placeholder.
- frontend/app/dashboard/board/page.tsx:
  - loading-ul coloanelor Board foloseste `BoardLoadingSkeleton`.
  - montarea initiala nu mai intoarce `null`; afiseaza skeleton de board.
- frontend/app/dashboard/workload/page.tsx:
  - loading-ul initial foloseste skeleton comun cu zona laterala.
- frontend/app/dashboard/reports/page.tsx:
  - loading-ul initial foloseste skeleton comun cu metrici si panouri grafice.
- frontend/app/dashboard/calendar/page.tsx:
  - loading-ul initial foloseste skeleton comun pentru calendar/events/side panel.
- frontend/app/dashboard/devops/page.tsx:
  - loading-ul initial foloseste skeleton comun pentru repository/config/event panels.
- frontend/app/dashboard/backlog/page.tsx:
  - loading-ul initial foloseste skeleton comun cu randuri de planning/lista.
- frontend/app/dashboard/documentation/page.tsx:
  - loading-ul initial foloseste skeleton comun cu side panel.
- frontend/app/dashboard/team/page.tsx:
  - loading-ul initial foloseste skeleton comun cu metrici, panouri si randuri.
- frontend/app/dashboard/support/page.tsx:
  - lista de support tickets afiseaza skeleton cards cat timp se incarca.
- frontend/app/dashboard/notifications/page.tsx:
  - loading-ul initial foloseste skeleton comun cu lista.

Verificari batch BO:
- `rg` pentru ramasite:
  - `SDLC checklist`
  - `SDLC_CHECKLIST`
  - `handleApplySdlcChecklist`
  - `handleUseSdlcChecklist`
  - `Account notes`
  - rezultat: fara match-uri in fisierele vizate.
- `npm run lint`: OK.
- `npm run build`: OK.
- Playwright visual audit:
  - comanda:
    - `NODE_PATH=frontend/node_modules SDLC_BASE_URL=http://127.0.0.1:3000 node scripts/visual_audit_playwright.js`
  - rezultat:
    - `findings: []`
  - raport:
    - `/tmp/sdlc-visual-audit/report.json`

Fisiere cheie batch BO:
- frontend/components/ui/skeleton.tsx
- frontend/components/dashboard/workspace-loading-skeleton.tsx
- frontend/app/dashboard/layout.tsx
- frontend/app/dashboard/page.tsx
- frontend/app/dashboard/tasks/page.tsx
- frontend/app/dashboard/tasks/[taskId]/page.tsx
- frontend/components/dashboard/create-task-dialog.tsx
- frontend/app/dashboard/account/page.tsx
- frontend/components/dashboard/account-security-panel.tsx
- frontend/app/dashboard/activity/page.tsx
- frontend/app/dashboard/board/page.tsx
- frontend/app/dashboard/workload/page.tsx
- frontend/app/dashboard/reports/page.tsx
- frontend/app/dashboard/calendar/page.tsx
- frontend/app/dashboard/devops/page.tsx
- frontend/app/dashboard/backlog/page.tsx
- frontend/app/dashboard/documentation/page.tsx
- frontend/app/dashboard/team/page.tsx
- frontend/app/dashboard/support/page.tsx
- frontend/app/dashboard/notifications/page.tsx
- PLAN_CONTINUARE_LICENTA_SDLC_HUB.txt

Ce ramane imediat dupa batch BO:
1. Admin console loading polish:
   - consola Admin nu are inca un gate de loading global.
   - de adaugat skeleton granular pe metrici/liste cand `loading === true`, fara sa stricam datele deja afisate.
2. Animatie mobile drawer:
   - intrare/iesire.
   - overlay fade.
   - inchidere fluida la click pe link/backdrop.
3. Performance pass:
   - debounce/useDeferredValue pentru cautari.
   - reducere re-render la liste lungi.
   - verificare request-uri duplicate.
   - eventual virtualizare pentru listele foarte lungi.
4. Investigare warning build:
   - `--localstorage-file was provided without a valid path`.
5. UI/UX bug hunting:
   - continuat cu screenshots manuale + scriptul Playwright.


6.20. BATCH BP - Mobile drawer animation si primul performance pass

Context cerere:
- Continuare dupa skeleton loaders.
- Urmatorul pas logic din plan:
  - animatie pentru mobile drawer.
  - performanta mai buna la cautari/listari.
  - curatare warning-uri de lint aparute in Reports.

Implementat:
- frontend/components/dashboard/app-sidebar.tsx:
  - drawer-ul mobile nu mai este montat/demontat brusc doar cu `mobileOpen &&`.
  - adaugat `mobileDrawerMounted` pentru animatie de iesire.
  - adaugat `mobileCloseTimeoutRef` pentru a evita race condition la open/close rapid.
  - adaugate tranzitii:
    - overlay fade `opacity`.
    - drawer slide `translate-x`.
  - adaugat body scroll lock cat drawer-ul este montat.
  - inchiderea la schimbare ruta ramane functionala.
- frontend/app/dashboard/tasks/page.tsx:
  - cautarea foloseste `useDeferredValue(query)`.
  - input-ul ramane instant, filtrarea listei se decaleaza usor pentru a evita blocarea tastarii.
- frontend/app/dashboard/activity/page.tsx:
  - cautarea timeline-ului foloseste `useDeferredValue(searchTerm)`.
  - util pentru audit streams mari.
- frontend/app/dashboard/team/page.tsx:
  - cautarea membrilor foloseste `useDeferredValue(search)`.
  - reduce blocajele cand lista de membri/roluri creste.
- frontend/app/dashboard/reports/page.tsx:
  - eliminat cod mort pentru Release Notes din Reports.
  - butonul era comentat, dar state-uri/handlere/importuri ramasesera active.
  - curatate warning-urile ESLint:
    - `generatingNotes` nefolosit.
    - `handleGenerateReleaseNotes` nefolosit.

Verificari batch BP:
- `npm run lint`: OK, fara warning-uri.
- `npm run build`: OK.
- ramane warning-ul existent Next:
  - `--localstorage-file was provided without a valid path`.
  - nu blocheaza build-ul.
- Playwright visual audit:
  - comanda:
    - `NODE_PATH=frontend/node_modules SDLC_BASE_URL=http://127.0.0.1:3000 node scripts/visual_audit_playwright.js`
  - rezultat:
    - `findings: []`
  - raport:
    - `/tmp/sdlc-visual-audit/report.json`

Fisiere cheie batch BP:
- frontend/components/dashboard/app-sidebar.tsx
- frontend/app/dashboard/tasks/page.tsx
- frontend/app/dashboard/activity/page.tsx
- frontend/app/dashboard/team/page.tsx
- frontend/app/dashboard/reports/page.tsx
- PLAN_CONTINUARE_LICENTA_SDLC_HUB.txt

Ce ramane imediat dupa batch BP:
1. Performance pass continuare:
   - verificare request-uri duplicate in paginile mari.
   - eventual virtualizare pentru Tasks/Activity daca seed-ul creste mult.
2. Investigare warning build:
   - `--localstorage-file was provided without a valid path`.
3. UI/UX bug hunting manual:
   - continuat pe screenshot-uri punctuale.


6.21. BATCH BQ - Admin loading polish si Admin deferred search

Context:
- Dupa mobile drawer si deferred search pe paginile principale, a ramas Admin console loading polish.

Implementat:
- frontend/app/admin/page.tsx:
  - adaugat `AdminConsoleSkeleton`.
  - skeleton-ul se afiseaza doar la prima incarcare, cand nu exista date locale:
    - `loading === true`
    - `overview === null`
    - users/projects/tickets/errors/aiUsage sunt goale.
  - la refresh dupa ce exista date, consola ramane vizibila si nu face flicker.
  - adaugat `useDeferredValue(query)` pentru cautarea globala din Admin.
  - `normalizedQuery` foloseste `deferredQuery`, reducand blocajele cand filtreaza simultan users/projects/tickets/errors/AI logs.

Verificari batch BQ:
- `npm run lint`: OK.
- `npm run build`: OK.
- ramane warning-ul existent Next:
  - `--localstorage-file was provided without a valid path`.
- Playwright visual audit:
  - comanda:
    - `NODE_PATH=frontend/node_modules SDLC_BASE_URL=http://127.0.0.1:3000 node scripts/visual_audit_playwright.js`
  - rezultat:
    - `findings: []`
  - raport:
    - `/tmp/sdlc-visual-audit/report.json`

Fisiere cheie batch BQ:
- frontend/app/admin/page.tsx
- PLAN_CONTINUARE_LICENTA_SDLC_HUB.txt

Ce ramane imediat dupa batch BQ:
1. Performance pass continuare:
   - verificare request-uri duplicate in paginile mari.
   - eventual virtualizare pentru Tasks/Activity daca seed-ul creste mult.
2. Investigare warning build:
   - `--localstorage-file was provided without a valid path`.
3. UI/UX bug hunting manual:
   - continuat pe screenshot-uri punctuale.


6.22. BATCH BR - Project persistence, multi-project access si dialog responsive

Context cerere:
- Inainte de continuarea planului initial au aparut 3 bug-uri/prioritati:
  1. La refresh dintr-un proiect secundar, aplicatia revenea la primul proiect din lista.
  2. Un utilizator deja inregistrat si membru intr-un proiect nu avea o cale clara sa intre si in alt proiect.
  3. Modalurile/dialogurile aratau inconsistent si se rupeau pe mobile.

Implementat:
- frontend/store/use-project-store.ts:
  - adaugat `hasHydrated` in store-ul de proiect.
  - persist-ul seteaza explicit `hasHydrated` dupa rehidratarea din localStorage.
  - scop: layout-ul si sidebar-ul nu mai aleg primul proiect pana cand proiectul salvat nu este incarcat.
- frontend/app/dashboard/layout.tsx:
  - efectul de incarcare a contextului de proiect asteapta `hasHydrated`.
  - previne suprascrierea proiectului curent la refresh.
- frontend/components/dashboard/app-sidebar.tsx:
  - lista de workspace-uri asteapta `hasHydrated`.
  - schimbarea manuala a proiectului ramane neschimbata, dar refresh-ul nu mai forteaza primul proiect.
- frontend/app/dashboard/account/page.tsx:
  - adaugata sectiunea `Project access`.
  - utilizatorul vede invitatiile pending pentru email-ul lui si le poate accepta direct.
  - adaugat camp manual `Join with code` pentru coduri primite de la Project Admin.
  - dupa accept/join, proiectul nou este setat ca workspace curent si utilizatorul este dus in dashboard.
  - aceasta rezolva fluxul de "append to another project" pentru userii deja existenti.
- frontend/components/ui/dialog.tsx:
  - baza globala pentru modaluri este acum responsive:
    - desktop: dialog centrat.
    - mobile: dialog cu margini, `max-height`, scroll intern si animatie mai naturala.
  - overlay-ul are blur si contrast mai bun.
  - butonul de inchidere are hit area mai buna pe mobile.

Verificari batch BR:
- `npm run lint`: OK.
- `npm run build`: OK.
- ramane warning-ul existent Next:
  - `--localstorage-file was provided without a valid path`.
  - nu blocheaza build-ul.
- Playwright visual audit:
  - comanda:
    - `NODE_PATH=frontend/node_modules SDLC_BASE_URL=http://127.0.0.1:3000 node scripts/visual_audit_playwright.js`
  - rezultat:
    - `findings: []`
  - raport:
    - `/tmp/sdlc-visual-audit/report.json`

Fisiere cheie batch BR:
- frontend/store/use-project-store.ts
- frontend/app/dashboard/layout.tsx
- frontend/components/dashboard/app-sidebar.tsx
- frontend/app/dashboard/account/page.tsx
- frontend/components/ui/dialog.tsx
- PLAN_CONTINUARE_LICENTA_SDLC_HUB.txt

Ce ramane imediat dupa batch BR:
1. Verificare manuala multi-project:
   - selecteaza proiectul 2, refresh pe `/dashboard/*`, trebuie sa ramana proiectul 2.
   - accepta o invitatie din Account, proiectul acceptat trebuie sa devina curent.
2. Audit complet modaluri:
   - Create Issue.
   - Create Sprint / Complete Sprint.
   - Invite teammate.
   - Delete/Archive/Restore confirmations.
   - Support ticket/comment dialogs.
3. Dupa stabilizarea acestor bug-uri, revenire la planul initial:
   - performance pass.
   - bug-uri logice.
   - functionalitati ramase din lista licentei.


6.23. BATCH BS - Modal layout polish pentru Calendar si Create Issue

Context cerere:
- Modalul de calendar avea campurile de timp inghesuite.
- Modalul `Create New Issue` nu era aranjat profesional si trebuia verificat si pe mobile.
- Cerere generala: pass peste modaluri inainte de revenirea la planul initial.

Implementat:
- frontend/app/dashboard/calendar/page.tsx:
  - modalul de event nu mai foloseste override local `max-h-[92vh] overflow-y-auto`; se bazeaza pe componenta globala responsive `DialogContent`.
  - `Type` si `Date` sunt intr-un rand separat.
  - `Start` si `End` sunt intr-un rand separat, cu latime suficienta pentru afisari locale cu AM/PM.
  - blocul de timp este incadrat vizual intr-un panel discret.
  - `Repeat` foloseste un grid mai flexibil.
  - footer-ul are butoane full-width pe mobile si compacte pe desktop.
- frontend/components/dashboard/create-task-dialog.tsx:
  - modalul a fost largit la `sm:max-w-2xl`.
  - metadatele taskului sunt grupate intr-un panel:
    - Priority.
    - Story Points.
    - Assignee.
    - Delivery Team.
    - Target Date.
  - `Delivery Team` si `Target Date` sunt puse pe rand separat pentru un layout mai echilibrat.
  - actiunile AI din Description folosesc wrapping natural pe mobile.
  - footer-ul foloseste butoane full-width pe mobile si compacte pe desktop.
- frontend/app/dashboard/settings/page.tsx:
  - eliminat override-ul local de scroll/max-height pe dialogul de schimbare metodologie.
- frontend/app/dashboard/backlog/page.tsx:
  - dialogul mare de Release Notes foloseste `sm:max-w-3xl`, nu `max-w-3xl` global pe toate viewporturile.

Verificari batch BS:
- `npm run lint`: OK.
- `npm run build`: OK.
- ramane warning-ul existent Next:
  - `--localstorage-file was provided without a valid path`.
  - nu blocheaza build-ul.
- Playwright screenshots generate pentru:
  - `/tmp/sdlc-modal-issue-desktop.png`
  - `/tmp/sdlc-modal-issue-mobile.png`
  - `/tmp/sdlc-modal-calendar-desktop.png`
  - `/tmp/sdlc-modal-calendar-mobile.png`
- Capturile confirma:
  - Create Issue nu mai are randul principal inghesuit pe desktop.
  - Create Issue se stivuieste corect pe mobile.
  - Calendar event are Start/End suficient de late pe desktop.
  - Calendar event se stivuieste coerent pe mobile.

Fisiere cheie batch BS:
- frontend/app/dashboard/calendar/page.tsx
- frontend/components/dashboard/create-task-dialog.tsx
- frontend/app/dashboard/settings/page.tsx
- frontend/app/dashboard/backlog/page.tsx
- PLAN_CONTINUARE_LICENTA_SDLC_HUB.txt

Ce ramane imediat dupa batch BS:
1. Continuare plan initial:
   - performance pass.
   - bug-uri logice.
   - functionalitati ramase din lista licentei.
2. UI/UX manual pe modaluri speciale:
   - support ticket inspect/reply.
   - admin inspect dialogs.
   - task estimate invalidation / delete confirmations.


6.24. BATCH BT - Tasks metrics click surface si Create Issue final alignment

Context cerere:
- Cardul `Overdue` din pagina Tasks avea doua contururi/carduri suprapuse.
- Selectarea/apasarea trebuia sa functioneze pe toata suprafata cardului.
- Modalul `Create New Issue` inca parea dezorganizat dupa primul pass.

Implementat:
- frontend/app/dashboard/tasks/page.tsx:
  - `Overdue` nu mai este un `Card` cu `button` inauntru.
  - `Overdue` este acum un button-card direct, cu un singur contur.
  - click-ul functioneaza pe toata suprafata cardului.
  - `Done` si `Unassigned` au primit acelasi comportament full-surface pentru filtrele lor.
  - `Active` ramane metric informativ, pentru ca nu exista inca filtru dedicat `not done` in dropdown.
- frontend/components/dashboard/create-task-dialog.tsx:
  - grid-ul de planning a fost simplificat la doua coloane egale pe desktop si o coloana pe mobile.
  - toate Select/Input controls din planning au `w-full`.
  - `Estimate` este aliniat in header-ul campului Story Points.
  - `Target Date` ocupa rand complet pentru a elimina golul vizual din dreapta.

Verificari batch BT:
- `npm run lint`: OK.
- `npm run build`: OK inainte de ultimul micro-fix; dupa micro-fix s-a rerulat `npm run lint`: OK.
- Playwright screenshots:
  - `/tmp/sdlc-tasks-metrics-dark.png`
  - `/tmp/sdlc-modal-issue-desktop-final.png`

Fisiere cheie batch BT:
- frontend/app/dashboard/tasks/page.tsx
- frontend/components/dashboard/create-task-dialog.tsx
- PLAN_CONTINUARE_LICENTA_SDLC_HUB.txt

Ce ramane imediat dupa batch BT:
1. Revenire la planul initial.
2. Continuare cu performance pass si bug-uri logice.


6.25. BATCH BU - Performance pass: request dedupe si Activity render window

Context:
- Revenire la planul initial dupa polish-ul modalurilor si cardurilor Tasks.
- Urmatorul pas ramas era performance pass:
  - verificare request-uri duplicate in paginile mari.
  - reducerea costului de randare pentru liste mari.
  - investigarea warning-ului build `--localstorage-file`.

Implementat:
- frontend/services/project.ts:
  - adaugat helper intern `cachedGet` cu TTL foarte scurt, 1500ms.
  - scopul nu este cache agresiv, ci dedupe pentru request-uri simultane identice la mount/navigare.
  - endpointuri deduplicate:
    - `getMyProjects`;
    - `getProjectMembers`;
    - `getMyProjectPermissions`;
    - `getProjectRoles`;
    - `getProjectTeams`.
  - adaugata invalidare dupa mutatii relevante:
    - join project;
    - create project;
    - update settings/workflow/AI settings;
    - methodology transition;
    - roles create/update/delete/permissions;
    - invite/cancel invitation;
    - update/remove member;
    - teams create/update/delete;
    - update member team.
  - efect asteptat:
    - layout/sidebar/paginile nu mai lovesc simultan aceleasi endpointuri cand se monteaza.
    - datele raman proaspete dupa actiuni, pentru ca mutatiile invalideaza cache-ul.
- frontend/app/dashboard/activity/page.tsx:
  - adaugat render window pentru timeline:
    - initial 60 evenimente vizibile.
    - `Show more events` adauga cate 60.
  - filtrele continua sa se aplice pe toate evenimentele incarcate, dar DOM-ul initial este mai mic.
  - la schimbarea filtrelor/search/sort/time se reseteaza vizibilitatea la primele 60.

Investigatie warning build:
- Warning-ul Next:
  - `--localstorage-file was provided without a valid path`.
- In mediul curent exista `ELECTRON_RUN_AS_NODE=1`.
- Test cu `env -u ELECTRON_RUN_AS_NODE npm run build`:
  - warning-ul nu a mai aparut;
  - build-ul a picat insa din cauza fetch-ului catre Google Fonts, blocat de network/sandbox:
    - `Failed to fetch Inter from Google Fonts`.
- Concluzie:
  - warning-ul pare de mediu/runtime Electron, nu bug in codul aplicatiei.
  - nu am schimbat scripturile npm, pentru ca build-ul actual trece stabil in mediul curent.

Verificari batch BU:
- `npm run lint`: OK.
- `npm run build`: OK.
- warning-ul `--localstorage-file` ramane, dar build-ul trece.

Fisiere cheie batch BU:
- frontend/services/project.ts
- frontend/app/dashboard/activity/page.tsx
- PLAN_CONTINUARE_LICENTA_SDLC_HUB.txt

Ce ramane imediat dupa batch BU:
1. Continuare bug-uri logice:
   - arhivare proiect / readonly.
   - support tickets comments/delete flows.
   - realtime unde inca se simte ca reload.
2. Performance pass suplimentar posibil:
   - request dedupe similar pe task/sprint/calendar services daca apar duplicate masive.
   - virtualizare reala daca Activity/Tasks depasesc cateva sute/mii de randuri.


6.26. BATCH BV - Stabilizare tranzitii metodologie si workflow board

Context:
- Revenire la planul initial dupa batch-ul de performance.
- Bug vizat:
  - dupa treceri repetate SCRUMBAN -> SCRUM -> KANBAN -> SCRUMBAN, board-ul putea ramane cu configuratii/coloane vechi si se simtea ca se reincarca in bucla.
- Cauza principala gasita:
  - endpoint-ul de methodology transition schimba metodologia si task-urile/sprinturile, dar nu reseta explicit `workflow_config`.
  - proiectele vechi fara `workflow_config` primeau default generic, nu presetul metodologiei active.

Implementat:
- backend/routers/projects.py:
  - adaugate preseturi server-side pentru workflow:
    - SCRUM:
      - Sprint To Do / In Progress / Sprint Review / Done.
      - fara WIP limits active implicit.
    - KANBAN:
      - Intake / In Progress / Review / Done.
      - WIP limits implicite pentru In Progress si Review.
    - SCRUMBAN:
      - Ready / In Progress / Code Review / Done.
      - WIP limits implicite pentru flow control.
  - adaugat helper `_workflow_config_for_methodology`.
  - la creare proiect se salveaza direct workflow-ul potrivit metodologiei alese.
  - la methodology transition:
    - se schimba metodologia;
    - se reseteaza `workflow_config` la presetul metodologiei tinta;
    - se pastreaza audit pentru schimbarea metodologiei;
    - se adauga audit separat `PROJECT_WORKFLOW_UPDATED` cu motiv `methodology_transition`;
    - broadcast-ul realtime trimite proiectul serializat cu workflow-ul nou.
  - adaugat helper `_project_workflow_config`:
    - daca proiectul are config salvat, il normalizeaza;
    - daca nu are config, intoarce presetul metodologiei lui.

Impact asteptat:
- Board-ul, Settings si Reports primesc acelasi workflow dupa refresh/realtime.
- Tranzitiile repetate intre metodologii nu mai cara accidental coloane/limite vechi.
- Proiectele create inainte de `workflow_config` raman coerente cu metodologia curenta.

Verificari batch BV:
- `.venv/bin/python -m py_compile backend/routers/projects.py`: OK.
- `npm run lint`: OK.
- `npm run build`: OK.
- Warning-ul `--localstorage-file` ramane acelasi warning de mediu identificat in batch BU, dar build-ul trece.

Fisiere cheie batch BV:
- backend/routers/projects.py
- PLAN_CONTINUARE_LICENTA_SDLC_HUB.txt

Ce ramane imediat dupa batch BV:
1. Continuare bug-uri logice:
   - support tickets comments/delete flows.
   - realtime unde inca se simte ca reload.
2. Functionalitati/polish ramase din planul initial:
   - skeleton loaders pe paginile mari.
   - animatie drawer mobile la inchidere/deschidere.
   - performance suplimentar pe task/sprint/calendar services daca se observa duplicate.


6.27. BATCH BW - Arhivare read-only backend si browser branding

Context:
- Continuare plan initial.
- Prioritate logica ramasa:
  - proiect arhivat trebuie sa ramana accesibil pentru citire, dar actiunile trebuie dezactivate real, inclusiv la nivel API.
- Cerere adaugata in timpul batch-ului:
  - in browser sa apara `SDLC Hub` si logo-ul aplicatiei, nu `SDLC AI Hub` / favicon Next.js.

Implementat arhivare:
- backend/utils/permissions.py:
  - adaugat `ensure_project_not_archived(project)`.
  - `require_project_permission` blocheaza proiectele arhivate pentru toate permisiunile mutative.
  - exceptie pastrata pentru `REPORT_VIEW`, ca proiectul arhivat sa poata fi auditat/citit.
  - raspuns API:
    - HTTP 423 Locked.
    - mesaj: `Project is archived. Restore it before making changes.`
- backend/routers/calendar.py:
  - acoperite explicit endpointurile care scriau folosind doar `check_project_permission`:
    - create/update/delete availability.
    - update/delete calendar event.
    - delete recurring event series.
  - proiectul arhivat nu mai permite nici autorului evenimentului sa modifice.
- backend/routers/github.py:
  - owner actions pentru GitHub integration/ngrok pe proiect sunt blocate daca proiectul este arhivat.
  - webhook processing ignora taskurile din proiecte arhivate, ca sa nu creeze comentarii/audit/status transitions automat.
  - confirmarea manuala PR era deja acoperita prin `require_project_permission`.
- backend/routers/notifications.py:
  - `generate-risk` este blocat pe proiect arhivat, desi foloseste `REPORT_VIEW`, pentru ca endpointul creeaza notificari.

Impact arhivare:
- Board, Tasks, Backlog, Team, Settings, Calendar, DevOps, Documentation si AI actions sunt protejate server-side.
- Support ramane functional, pentru ca flow-ul lui este in zona admin/support tickets si nu depinde de mutatiile proiectului.
- UI-ul existent cu banner + read-only overlay ramane un strat vizual; backend-ul este acum sursa de siguranta.

Implementat browser branding:
- frontend/app/layout.tsx:
  - titlul global schimbat din `SDLC AI Hub` in:
    - default: `SDLC Hub`.
    - template: `%s | SDLC Hub`.
  - descriere actualizata:
    - `Adaptive project management for Scrum, Kanban and Scrumban teams.`
  - metadata icons nu mai declara fallback catre `/favicon.ico`.
- frontend/app/favicon.ico:
  - inlocuit favicon-ul binar ramas din scaffold cu un favicon SDLC Hub 32x32.
  - iconita foloseste aceeasi directie vizuala ca `icon.svg`: fundal inchis, hexagon/noduri cyan-blue-green.
- frontend/app/icon.svg si manifestul raman sursele principale pentru icon/PWA.

Verificari batch BW:
- `.venv/bin/python -m py_compile backend/utils/permissions.py backend/routers/calendar.py backend/routers/github.py backend/routers/notifications.py backend/routers/projects.py`: OK.
- `npm run lint`: OK.
- `npm run build`: OK.
- Warning-ul `--localstorage-file` ramane acelasi warning de mediu identificat in batch BU, dar build-ul trece.
- Cautare frontend:
  - nu mai exista `SDLC AI Hub`.
  - nu mai exista referinta metadata la `favicon.ico`.
- Verificare favicon:
  - `frontend/app/favicon.ico`: ICO valid, 32x32, 32-bit.

Fisiere cheie batch BW:
- backend/utils/permissions.py
- backend/routers/calendar.py
- backend/routers/github.py
- backend/routers/notifications.py
- frontend/app/layout.tsx
- frontend/app/favicon.ico
- PLAN_CONTINUARE_LICENTA_SDLC_HUB.txt

Ce ramane imediat dupa batch BW:
1. Realtime fin:
   - verificat unde inca pare refresh complet.
   - comentarii/task updates fara refetch greu unde se poate.
2. Functionalitati ramase pentru finalizare:
   - eventuale goluri din DevOps PR mapping.
   - polish UI/UX final cu Playwright screenshots.
   - cleanup final document plan si instructiuni de continuare.


6.28. BATCH BX - Support tickets hardening si conversatii complete

Context:
- Continuare dupa arhivare/branding.
- Prioritate ramasa:
  - Support tickets user/admin trebuie sa fie complet functionale.
  - Comentariile multe nu trebuie sa para ascunse sau taiate.
  - Stergere ticket/comentariu trebuie sa fie robusta.

Implementat backend:
- backend/routers/admin.py:
  - adaugate constante de validare:
    - prioritati permise: LOW, MEDIUM, HIGH, CRITICAL.
    - statusuri permise: OPEN, IN_PROGRESS, RESOLVED, CLOSED.
  - adaugate helpers:
    - `normalize_support_priority`.
    - `normalize_support_status`.
    - `normalize_support_body`.
  - create ticket:
    - valideaza titlul dupa `strip`, nu doar prin Pydantic inainte de trim.
    - normalizeaza prioritatea.
  - update ticket:
    - normalizeaza si valideaza status/prioritate.
  - create comment:
    - refuza comentarii goale dupa `strip`.
  - stergerea ticket/comment era deja functionala:
    - Global Admin poate sterge orice.
    - userul poate sterge propriul ticket.
    - userul poate sterge doar propriile comentarii.

Implementat frontend:
- frontend/app/dashboard/support/page.tsx:
  - conversatia ticketului nu mai este intr-o cutie cu `max-height`.
  - toate comentariile se afiseaza natural in pagina; scroll-ul este al paginii.
- frontend/app/admin/page.tsx:
  - conversatia din Support Desk admin nu mai este limitata la `max-height`.
  - adminul vede toate comentariile in flux, fara sa para ca se opreste dupa cateva mesaje.

Verificari batch BX:
- `.venv/bin/python -m py_compile backend/routers/admin.py backend/utils/permissions.py backend/routers/calendar.py backend/routers/github.py backend/routers/notifications.py`: OK.
- `npm run lint`: OK.
- `npm run build`: OK.
- Warning-ul `--localstorage-file` ramane acelasi warning de mediu identificat in batch BU, dar build-ul trece.

Fisiere cheie batch BX:
- backend/routers/admin.py
- frontend/app/dashboard/support/page.tsx
- frontend/app/admin/page.tsx
- PLAN_CONTINUARE_LICENTA_SDLC_HUB.txt

Ce ramane imediat dupa batch BX:
1. Functionalitati ramase pentru finalizare:
   - eventuale goluri din DevOps PR mapping.
   - polish UI/UX final cu Playwright screenshots.
   - cleanup final document plan si instructiuni de continuare.


6.29. BATCH BY - Realtime debounce si fix Tasks event name

Context:
- Dupa support hardening, urmatorul punct ramas era realtime fin.
- Problema:
  - multe pagini agregate faceau refetch imediat pentru fiecare eveniment WebSocket.
  - cand o actiune genera mai multe evenimente apropiate, UI-ul se simtea ca un reload.
  - pagina Tasks asculta `task.created/task.updated/task.deleted`, dar backend-ul emite `task.changed`.

Implementat:
- frontend/hooks/use-realtime-event.ts:
  - adaugat `useDebouncedRealtimeEvent`.
  - debounce implicit: 350ms.
  - accepta `shouldQueue(message)` ca filtru inainte de debounce.
  - motiv:
    - evenimente irelevante, precum presence, nu pot anula un refresh relevant.
    - mai multe evenimente relevante apropiate devin un singur refetch.
- frontend/app/dashboard/page.tsx:
  - Dashboard foloseste debounce pentru:
    - task.changed;
    - sprint.changed;
    - calendar.changed;
    - user.updated.
- frontend/app/dashboard/board/page.tsx:
  - Board foloseste debounce pentru:
    - task.changed;
    - sprint.changed;
    - project.changed.
- frontend/app/dashboard/tasks/page.tsx:
  - fix event name:
    - inainte asculta task.created/task.updated/task.deleted.
    - acum asculta `task.changed`, conform backendului.
  - refetch debounced pentru task/project/sprint changes.
- frontend/app/dashboard/backlog/page.tsx:
  - refetch debounced pentru task/sprint changes.
- frontend/app/dashboard/calendar/page.tsx:
  - refetch debounced pentru calendar/task/sprint changes.
- frontend/app/dashboard/documentation/page.tsx:
  - refetch debounced pentru documentation/task changes.
- frontend/app/dashboard/team/page.tsx:
  - refetch debounced pentru user/project changes.
- frontend/app/dashboard/notifications/page.tsx:
  - refetch debounced pentru notification created/read/read_all.

Lasat intentionat neschimbat:
- frontend/app/dashboard/tasks/[taskId]/page.tsx:
  - deja aplica local comentarii create/update/delete din payload realtime.
  - ignora `task.changed` cand actiunea vine din comentarii.
  - pastreaza refetch doar pentru schimbari reale ale taskului sau user updates.
- frontend/components/dashboard/app-sidebar.tsx:
  - badge-ul de notificari ramane imediat pentru feedback rapid.

Impact asteptat:
- Mai putine refetch-uri duplicate.
- Tasks se actualizeaza corect in realtime.
- Board/Dashboard/Calendar/Backlog nu mai par ca se reincarca de mai multe ori pentru aceeasi actiune.

Verificari batch BY:
- `npm run lint`: OK.
- `npm run build`: OK.
- Warning-ul `--localstorage-file` ramane acelasi warning de mediu identificat in batch BU, dar build-ul trece.

Fisiere cheie batch BY:
- frontend/hooks/use-realtime-event.ts
- frontend/app/dashboard/page.tsx
- frontend/app/dashboard/board/page.tsx
- frontend/app/dashboard/tasks/page.tsx
- frontend/app/dashboard/backlog/page.tsx
- frontend/app/dashboard/calendar/page.tsx
- frontend/app/dashboard/documentation/page.tsx
- frontend/app/dashboard/team/page.tsx
- frontend/app/dashboard/notifications/page.tsx
- PLAN_CONTINUARE_LICENTA_SDLC_HUB.txt

Ce ramane imediat dupa batch BY:
1. Functionalitati ramase pentru finalizare:
   - verificare DevOps PR mapping/manual confirm.
   - polish UI/UX final cu Playwright screenshots.
   - cleanup final document plan si instructiuni de continuare.


6.30. BATCH BZ - Browser branding final si DevOps PR realtime

Context:
- In browser inca se vedea uneori titlul vechi / favicon din scaffold.
- DevOps avea fluxuri functionale, dar:
  - pagina Pull Requests era hardcoded pe dark UI.
  - webhook-urile GitHub nu emiteau un eveniment realtime dedicat pentru DevOps.
  - confirmarile manuale PR actualizau taskul, dar nu anuntau explicit pagina DevOps/PR.
  - auditul de schimbare status din webhook PR era in ramura gresita.

Implementat branding browser:
- frontend/app/layout.tsx:
  - metadata title ramane `SDLC Hub`.
  - iconurile declarate explicit:
    - `/favicon.ico?v=sdlc-hub-2`
    - `/icon.svg?v=sdlc-hub-2`
    - `/apple-icon.svg?v=sdlc-hub-2`
  - motiv:
    - cache-bust pentru browserele care pastreaza faviconul Next.js sau titlul vechi.
- frontend/app/favicon.ico:
  - ramane favicon SDLC Hub valid 32x32.

Implementat backend DevOps:
- backend/routers/github.py:
  - `_handle_push` si `_handle_pull_request` intorc acum si taskurile afectate.
  - dupa `db.commit()`, webhook-ul emite:
    - `task.changed` pentru taskurile legate de commit/PR.
    - `github.changed` pentru proiectele afectate.
  - ping-ul GitHub emite `github.changed`.
  - confirmarea manuala PR emite atat:
    - `task.changed`;
    - `github.changed`.
  - detectarea de duplicate GitHub delivery prinde si delivery ID-urile salvate cu sufix pentru taskuri.
  - auditul `TASK_UPDATED/status` este scris cand PR-ul schimba efectiv statusul taskului.

Implementat frontend DevOps:
- frontend/app/dashboard/devops/page.tsx:
  - asculta realtime debounced pentru:
    - `github.changed`;
    - `task.changed`;
    - `project.changed`.
  - refetch fara loader complet pentru a evita senzatia de reload.
- frontend/app/dashboard/devops/pull-requests/page.tsx:
  - refacut UI-ul sa fie theme-aware light/dark.
  - adaugat `WorkspaceLoadingSkeleton`.
  - adaugat realtime debounced pentru PR/webhook/task changes.
  - butoanele de confirmare sunt dezactivate daca proiectul este arhivat.
  - modalul de confirmare este mai responsive si foloseste culori compatibile cu light/dark.

Verificari batch BZ:
- `.venv/bin/python -m py_compile backend/routers/github.py backend/utils/permissions.py backend/routers/calendar.py backend/routers/notifications.py`: OK.
- `npm run lint`: OK.
- `npm run build`: OK.
- Warning-ul `--localstorage-file` ramane warning-ul de mediu identificat anterior, dar build-ul trece.

Fisiere cheie batch BZ:
- backend/routers/github.py
- frontend/app/layout.tsx
- frontend/app/dashboard/devops/page.tsx
- frontend/app/dashboard/devops/pull-requests/page.tsx
- PLAN_CONTINUARE_LICENTA_SDLC_HUB.txt

Ce ramane imediat dupa batch BZ:
1. Functionalitati ramase pentru finalizare:
   - verificare finala DevOps pe task detail: events GitHub sub comentarii si audit corect.
   - polish UI/UX final cu Playwright screenshots.
   - cleanup final document plan si instructiuni de continuare.


6.31. BATCH BZ2 - Task detail GitHub events pe endpoint corect

Context:
- In task detail exista sectiunea `Development links` sub comentarii.
- Problema:
  - pagina incarca evenimentele GitHub la nivel de proiect si le filtra local.
  - pentru proiecte cu multe evenimente, taskul putea sa nu primeasca exact evenimentele lui.
  - in debugging au aparut 404-uri / date lipsa legate de evenimentele GitHub pe task.

Implementat:
- frontend/app/dashboard/tasks/[taskId]/page.tsx:
  - inlocuit `getProjectGitHubEvents(...)` cu `getTaskGitHubEvents(task.id, 120)`.
  - sectiunea `Development links` ramane sub comentarii, asa cum s-a cerut.
  - filtrarea client-side ramane defensiva, dar datele vin acum din endpointul dedicat taskului.

Verificari batch BZ2:
- `npm run lint`: OK.
- `npm run build`: OK.
- Warning-ul `--localstorage-file` ramane warning-ul de mediu identificat anterior, dar build-ul trece.

Fisiere cheie batch BZ2:
- frontend/app/dashboard/tasks/[taskId]/page.tsx
- frontend/services/github.ts
- backend/routers/github.py
- PLAN_CONTINUARE_LICENTA_SDLC_HUB.txt

Ce ramane imediat dupa batch BZ2:
1. Functionalitati ramase pentru finalizare:
   - polish UI/UX final cu Playwright screenshots.
   - verificare performanta pe pagini cu liste mari.
   - cleanup final document plan si instructiuni de continuare.


6.32. BATCH BZ3 - Request dedupe pentru performanta pe servicii

Context:
- Dupa realtime, skeletons si optimizari pe Activity, mai ramasese un pass de performanta pe servicii.
- Problema vizata:
  - unele pagini mari declanseaza GET-uri similare aproape simultan din layout, pagina curenta, realtime si componente copil.
  - asta poate crea senzatia de input lag sau navigare greoaie.

Implementat:
- frontend/lib/request-dedupe.ts:
  - helper nou `dedupeRequest(key, factory)`.
  - deduplicate doar request-uri GET simultane aflate in flight.
  - nu pastreaza cache dupa finalizarea requestului, deci urmatorul refresh ia date proaspete.
- frontend/services/task.ts:
  - dedupe pentru:
    - getProjectTasks;
    - getTaskDetail;
    - getProjectActivity.
- frontend/services/sprint.ts:
  - dedupe pentru getProjectSprints.
- frontend/services/calendar.ts:
  - dedupe pentru:
    - getProjectCalendarEvents;
    - getProjectCalendarAvailability.
- frontend/services/github.ts:
  - dedupe pentru:
    - getProjectGitHubEvents;
    - getTaskGitHubEvents;
    - getProjectPullRequests;
    - getProjectGitHubIntegration;
    - getNgrokTunnelStatus.

Impact asteptat:
- Mai putine request-uri duplicate la montarea paginilor mari.
- Mai putin zgomot cand vin evenimente realtime apropiate.
- Fara stale cache persistent, pentru ca dedupe-ul dispare cand request-ul s-a terminat.

Verificari batch BZ3:
- `npm run lint`: OK.
- `npm run build`: OK.
- Warning-ul `--localstorage-file` ramane warning-ul de mediu identificat anterior, dar build-ul trece.

Fisiere cheie batch BZ3:
- frontend/lib/request-dedupe.ts
- frontend/services/task.ts
- frontend/services/sprint.ts
- frontend/services/calendar.ts
- frontend/services/github.ts
- PLAN_CONTINUARE_LICENTA_SDLC_HUB.txt

Ce ramane imediat dupa batch BZ3:
1. QA final:
   - polish UI/UX final cu Playwright screenshots.
   - verificare manuala pe fluxuri critice:
     - metodologia ramane corecta dupa refresh;
     - proiect arhivat este read-only;
     - invitatiile pending pot fi acceptate din Account;
     - GitHub events apar in DevOps si Task Detail;
     - light/dark/mobile nu au overflow evident.
2. Cleanup final document plan si instructiuni de continuare.


6.33. BATCH BZ4 - Branding cleanup si audit vizual partial

Context:
- Utilizatorul a observat ca in browser inca putea aparea numele vechi / iconita scaffold.
- Dupa cache-bust pe metadata, am curatat si textele active ramase in backend.

Implementat:
- backend/config.py:
  - `app_name` schimbat in `SDLC Hub API`.
- backend/main.py:
  - mesajul root API schimbat in `SDLC Hub API is running`.
- backend/utils/email.py:
  - subiectul mailului de activare schimbat in `Activate your SDLC Hub Account`.
- frontend/app/layout.tsx:
  - iconurile raman versionate cu `?v=sdlc-hub-2`.

Verificari branding:
- Cautare in cod activ:
  - `rg "SDLC AI Hub|SDLC AI HUB" backend frontend/app frontend/components frontend/services --glob "!*.bak*"`: fara rezultate.
- `.venv/bin/python -m py_compile backend/main.py backend/config.py backend/utils/email.py backend/routers/github.py`: OK.
- `npm run lint`: OK.
- `npm run build`: OK.

Audit vizual Playwright:
- Rulat `scripts/visual_audit_playwright.js` pe light/dark + mobile/desktop.
- Raport generat:
  - `/tmp/sdlc-visual-audit/report.json`.
- Observatie importanta:
  - auditul a rulat cu storage state generat manual, dar backend-ul activ pe portul 8000 a refuzat tokenul cu 401.
  - cauza probabila: proces backend vechi sau pornit cu alt `SECRET_KEY` fata de mediul curent.
  - din acest motiv raportul este util doar pentru overflow/contrast de layout, nu pentru QA functionala autentificata.
- Rezultat layout din audit:
  - `docOverflow: 0` pe rutele verificate.
  - `outOfBounds: 0` pe rutele verificate.
  - `darkInLight: 0` pe rutele verificate.
  - 401-urile trebuie ignorate pana cand backend-ul este repornit cu env-ul curent si storage-ul Playwright este regenerat prin login real.

Fisiere cheie batch BZ4:
- backend/config.py
- backend/main.py
- backend/utils/email.py
- frontend/app/layout.tsx
- PLAN_CONTINUARE_LICENTA_SDLC_HUB.txt

Ce ramane imediat dupa batch BZ4:
1. QA final autentificat dupa restart backend/frontend cu env curent:
   - login real;
   - regenerare storage state Playwright;
   - rerulare audit vizual.
2. Test manual final pe fluxuri critice:
   - auth/mail;
   - methodology transition;
   - task CRUD/subtask/comment/audit;
   - calendar availability/events;
   - DevOps setup/events/PR confirm;
   - support tickets user/admin;
   - account settings/avatar/sessions.


7. OBSERVATII IMPORTANTE

- Nu sterge fisierele .bak_batch si scripturile batch fara confirmare; au fost folosite ca backup/istoric.
- Worktree-ul este murdar cu multe modificari si fisiere noi. Nu rula git reset/checkout.
- Pentru modificari manuale foloseste apply_patch.
- Dupa modificari backend cu modele/tabele, creeaza migrare Alembic si ruleaza upgrade head.
- Dupa modificari frontend, ruleaza lint si build.
- Daca schimbi comportamentul metodologiilor, verifica mereu toate cele trei moduri: SCRUM, KANBAN, SCRUMBAN.
